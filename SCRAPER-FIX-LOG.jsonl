{"date": "2026-06-28", "scrapers": ["puppeteer-config.js"], "category": "code-bug", "summary": "Fixed Windows-specific Puppeteer/Chrome launch failures (invalid --no-zygote flag, wrong Chrome path, needed bundled-Chromium fallback) that crashed every scraper on Windows."}
{"date": "2026-06-29", "scrapers": ["Cecil-County", "LibraryMarket"], "category": "code-bug", "summary": "Fixed Nominatim/geocoding 429 rate-limit floods for Cecil County Library and LibraryMarket scrapers, plus an SSL cert error in LibraryMarket."}
{"date": "2026-06-29", "scrapers": ["PG-Parks"], "category": "code-bug", "summary": "PG Parks scraper was using the wrong Firestore-style save path instead of Supabase, plus a geocoding bug."}
{"date": "2026-06-29", "scrapers": ["Venue-Events-ChildrensMuseums"], "category": "site-change", "summary": "Children's museums scraper needed date-extraction fixes, a switch to the source site's REST API, and URL corrections after the source sites changed."}
{"date": "2026-06-30", "scrapers": ["scraper-trio-06-30"], "category": "site-change", "summary": "Fixed geocoding, URL rot, and event extraction issues across three scrapers in one pass."}
{"date": "2026-07-01", "scrapers": ["State-Parks-Events"], "category": "site-change", "summary": "GA and KY state parks event extraction broke after their calendar pages changed."}
{"date": "2026-07-01", "scrapers": ["Group1-ALL"], "category": "code-bug", "summary": "Fixed two issues surfaced by the first Group 1 run after the eastern-region rollout."}
{"date": "2026-07-02", "scrapers": ["LibCal", "Communico"], "category": "code-bug", "summary": "Several LibCal/Communico state scrapers were registered without their export function actually existing, or missing from the registry entirely; added the missing exports and registry entries."}
{"date": "2026-07-02", "scrapers": ["Pratt-Library", "State-Parks-Events", "local-scraper-runner.js"], "category": "code-bug", "summary": "Fixed Pratt Library's ~980 request failures, FL State Parks geocoding, and a broken runner checkpoint path found in the July 1 run."}
{"date": "2026-07-03", "scrapers": ["local-scraper-runner.js"], "category": "code-bug", "summary": "Fixed closure/shutdown notices being misreported, incorrect runner stats, and Windows Chrome path detection."}
{"date": "2026-07-03", "scrapers": ["scripts/data-quality-check.js"], "category": "code-bug", "summary": "Fixed data-quality false positives, wrong category slugs, and dateless-event handling in the audit scripts."}
{"date": "2026-07-03", "scrapers": ["scripts/fix-broken-event-dates.js"], "category": "code-bug", "summary": "Dateless-events cleanup script crashed on invalid day numbers (e.g. \"June 51, 2026\") instead of treating them as unparseable."}
{"date": "2026-07-03", "scrapers": ["Communico-MA (Worcester)", "Drupal-PA"], "category": "site-change", "summary": "Worcester Public Library was registered on the wrong platform scraper (platform mismatch); Drupal-PA was failing silently with no error surfaced."}
{"date": "2026-07-05", "scrapers": ["puppeteer-config.js"], "category": "code-bug", "summary": "Fixed a broken Chromium launch path, an SPA rendering timeout, and redundant geocoding calls across several scrapers."}
{"date": "2026-07-05", "scrapers": ["event-save-helper.js"], "category": "code-bug", "summary": "Invalid-date and duplicate-event skip counts were merged into one \"skipped\" counter, so invalid-date extraction bugs were misreported as ordinary duplicates. Split into separate counters."}
{"date": "2026-07-05", "scrapers": ["Communico-FL (OCLS)"], "category": "code-bug", "summary": "Fixed OCLS date extraction pulling the wrong/unparseable date text."}
{"date": "2026-07-05", "scrapers": ["Venue-Events-ZoosAquariums (SC Aquarium)", "Hampton", "Communico-TN"], "category": "site-change", "summary": "SC Aquarium's URL changed and needed a longer timeout; Hampton's events moved into an iframe requiring frame-aware scraping; removed dead Communico-TN entries."}
{"date": "2026-07-06", "scrapers": ["local-scraper-monitor.js", "LibCal-NH", "Kelley Library"], "category": "code-bug", "summary": "Fixed scraper-monitor Firestore crash and LibCal-NH crash/save-bug/selectors; moved Kelley Library to its correct platform scraper."}
{"date": "2026-07-07", "scrapers": ["WordPress-PA"], "category": "code-bug", "summary": "Date-extraction regex accepted calendar-widget chrome (day-grid cells, month/year pickers) as valid dates, silently rejecting ~91% of real events downstream as invalid-date."}
{"date": "2026-07-07", "scrapers": ["WordPress-ALL"], "category": "seed-data", "summary": "Removed ~5,500 fabricated {city}library.org domains and repaired ~900 more across all 44 WordPress-{state} library scrapers (auto-generated seed data was never verified against real domains); restored 3 libraries wrongly flagged dead and fixed Birmingham AL's domain in follow-up passes."}
{"date": "2026-07-07", "scrapers": ["local-scraper-runner.js"], "category": "code-bug", "summary": "scraper-summary.log formatting was inconsistent between the live per-scraper line and the end-of-run recap table (InvalidDate silently omitted when zero); unified into one formatter used by both."}
{"date": "2026-07-07", "scrapers": ["ChildrensTheater-Eastern", "Venue-Events-ZoosAquariums", "Venue-Events-GardensNature", "YMCA-Community-Eastern", "Venue-Events-Tourism", "Venue-Events-ChildrensMuseums", "Venue-Events-ScienceArts"], "category": "site-change", "summary": "URL rot sweep across 7 multi-venue scrapers: stale /events paths on real domains repaired via homepage-crawl probing, 6 genuinely dead YMCA/JCC domains removed."}
{"date": "2026-07-08", "scrapers": ["Brooklyn-Library"], "category": "code-bug", "summary": "logScraperResult() called with the wrong signature (one object instead of 3 positional args) crashed every run; the site also migrated to a BiblioCommons-style search portal requiring new selectors and date extraction."}
{"date": "2026-07-08", "scrapers": ["event-save-helper.js"], "category": "code-bug", "summary": "Same-day events were always compared against midnight, so any event scraped later than 00:00 local looked already-past regardless of its real start/end time. Now re-checks against the actual start/end time before rejecting a same-day event."}
{"date": "2026-07-08", "scrapers": ["scraper-registry.js"], "category": "code-bug", "summary": "A duplicate \"new states\" registry block silently overrode WordPress-RI's rotation group from 3 to 1 (and redundantly re-declared WordPress-MN); removed the accidental duplicates."}
{"date": "2026-07-09", "scrapers": ["BarnesNoble-Eastern"], "category": "site-change", "summary": "B&N rebuilt stores.barnesandnoble.com as a Next.js/MUI app: all 136 hardcoded store IDs are stale (some now silently resolve to a DIFFERENT physical store, e.g. our Dover-DE id now points to a real store in Eugene OR), and the old a[href*=/event/] extraction selector no longer matches anything. Diagnosed and deferred rather than patched selectors alone, since the scraper trusts our config's city/state/venue name rather than the live page - a selector-only fix would have started saving real events under the wrong store's location. Needs a real fix: rediscover current store IDs for all 136 targets, add a live JSON-LD BookStore cross-check (name/addressLocality/addressRegion) to skip any store where our config no longer matches, and rewrite extraction against the new .MuiCardContent-root card structure."}
{"date": "2026-07-09", "scrapers": ["BarnesNoble-Eastern"], "category": "code-bug", "summary": "Completed the rewrite deferred earlier the same day. Instead of rediscovering 136 store IDs, found the site's own JSON locator API (location/change/{zip} then /events, both undocumented) which returns every event within ~40mi of a zip with its own live storeId/name/address/city/state per event - eliminating the stale-ID risk entirely since venue data now always comes from the live response, never our config. Replaced all Puppeteer DOM-scraping with plain fetch(); STORES now used purely as search-point zips, deduped by eventId across overlapping searches, filtered to ACTIVE_STATES. Verified live: RI+MA test run saved 272 events, 0 errors, 0 geocode failures."}
{"date": "2026-07-09", "scrapers": ["macaroni-runner-group1.js", "macaroni-runner-group2.js", "macaroni-runner-group3.js"], "category": "code-bug", "summary": "The dedicated MacaroniKid runners (split out from local-scraper-runner.js in the April 2026 separation) built their scraper list from MACARONI_SCRAPERS filtered only by group, never calling isScraperActive/getActiveStates - so all 43 state files ran every rotation regardless of region-config.json, even though the non-Macaroni path already used getScrapersForGroupByRegion for exactly this. Added the same active-state filter to all three runners; verified via --dry-run that inactive-region states (CA, TX, WA, CO, etc.) now correctly drop out of each group's run list."}
{"date": "2026-07-09", "scrapers": ["events table cleanup"], "category": "other", "summary": "One-time cleanup following the macaroni-runner region-filter fix: deleted 4,481 pre-existing out-of-region MacaroniKid events (CA, MN, IA, HI, NE, SD, AZ, OR, LA, KS, AR, TX, CO, WA, MO, ID, AK - states in neither scraper region-config.json nor the frontend's ACTIVE_STATES list, so zero site visibility either way). Left 1,486 OH/IN/MI/IL/WI MacaroniKid events alone since those states ARE in the frontend's ACTIVE_STATES and are currently visible on the live site - deleting them was not this task's call to make unilaterally. Also deleted 38 borderline-review events (non-family: winery/wine-festival tastings, 18+ adult rec registrations, pub psychic/murder-mystery/quiz nights, brewery karaoke, drag bingo at a distillery) flagged by cleanup-nonfamily-events.js Tier 3 pattern matching and manually reviewed against fetched descriptions."}
{"date": "2026-07-10", "scrapers": ["Drupal-Pennsylvania"], "category": "code-bug", "summary": "0 events found in 3177s (53min) every run despite the feed domains being healthy (fast 200s). Root cause: geocodeAddress() was a local ad-hoc axios call to Nominatim with zero delay, zero timeout, and no shared rate-limit cooldown, hammering Nominatim on every event across 60 dates x 2 libraries and hitting sustained 429s. Swapped both call sites to the shared, cached, rate-limited geocodeWithFallback() from helpers/geocoding-helper.js. Verified live: 3 dates now complete in 38s with zero 429s (Photon resolving real branch addresses), extrapolating to ~12-13min for the full 60-day run instead of 53min."}
{"date": "2026-07-10", "scrapers": ["FreeLibrary-Philadelphia"], "category": "code-bug", "summary": "0 events found every run despite the site rendering fine under Puppeteer (past a Cloudflare JS challenge). The site now wraps each event in two <a href=/calendar/event/ID> tags - one around a thumbnail image with empty text, one around the real title - and the extraction's processedUrls dedup marked the href consumed on the first (empty-text) match, silently discarding the real title link for every single event. Fixed by checking for a non-empty title before consuming the href. Also swapped the same unrate-limited/uncached geocodeAddress() pattern as Drupal-Pennsylvania to the shared geocodeWithFallback() helper, since this scraper will now actually process real events every run. Verified live: page 1 now finds 20 events (was 0) and saves successfully."}
{"date": "2026-07-10", "scrapers": ["Miami-Dade-Library"], "category": "other", "summary": "Removed as a duplicate: mdpls.org/events is already scraped successfully by Communico-FL (520 found/345 new in recent runs) via the real Communico API. This bespoke scraper fought a client-side React/MUI widget whose JS crashes under Puppeteer (Uncaught RangeError: Maximum call stack size exceeded), so the events-grid never left its Loading events... placeholder; the DOM elements it did match instead captured the calendar's Focused day label text as every event's date, so all 7 extracted rows were misdated to today with no time and rejected as past. Removed from scraper-registry.js; file left on disk unregistered."}
{"date": "2026-07-10", "scrapers": ["Communico-MA", "Communico-MS", "LibCal-AL"], "category": "seed-data", "summary": "Removed three dead single-library registry entries that always logged a pointless 0 found. Communico-MA: the only MA library was already relocated to LibCal-MA years ago, leaving zero entries in this file's MA array. Communico-MS (Jackson-Hinds Library System) and LibCal-AL (Huntsville-Madison County Public Library): both source URLs are genuinely decommissioned (jhlibrary.libnet.info 302-redirects to google.co.uk; hmcpl.libcal.com 404s as no longer online), not transient outages - both libraries migrated to new platforms (Drupal 10 site for Jackson-Hinds, a React/MUI Intercept widget for Huntsville) that need a properly verified new scraper, not a guessed DOM patch. Disabled the source entries with explanatory comments and removed the now-dead registry entries/functions rather than leave them silently logging 0 every rotation. AL and MS both retain other library coverage (WordPress-AL/Communico-AL, WordPress-MS)."}
{"date": "2026-07-10", "scrapers": ["Communico-MA", "Communico-MS"], "category": "code-bug", "summary": "User asked to verify the two dead-entry removals from earlier today actually had replacement coverage before treating them as resolved. Communico-MS (Jackson-Hinds): confirmed - live-tested WordPress-MS's existing extraction against jhlibrary.org/events and it finds 23 real events. No action needed. Communico-MA (Worcester Public Library): the removal was WRONG - it trusted a stale comment claiming Worcester had moved to LibCal-MA, but LibCal-MA only ever had a TODO stub for it, never a real entry, so Worcester had zero coverage. Found the library's real live calendar at mywpl.libnet.info/events (same LibNet/Communico platform as other entries in this file) and re-added scrapeCommunicoMA with the correct URL. Verified live: 10 real events saved with correct branch-level geocoding, 0 errors."}
{"date": "2026-07-10", "scrapers": ["supabase-adapter.js"], "category": "code-bug", "summary": "Found while verifying Jackson-Hinds coverage: isJunkTitle()'s skip-link pattern only matched exact \"Skip to content/main/navigation\", missing common variants \"Skip to main content\" and \"Skip to current day\" seen on jhlibrary.org and other Drupal sites - these would have saved as garbage events. Broadened the regex to catch both variants; verified against a mix of junk and real titles."}
{"date": "2026-07-10", "scrapers": ["SCRAPER-DIAGNOSIS-PROMPT.md"], "category": "other", "summary": "Added a verify-before-archive convention to the diagnosis prompt after the Worcester incident above: before deleting a scraper/library entry as covered elsewhere, actually run the alleged replacement and confirm real event output for that specific library rather than trusting an existing comment; fully remove (not comment-out) entries confirmed dead with confirmed replacement coverage; explicitly flag as an open gap (not resolved) any entry disabled without a confirmed replacement."}
{"date": "2026-07-10", "scrapers": ["supabase-adapter.js", "Drupal-Pennsylvania", "FreeLibrary-Philadelphia", "Communico-MA"], "category": "code-bug", "summary": "Manually re-ran the two scrapers fixed earlier today; output showed paired lines like 'Skipping cancelled/closed event: X' immediately followed by 'success checkmark X', meaning cancelled/closed events (and by the same code path: past, non-family, adult-only, junk-title, dateless, invalid-date events) were correctly rejected at the DB layer but still logged as saved and counted in imported++ by the calling scraper. Root cause: the Firestore-compat db.collection().add() wrapper's silent-skip path and its real-insert success path both returned the same bare {id} shape, so the ~89 scrapers using this .add() pattern have no way to tell a rejected event from a real insert unless they check for a distinguishing field, which none of them do. Added skipped/skipReason fields to the silent-skip return and updated the three call sites already touched today (Drupal-PA x2, FreeLibrary-Philadelphia x2, Communico-multi-state x2) to check it and log/count accordingly. Verified directly: add() on a crafted cancelled-event doc now returns {skipped:true, skipReason}, a normal event still returns the plain success shape. NOT fixed: the same miscounting bug likely exists in the other ~85+ scrapers that call .add() without checking the return value - out of scope for this session, flagging for a future sweep."}
{"date": "2026-07-10", "scrapers": ["ADD-SWEEP"], "category": "code-bug", "summary": "Full sweep of the add()-miscounting bug found earlier today (Drupal-PA/FreeLibrary-Philadelphia/Communico), applied to every other scraper calling db.collection('events').add() without checking the result: 42 of the 43 MacaroniKid state files (scripted fix on an identical shared block; MD uses a different save path and was unaffected), 43 other library/festival/farm scrapers (scripted regex fix matching the common await-add-then-console.log-then-counter++ shape, tolerant of differing counter/field variable names), and 4 hand-fixed stragglers with nonstandard shapes: macaroni-kid-scraper.js and scraper-macaroni-nc-cloud.js (unregistered legacy variants, same simple pattern as the other MK files but no console.log), scraper-festivals-eastern-us.js (counter/log order reversed - registered and active), and scraper-farms-eastern-us.js (had an activity_id-FK-retry branch feeding a single downstream success log - registered and active, needed the addResult captured across both the initial attempt and the retry). All 89 modified files pass node -c. Verified live: CivicEngage-Libraries (0 real events, ran clean) and Intercept-Camden-NJ (6 real events saved, 4 correctly skipped via unrelated duplicate-check path, no false positives). Independently confirmed via a full-codebase regex scan that zero files still contain the vulnerable pattern. Also confirmed activities.add() is not affected - no scraper calls it and flattenActivity() has no matching skip-throw reasons."}
{"date": "2026-07-10", "scrapers": ["event-save-helper.js", "supabase-adapter.js"], "category": "code-bug", "summary": "Same miscounting bug class as the .add() sweep, found in the batch()/commit() path used by saveEventsWithGeocoding() and saveEventsSimple() (helpers/event-save-helper.js, shared by ~50 scrapers) plus 8 standalone scrapers calling db.batch() directly. batch.set() queues an operation and the caller increments saved/imported immediately, but the actual content check (past/cancelled/junk-title/non-family/adult-only/dateless/invalid-date) only happens later inside batch.commit(), which silently dropped rejected rows without telling the caller - so every commit-time rejection was still counted as saved. Fixed commit() to collect and return skippedReasons; updated both event-save-helper.js functions (4 commit call sites total) to net out saved/skipped using the existing categorized counters (pastEvent/noDate/invalidDate) where they map cleanly, generic skipped bucket otherwise. Fixed the 8 standalone batch()-calling scrapers individually: scraper-aarecparks-md.js, scraper-fairfax-parks-va.js, scraper-kidsoutandabout-dmv.js, scraper-montgomery-parks-md.js (active/registered), scraper-allegany-county-library-MD.js, scraper-kent-county-library-MD.js, scraper-ruth-enlow-library-MD.js, scraper-worcester-county-library-MD.js (already disabled as redundant duplicates by a prior session, fixed anyway for consistency - zero current production impact). Verified live: saveEventsWithGeocoding and saveEventsSimple both correctly returned saved:1/skipped:2 (and 1/1) for crafted cancelled-event test batches, matching actual DB row counts exactly; a live AARecParks-MD run completed cleanly with no regression."}
{"date": "2026-07-10", "scrapers": ["ADD-SWEEP-PART2"], "category": "code-bug", "summary": "Extended the same fix to the doc(id).set() single-write path (also silently returned bare undefined on both skip and success, same ambiguity as .add()) and the corresponding update-path callers: 44 MacaroniKid files' existing-event UPDATE branch (doc(existingDoc.id).set(eventDoc) then unconditional updated++ - separate code path from the .add()-based new-event branch already fixed in the earlier sweep; scraper-macaroni-md.js needed two manual fixes since its whole file uses a different structure than the other 43), scraper-port-discovery-md.js, and scraper-pratt-library-MD.js. Left scraper-festivals-eastern-us.js's analogous update-path alone - it already unconditionally buckets updates as skipped (not saved) for reporting, so it was never inflating a success count, just coarse-graining the skip reason; not the same bug class. Verified live: updating an existing DB row to cancelled content via .set() now correctly returns {skipped:true} and leaves the original row untouched instead of corrupting it or miscounting it as updated. All 57 files touched across both fixes pass node -c."}
{"date": "2026-07-11", "scrapers": ["Montgomery-Parks"], "category": "code-bug", "summary": "0 events saved every run (27 found, 0 new, 0 dupes, 0.8s - the fast runtime and 0/0 split was the tell) despite finding real events, only surfaced now because yesterday's batch-commit miscounting fix stopped falsely crediting these as saved. Root cause: the RSS feed's <pubDate> (when the WordPress post was published) was used as eventDate instead of the actual event occurrence date, so every event looked past the moment its RSS entry aged past today. The real date/time/location only exist on each event's individual page, which sits behind Cloudflare (blocks plain axios/curl). Rewrote the scraper to launch Puppeteer and visit each event's page for the real date/time (.content .date / .content .time selectors) and address, replacing the pubDate-based date entirely; also switched geocoding to use the real per-event address via geocodeWithFallback instead of only static keyword-matched park coordinates. Verified live: 21 of 27 events now genuinely saved (was 0), with correct future dates, times, and specific venue names confirmed in the DB. Runtime went from a fake 0.8s to a real 92.5s (visiting 27 individual pages), which is expected and reasonable. Louisville-Library's 0-found run the same day was checked and found to be a transient failure only - re-ran clean with 132 saved, 0 errors, no code change needed."}
{"date": "2026-07-11", "scrapers": ["Louisville-Library", "local-scraper-runner.js"], "category": "code-bug", "summary": "Correction to earlier same-day diagnosis: the 0/0/0/0 Louisville-Library line in today's run was NOT a transient failure as first assumed - the raw stdout log showed the scraper's own internal report said 144 saved, 1 skipped and it really did write 144 events to the DB. Root cause: scrapeLouisvilleLibrary() returned the raw {success, stats: logger.stats} shape straight from ScraperLogger.finish() instead of flattening it like every other logger.finish()-based scraper in the codebase does (return {imported: result.stats.new, skipped: ..., failed: ...}). local-scraper-runner.js's stats normalizer only unwraps a result.result nesting (the CloudFunction-wrapper convention from a prior incident), never a result.stats nesting, so every field read back as undefined and the whole run displayed as a total failure despite real data landing correctly. Fixed by flattening Louisville's return value to match the established convention, and also hardened the runner to unwrap result.stats defensively as a safety net for the next scraper that makes the same mistake (scraper-miami-dade-library-FL.js has the identical bug but is currently disabled/unregistered, so no live impact there). Verified live: raw return is now {imported:132, skipped:13, failed:0}, and running that through the runner's actual normalization logic produces Found:145 New:132 Duplicates:13 - correct and matching the scraper's own internal report."}
{"date": "2026-07-11", "scrapers": ["run-scrapers.bat", "task-scheduler/setup-tasks.ps1"], "category": "other", "summary": "User requested the daily fix-all --recent-only data quality pass run automatically after the morning scraper batch finishes, instead of manually. Chained it into scrapers/run-scrapers.bat (the file Task Scheduler's FunHive-Scrapers task actually executes) right after the scraper step, redirecting output to a new logs/fix-all-recent.log. Used chaining inside the existing bat rather than a second fixed-time Task Scheduler trigger because observed scraper runtimes vary widely day to day (409min to 10h+), so a fixed time would risk running fix-all while the scraper was still active. Also updated task-scheduler/setup-tasks.ps1 (the source-of-truth registration script) to match what's actually deployed - it previously pointed Task 1's action directly at node local-scraper-runner.js, but the live registered task already pointed at run-scrapers.bat (a prior undocumented change); fixed the drift and bumped ExecutionTimeLimit from 4h to 12h since 4h was already tight against observed runtimes even before adding the fix-all step. Verified live: ran fix-all.ps1 --recent-only twice with the exact working-directory/invocation pattern the bat file uses (cd to scrapers/, then powershell -File ../scripts/fix-all.ps1) and confirmed correct Supabase credential loading and successful completion through all 4 steps. Did not re-register the live Windows Scheduled Task (requires admin elevation, which this session doesn't have) - the .bat file change alone is sufficient for tomorrow's run since Task Scheduler already calls that file; only the ExecutionTimeLimit increase needs the user to re-run setup-tasks.ps1 as admin."}
{"date": "2026-07-12", "scrapers": ["task-scheduler/setup-tasks.ps1"], "category": "other", "summary": "Today's 6 AM scraper run died silently ~66 minutes in (Group 3, index 9, BiblioCommons-MA) before ever reaching the new fix-all chain - Task Scheduler's own LastTaskResult (3221225786 / 0xC000013A, a forced-termination code) plus the live task's StopIfGoingOnBatteries=True setting point to the machine switching to battery power mid-run and Task Scheduler killing the whole process tree, a pre-existing config issue unrelated to yesterday's fix-all chaining work. Updated setup-tasks.ps1 to explicitly disable DisallowStartIfOnBatteries/StopIfGoingOnBatteries on both tasks and switch LogonType to S4U so the task also survives the interactive session ending (lock/logoff/RDP disconnect) rather than dying with it. Not yet applied to the live registered task - requires the user to re-run setup-tasks.ps1 as admin, same as the still-pending ExecutionTimeLimit change from 2026-07-11. Manually resumed today's run via node local-scraper-runner.js --resume (picks up correctly at BiblioCommons-MA per the checkpoint's lastIndex-is-currently-running-minus-one semantics) chained with fix-all.ps1 --recent-only afterward, running in the background."}
{"date": "2026-07-16", "scrapers": ["event-save-helper.js", "supabase-adapter.js"], "category": "code-bug", "summary": "The 48 WordPress-{state} library scrapers generic DOM parser was matching mini-calendar day cells instead of real event cards on many sites, producing fake events like a bare day-summary badge title of 3 events or a bare day number with garbage/time-only dates. saveEventsWithGeocoding checked date validity before any junk-title check, so this junk was miscounted as an invalid-date parsing bug rather than filtered as junk, accounting for most of the high INVALID counts seen this run across WordPress-GA 1352, WordPress-NC 889, WordPress-CT 610, WordPress-VT 603, WordPress-AL 438, WordPress-TN 381. Exported the existing isJunkTitle from supabase-adapter.js, added a new pattern for the N events badge text, and now call isJunkTitle at the top of both saveEventsWithGeocoding and saveEventsSimple loops in event-save-helper.js before the date/geocode work, with a new junkTitle skip counter. Not yet re-run to confirm the INVALID counts drop."}
{"date": "2026-07-16", "scrapers": ["CivicEngage-Libraries"], "category": "seed-data", "summary": "CivicEngage-Libraries found 0 events again this run (single active entry, Colonial Heights Public Library). Verified live: the configured URL calendar.aspx?CID=25 is the general city calendar with no library events; Colonial Heights own calendar filter list identifies CID=8 as the Library category. Updated the URL to CID=8. Not yet re-run to confirm library events actually populate under that CID."}
{"date":"2026-07-16","scrapers":["supabase-adapter.js"],"category":"code-bug","summary":"Yesterday's junk-title fix for the WordPress-{state} calendar day-cell badge pattern used an end-anchored regex (N events, nothing after), but the real DOM text glues the day number onto the badge via newlines (e.g. '3 events,' followed by whitespace and '30'), so it never matched and today's run still showed WordPress-GA at 1012 invalid out of 1920 found. Relaxed the pattern to drop the end anchor. Verified against real logged samples: now correctly catches the badge junk. However, a deeper look at today's WordPress-GA log showed this junk pattern was only ever a minor slice (88 of 1012 invalid-date entries, about 9 percent) - the dominant cause (810 of 1012, about 80 percent) is real, legitimately-titled events whose extracted date is time-only with no day or month at all (e.g. Patriotic Bracelets with date '10:30 am - 11:30 am'), because the actual date lives in a separate calendar day-group heading the per-event extraction never associates with its card. Spot-checked one GA library (Athens) and found it actually redirects to a LibCal widget, but NC libraries checked did not, so the underlying platforms are too heterogeneous across the 48 WordPress-state files for one generic fix. Not resolved - flagging as a real, large, systemic limitation of the generic WordPress library parser that needs per-platform investigation, not a quick patch."}
{"date":"2026-07-16","scrapers":["CivicEngage-Libraries"],"category":"seed-data","summary":"Yesterday's CID fix (25 to 8) deployed correctly - confirmed live via today's log showing the corrected URL and page title 'Calendar - Colonial Heights VA - CivicEngage'. Still 0 events found (monthItem:0 listItem:0 calItem:0 tableEvents:0). Independently verified via WebFetch that the Library category calendar genuinely shows no events for the current July 2026 view. This looks like a real absence of posted events rather than a scraper bug at this point - leaving as-is, but flagging since it cannot be fully distinguished from a selector mismatch without checking a month known to have library programming."}
{"date":"2026-07-16","scrapers":["Drupal-Pennsylvania"],"category":"code-bug","summary":"Confirmed NOT transient (0/0/0/0 for two consecutive runs, 2026-07-15 and 2026-07-16) despite the scraper genuinely working - today's raw log showed it correctly scraped both Lancaster and York feeds, filtered out 1718 adult programs (book clubs, mahjong, knitting), and saved 10 real family events (Toddler Time, Adopt a Dinosaur Day, Farmers Market Swap Meet, etc). Root cause: scraper-registry.js pointed exportName at scrapeDrupalPennsylvaniaLibraries, the plain function version which has no return statement, instead of scrapeDrupalPennsylvaniaLibrariesCloudFunction which flattens ScraperLogger stats into imported/skipped/failed. local-scraper-runner.js received undefined back from the plain function and every stat defaulted to 0. Fixed by pointing the registry at the CloudFunction export. Swept the full registry for the same exportName-has-a-CloudFunction-sibling-but-points-at-a-no-return-body pattern across all scrapers - no other matches found, appears to be an isolated case."}
{"date":"2026-07-16","scrapers":["local-scraper-runner.js"],"category":"code-bug","summary":"User asked whether every scraper in the last two runs had actually been checked, prompting a full line-by-line pass instead of just the flagged ones. Found that Pratt-Library, BiblioCommons-NJ, BiblioCommons-VA, Fairfax-Parks, KidsOutAndAbout-DMV, AACPL, and all five Activities-*-DMV venue scrapers were reporting 0 Duplicates and 100 percent New on every run, two days in a row. Investigated two different root causes: (1) Pratt-Library/BiblioCommons/Fairfax-Parks/KidsOutAndAbout-DMV/AACPL use a direct set-with-merge upsert keyed by a stable content-hash id (confirmed via the underlying supabase.upsert with onConflict:'id') with no separate read-before-write check, so they genuinely cannot distinguish a fresh insert from a re-confirm without an extra read - this is an intentional egress-saving trade-off already documented in CLAUDE.md, not a bug, and there is no real duplicate-row problem since upsert-by-id prevents that. (2) The five Activities-*-DMV venue scrapers (and likely other 'Activity scraper' shaped ones) genuinely do track saved vs updated internally via getOrCreateActivity's isNew flag, but local-scraper-runner.js's stats normalizer folded saved+updated into the New column with no way for updated to ever surface as Duplicates, so every re-confirmed venue was silently miscounted as new. Fixed the normalizer to only count saved as New and route updated into the Duplicates slot alongside the existing duplicates/skipped fallback. Verified against four representative return shapes (Activities day-1 all-new, Activities day-2 mostly-updated, Pratt's imported/skipped shape, WordPress's found/new/duplicates/invalidDate shape) - only the Activities shape changes output, and it now correctly reflects new vs re-confirmed. Also reviewed the rest of both runs (FairsFestivals-Eastern 4670 dupes, EventON-Lexington 126 dupes, etc) - duplicate detection is working normally everywhere else."}
{"date":"2026-07-17","scrapers":["data-quality-check.js"],"category":"code-bug","summary":"User asked why 51 registered scrapers (LibCal-CO/LA/OH, Communico-CA/IL/WI, BiblioCommons-AZ/CO/MO/OH, SanDiegoLibrary, SanAntonio-Library, LA-Public-Library, DesMoines-Library and others) were reported as 'Registered but never ran'. Not a scraper problem: every one targets a state in the central or western region, both marked active:false in region-config.json, so the runners correctly skip them via isScraperActive and they never write scraper_logs rows. Root cause was in the check script itself - the variable named ACTIVE_SCRAPERS was assigned the entire unfiltered SCRAPERS registry, so the never-ran pass at line 1161 walked all 221 entries and flagged the 70 inactive-region ones for doing exactly what they should. The registry already exported getActiveStates and isScraperActive and the runners used them; the check script just never called them. Fixed by applying the same gate at registry load. Verified 221 total to 151 active with all 51 reported entries in the filtered-out set; the catch-block fallback still works since isScraperActive returns true when activeStates is null. Confirmed data-quality-quick.js and scraper-fix-trends.js never require the registry so they lack the blind spot, and left fix-event-state.js unfiltered on purpose since it needs state hints for all scrapers to infer state on pre-existing rows. Also corrected the stale group counts in CLAUDE.md (was 51/49/45, actually 50/52/49)."}
{"date":"2026-07-18","scrapers":["Allentown-Public"],"category":"site-change","summary":"0/0/0/0 for 3 consecutive Group 3 runs (2026-07-12, 2026-07-17, 2026-07-18). Raw log showed every scraped event dated Feb-Oct 2024, all correctly rejected as past - the site's /events/ path is a stale WordPress post archive stuck at 2024, confirmed live via WebFetch (page 1 and homepage both show only 2024 posts, 105 pages of archive). Found the real live calendar at /library-calendar/ via homepage nav - confirmed via WebFetch it shows genuine July 2026 events (Fire Truck at the Library 7/21, Shakespeare for Kids 7/24, etc) with the same /page/N/ pagination pattern the scraper already uses. Repointed eventsPath from /events/ to /library-calendar/. Event permalinks still contain /events/{slug}/ so the existing link-extraction selector needs no change, and the MM/DD/YYYY date format and am/pm time format visible on the new page match the existing regexes. Could not inspect the page's literal CSS class names for individual event containers since this session had no shell access to fetch raw HTML and WebFetch only returns markdown - the existing generic selector fallback chain (article, .event-item, .wp-block-post, class-contains-event, .entry) was left as-is on the reasonable assumption it covers this WordPress Events Manager page too, but this is NOT independently confirmed. Needs a re-run to verify Found/New actually goes nonzero; if it's still 0, the selectors are the next thing to check with real DOM access."}
{"date":"2026-07-18","scrapers":["Wicomico-Public"],"category":"code-bug","summary":"Found:0/New:0/Duplicates:0 looked like a dead scraper but the raw log showed it genuinely found 22 events and skipped 21 as duplicates/adult plus 1 past - a reporting bug, not a coverage bug. Root cause: scrapeWicomicoEvents() tracks a skipped counter internally but its return statement only returned {imported, failed}, dropping skipped entirely, so local-scraper-runner.js's stats normalizer (which reads stats_src.skipped as the Duplicates fallback for this exact shape) always saw undefined and reported 0 across the board. Added skipped to the return statement. Verified against the normalizer logic in local-scraper-runner.js (dupCount = num(stats_src?.skipped) when no duplicates/invalidDate/updated fields are present) - this shape now flows through correctly. No site-side problem found; the library's page is working and mostly re-showing already-scraped recurring events, which is expected for a same-page re-scrape."}
{"date": "2026-07-20", "scrapers": ["date-normalization-helper.js"], "category": "code-bug", "summary": "Trailing numeric-date-range stripper fired unconditionally instead of only when a text-month date was already present, so an all-day event whose only date info was a numeric range like All Day 6/20-9/26 had that range deleted right after All Day was stripped, leaving nothing to parse. Guarded the strip on a month name still being present in the remainder; verified against live WordPress-NY samples."}
{"date": "2026-07-20", "scrapers": ["WordPress-NY", "WordPress-FL", "WordPress-NJ", "WordPress-MS", "WordPress-ME"], "category": "code-bug", "summary": "Root cause of most invalid-date skips on these 5 states, roughly 94 percent: on calendar-grid library sites the real date lives on the day-cell container, not the event card, so extraction captured a bare clock time with no day or month. Confirmed live on ypl.org/events (Yonkers Public Library, WordPress-NY): day cells carry data-date and a calendar day-header with the full date, event cards inside only show a time. Added a findAncestorDate DOM-walk that fires only when the card date has no day/month, walking up to 8 ancestor levels for a data-date attribute or day-header text. Verified live with a standalone Puppeteer test: 427 of 709 cards had no parseable date before, 427 resolve correctly after, 20 remain unparseable as unrelated noise. Applied identically to all 5 files. Not independently verified against every individual FL/NJ/MS library site - re-run these scrapers to confirm the INVALID column drops."}
{"date":"2026-07-20","scrapers":["Cecil-County"],"category":"code-bug","summary":"Flagged with the warning 0/0/0/0 marker for the first time today, but the raw log showed it genuinely found 24 events and correctly skipped all as duplicates/adults - same reporting-bug pattern as the earlier Wicomico/Louisville fixes. Root cause: scrapeCecilEvents() computed a full found/new/duplicates/errors object for logScraperResult() (the DB-facing scraper_logs write) but its actual return statement only included {imported, failed}, dropping skipped, so local-scraper-runner.js normalizer (which falls back to stats_src.skipped for this shape) saw undefined and displayed 0 across the board. Added skipped to the return statement. Verified live by re-running the scraper standalone: now correctly reports Found:24 New:5 Duplicates:19, no warning flag."}
{"date":"2026-07-20","scrapers":["WordPress-ME"],"category":"code-bug","summary":"Re-ran standalone to verify this morning's findAncestorDate fix (commit 6c2698a), since the group run that produced today's diagnosed 165 invalid-date figure had started hours before the fix landed. Confirmed real improvement across two independent post-fix runs (1:05 PM and 3:52 PM): Invalid dropped from a 165-378 pre-fix baseline to a stable 124, and New rose from ~104-126 to 153-237. Not a full resolution though - 124 invalid-date skips remain and are stable across both post-fix runs, so they're a different root cause than the calendar-grid ancestor-date pattern the fix targeted, consistent with the known finding that WordPress-{state} library sites run heterogeneous underlying platforms (see WordPress-GA 2026-07-16 entry). WordPress-NY/NJ/FL/MS also share this fix but were not individually re-run due to their long runtimes (40-67 min each); only WordPress-ME (19.5 min) was spot-checked."}
{"date":"2026-07-21","scrapers":["Allentown-Public"],"category":"code-bug","summary":"0/0/0/0 persisted for 5 consecutive Group 3 runs even after the 2026-07-18 URL repoint to /library-calendar/ (confirmed live via WebFetch that page shows genuine July 2026 events like Fire Truck at the Library 7/21). Raw log showed every page extracting exactly 1 fake event titled 'Library Events' dated June 29 2026 - the real page is a month-grid calendar table, but the selector loop tried 'article' first, matched the single page-wrapper article at length 1, and stopped immediately per 'if (eventElements.length > 0) break', never reaching the '[class*=event]' selector that would have matched real event cells. Fixed by preferring whichever selector yields more than 1 element, added 'td' to the selector list since WebFetch confirmed the real markup is a td-per-day calendar table, and widened the event-permalink selector to also match /library-calendar/ hrefs (old code only matched /events/, which the real permalinks do not contain). Not independently verified against literal HTML since curl/raw-HTML access was blocked in this session - needs a re-run to confirm Found goes nonzero; if still 0, the td-based grid assumption needs checking with real DOM access."}
{"date":"2026-07-21","scrapers":["Firespring-Libraries"],"category":"site-change","summary":"Firespring-Libraries covered exactly one library, Massanutten Regional Library (VA), whose URL https://mrlib.org/events/events/all-events.html now 404s (confirmed via WebFetch). The library's own site nav (mrlib.org/events/branches/) 301-redirects to https://mrlib.libcal.com/calendar?cid=22185, confirming the library migrated to the LibCal platform. Added a Massanutten Regional Library entry using that cid to the already-active LibCal-VA scraper (scraper-libcal-libraries-CA-CO-DE-FL-LA-MA-NY-SC-TN-TX-VA-WA.js), which today ran healthy (38 found/26 new for its other VA libraries). Removed the Firespring-Libraries registry entry since it had zero remaining coverage. Could not delete the now-dead scraper-firespring-libraries-VA.js file itself - Bash rm/git rm was denied in this session - so it's marked RETIRED in a file header comment; run 'git rm scrapers/scraper-firespring-libraries-VA.js' to finish removing it. Not independently confirmed via a live scraper run that Massanutten events actually come through LibCal-VA yet - WebFetch cannot execute the JS that renders LibCal's event list, so this needs a re-run of LibCal-VA to verify."}
{"date":"2026-07-21","scrapers":["CalendarWiz-Beaufort"],"category":"site-change","summary":"CalendarWiz-Beaufort dropped from a stable 8 found/run to 0 starting 2026-07-18 and stayed at 0 through today, 5 consecutive runs. Live check: the CalendarWiz URL now renders a blank 'CalendarWiz Welcome' placeholder with no embedded calendar. Beaufort County Library's own site nav points to https://beaufort.librarycalendar.com/events/upcoming, a LibraryMarket-platform calendar - and that exact URL is already registered for Beaufort County Library in LibraryMarket-SC (scraper-librarymarket-libraries-sc.js), which last ran 2026-07-16 with real, non-suspicious counts (38 found/14 new across its 2-library SC array), confirming live coverage already exists on the correct platform. Removed the CalendarWiz-Beaufort registry entry as redundant/dead. Could not delete scraper-calendarwiz-beaufort-county-sc.js itself - Bash rm/git rm was denied in this session - so it's marked RETIRED in a file header comment; run 'git rm scrapers/scraper-calendarwiz-beaufort-county-sc.js' to finish removing it."}
{"date":"2026-07-23","scrapers":["ActiveNet-Parks-Eastern"],"category":"code-bug","summary":"Summary stats bug present since at least 2026-07-08: found/new totals were computed from stateResults, which only accumulates saved-event counts per jurisdiction, not the actual number of events scraped. This made FOUND always equal NEW on every run and left DUPES, a separately-tracked and correct totalSkipped counter, looking disconnected from FOUND and sometimes exceeding it, e.g. 597 found vs 901 dupes on 2026-07-23. Added a totalFound accumulator that counts every jurisdiction's scraped events regardless of save outcome, and used it for both the console summary and the returned found value. No underlying duplicate-detection bug existed - DUPES itself was already correct, only FOUND/NEW were mislabeled."}
{"date":"2026-07-24","scrapers":["Supabase-project.js"],"category":"other","summary":"The entire 3:00 AM Group run and its chained fix-all --recent-only pass produced zero real database writes: the Supabase project itself is restricted for exceed_egress_quota (confirmed live via a direct select against events at 2:12 PM, still restricted hours after the run). Every scraper in the run shows Found>0 but New:0 because saveEvent/upsert calls all failed with the same restriction error, and 12 scrapers (Allentown-Public, WithApps-Libraries, RollyPollies-MD, WordPress-PA/MA/KY/SC/WV/DE/RI/NH, Assabet-NH-MA) hard-failed outright rather than just logging per-event errors. fix-all-recent.log Step 1 failed immediately with the same message and Total events: 0. This is not a code bug in any scraper - do not diagnose or re-fix the 0-New rows or the 12 failures as individual issues until this is resolved; they are all the same root cause. Also flagging that this restriction blocks ALL Supabase queries, so the live website is likely also broken for visitors right now. Needs the project owner to upgrade the Supabase plan or adjust spend caps in the Supabase dashboard billing/usage page - nothing in the codebase can fix this."}
{"date":"2026-07-25","scrapers":["Supabase-project.js"],"category":"other","summary":"Supabase egress-quota outage from 2026-07-24 is still active today, confirmed via a live direct select against events at time of diagnosis (still returns exceed_egress_quota restriction) and via both scraper-summary.log and fix-all-recent.log from the 3:00 AM run, which again show 15 hard-failed scrapers (FreeLibrary-Philadelphia, AACPL, Prince-Georges-County, Westmoreland-Library, CivicEngage-Libraries, Fairfax-Parks, Trumba-Spartanburg, WordPress-VA/GA/NC/CT/TN/AL/VT, LibCal-NH) and fix-all-recent.log Step 1 erroring immediately with Total events: 0, same as yesterday. Not a code bug - do not re-diagnose the 15 failures or any 0-New rows from today run until this clears. Needs the project owner to upgrade the Supabase plan or remove spend caps in the Supabase dashboard billing/usage page."}
{"date":"2026-07-31","scrapers":["Eventbrite-Family-Eastern"],"category":"code-bug","summary":"The 2026-07-30 Group 1 run silently hung forever partway through this scraper (checkpoint file still shows lastIndex 45 currentScraper Eventbrite-Family-Eastern from 2026-07-30T15:10:38Z, over 21 hours before this diagnosis ran) and blocked the entire rest of the rotation - 3 scrapers after it (KidsOutAndAbout-Eastern, SportsEngine-Youth-Eastern, ChildrensTheater-Eastern) never got a chance to run, and no 2026-07-31 run started at all, most likely because the previous instance never exited. Confirmed still-running node.exe and multiple chrome.exe processes on the machine at diagnosis time. Root cause: scrapeCity's Strategy 2 in-page fetch() calls (page.evaluate against Eventbrite's search API) had no timeout, and the per-city loop in the main function awaited scrapeCity() directly with no watchdog, so a single hung fetch (Eventbrite likely rate-limiting or dropping the connection) could freeze the whole process indefinitely with no error ever thrown. Added a 10s AbortController timeout to each in-page fetch endpoint attempt, and wrapped the main loop's scrapeCity call in a 90s Promise.race watchdog so future hangs move on to the next city instead of freezing the run. Did not kill the currently-running stuck process - process management is outside this session's tool access; user needs to end the lingering node.exe/chrome.exe processes in Task Manager (or reboot) before the next scheduled run. Not yet re-run to confirm the fix prevents a recurrence."}
{"date":"2026-07-31","scrapers":["WordPress-NY","WordPress-FL","WordPress-NJ","WordPress-MS","WordPress-ME"],"category":"code-bug","summary":"Investigated why WordPress-NJ's InvalidDate rate (28.8%) was nearly 3x FL/MS/NY/ME's (8-12%) despite NJ and FL running byte-identical DOM extraction code, ruling out a code difference between states. Live WebFetch against 5 NJ library sites confirmed Union Free Public Library and Verona Free Public Library both run WordPress's The Events Calendar (TEC) plugin, whose list view groups events under a bare day heading like Tue 4 with no month or year on the heading itself, only once at the top of the page - this markup carries no data-date attribute and no .calendar__day-header class, so the 2026-07-20 findAncestorDate fix (built against a different platform, ypl.org's Drupal calendar widget) never fires for it, and even if it did the heading alone lacks enough info to build a real date. Added scrapers/helpers/tec-rest-helper.js, which tries each library's TEC REST API (wp-json/tribe/events/v1/events) before falling back to DOM scraping - confirmed live against unionlibrary.org (110 events) and veronalibrary.org (78 events) returning clean start_date/end_date/title/description with correct dates and times, and confirmed it correctly returns null (falls through to existing DOM scraping) for a non-TEC site (westfieldlibrary.org). Wired the helper into all 5 state files ahead of their existing Puppeteer DOM-scrape loop. Not yet re-run as part of a full scraper rotation to confirm the InvalidDate counts drop in the summary log - WordPress-NY/FL/NJ/MS/ME are Group 2 and already ran earlier today, so verification needs to wait for the next Group 2 rotation. This fix generalizes beyond NJ - any WordPress-{state} library scraper hitting a TEC site benefits, so a future diagnosis session finding a high InvalidDate count on a WordPress library scraper should check for TEC (post_type=tribe_events / eventDisplay=list in the site's URLs) before assuming it needs another DOM-parsing patch."}
{"date":"2026-08-01","scrapers":["WordPress-NJ"],"category":"code-bug","summary":"Re-ran WordPress-NJ standalone to verify the 2026-07-31 TEC REST API fix. InvalidDate rate dropped from 637/2210 found (28.8%) to 324/3079 found (10.5%), now in line with FL/MS/ME's normal 8-12% baseline. Found and New both rose substantially too (2210 to 3079 found, 287 to 1850 new) - the REST API pulls each library's full paginated event list instead of whatever a single DOM snapshot happened to render, so this fix recovers real events the old DOM scraping was silently missing, not just fixing dates. Spot-checked one of the remaining invalid-date skips (Beach Haven Free Public Library) live against the TEC helper and confirmed it correctly returns null - genuinely not a TEC site, so detection is not mis-firing. The residual 324 are the same generic-WordPress-platform heterogeneity noted 2026-07-16, a separate already-documented limitation, plus a handful of unrelated Puppeteer navigation errors (timeouts, SSL failures) on non-TEC sites. Confirms the fix is working as designed; not re-tested against NY/FL/MS/ME individually since NJ was the outlier this was built to fix."}
{"date":"2026-08-01","scrapers":["WordPress-GA","WordPress-NC","WordPress-CT","WordPress-TN","WordPress-AL","WordPress-VT"],"category":"code-bug","summary":"User pointed at a 2026-08-01 run (a different rotation group than the NJ fix, run earlier the same day) showing these 6 scrapers with InvalidDate rates of 12.9-45.7 percent (GA 870/1903=45.7%, NC 548/1272=43.1%, AL 380/890=42.7%, TN 188/585=32.1%, CT 461/1567=29.4%, VT 178/1382=12.9%), higher in some cases than WordPress-NJ's original 28.8% outlier. Confirmed via grep that none of these 6 files contained findAncestorDate or tryFetchTecEvents - they never received either the 2026-07-20 ancestor-date fix or the 2026-07-31/08-01 TEC REST API fix, because both were scoped to whichever rotation group was under diagnosis at the time (NY/FL/NJ/MS/ME, a different group) and these 6 were simply never revisited, even though a 2026-07-16 entry already flagged this exact same scraper set for the same underlying WordPress-platform-heterogeneity problem. Same root cause, not a new bug. Wired tryFetchTecEvents into all 6 files (scraper-wordpress-libraries-{ga,nc,ct,tn,al,vt}.js), matching each file's existing selector-family and metadata-block conventions (GA/NC/AL match NJ/FL's broader eventSelectors array with platform:generic and needsReview:true; CT/VT match NY/ME/MS's tighter single-selector pattern). Verified all 6 pass node -c and load cleanly via require() with no network/DB calls triggered. Per explicit instruction, NOT re-run against live sites or the DB - the InvalidDate numbers above are the pre-fix baseline from before this commit. The next run of these 6 scrapers (next Group rotation, or a manual npm run scraper -- --scraper WordPress-GA etc from C:\\dev\\funhive-web) is needed to confirm the same improvement seen on WordPress-NJ (28.8% to 10.5%). Also added a note to SCRAPER-DIAGNOSIS-PROMPT.md Section 2 pointing future diagnosis sessions at tec-rest-helper.js and this entry, specifically so a diagnosis run reading a scraper-summary.log line dated on or before 2026-08-01 for these 6 scrapers does not re-diagnose them as a fresh unresolved problem."}
{"date":"2026-08-01","scrapers":["RecDesk-Parks"],"category":"code-bug","summary":"RecDesk-Parks found 0 events across all 46 sites in today's run (⚠️-flagged, 301.8s runtime meaning it genuinely queried every site rather than failing fast) - not previously in SCRAPER-FIX-LOG despite an intermittent pattern of 0-results runs on 2026-07-07 and 2026-07-25 interleaved with healthy runs (407-749 found) on 2026-07-04/07-10/07-16. Live curl against 3 different tenants (mprd.recdesk.com, hoboken.recdesk.com, jacksonvilleal.recdesk.com) with the scraper's exact request body (facilityId:0, plus date/view params) all returned HTTP 200 with a valid-shaped but empty Events array - not a network/auth failure. Downloaded hoboken.recdesk.com's live calendar page HTML and found the actual jQuery frontend sends facilityId as the selected value of the #FacilityID dropdown, whose \"All Facilities\" option has value=\"-1\", not 0 - confirmed by resending the identical request with facilityId:-1 against both hoboken and jacksonvilleal and getting real events back (Senior Zumba Class, NFL Flag Football, etc). facilityId:0 refers to a facility slot that generally doesn't exist, so it silently returns zero events on every tenant rather than erroring - this explains the historical intermittent pattern too (RecDesk's backend has apparently flip-flopped on whether 0 degrades to \"no facility\" or something more permissive across some past window). Changed scrapers/scraper-recdesk-parks.js line ~420 from facilityId:0 to facilityId:-1 (the fetchEventDetail's separate facId:...||0 fallback at line 469 is unrelated - it identifies a specific event's own facility, not a filter, and was left as-is). node -c clean. Re-ran the scraper standalone to confirm: 27816 found / 20831 new / 6985 duplicates / 0 invalid across all 46 sites (up from 0), confirming the fix. IMPORTANT - this verification run already wrote all 20831 new rows to the live production DB (this is a standard scraper run, not a dry-run tool) and flagged a real, separate concern that needs a human decision, not a further autonomous fix: RecDesk's API returns extremely granular data per site - a sample of the newly-saved rows includes many individual recurring practice sessions (e.g. repeated 'WAT Practice (Indoor Pool)' rows, one per date), adult recreational sports leagues (e.g. 'Men's Fall Softball - Tuesdays', 'Adult 16\" Softball'), and registration/fee line items with no real event content ('FALL VOLLEYBALL FREE AGENT', 'SUMMER SOFTBALL PLAYER FEE') - none of these trip the existing NON_FAMILY_PATTERNS/adult-only filters in supabase-adapter.js since those target explicit adult content (bars, 21+, singles nights), not adult-league sports or admin line items, so this is a curation-scope question (does FunHive want granular rec-league scheduling data at all) rather than a bug, and was left unfiltered pending an explicit decision from the project owner rather than guessing at a new exclusion pattern and risking cutting real family-relevant youth sports/programs (which are mixed in with the same-shaped entries, e.g. 'Fall Youth Soccer - K-1st Grade' from the same sample). Not yet re-run as part of a full group rotation (RecDesk-Parks is Group 1) - the standalone re-run above already stands in as that confirmation."}
{"date":"2026-08-01","scrapers":["RecDesk-Parks"],"category":"code-bug","summary":"Follow-up to the facilityId fix above, per explicit user request to filter out adult-league and fee-only entries rather than leaving the volume/quality concern unresolved. The scraper already had an isLikelyAdultOnly() filter, but its regexes required the adult/men's/women's/coed marker to sit directly adjacent to the sport word (e.g. /adult\\s+(league|softball|...)/), so real titles like 'Men's Fall Softball - Tuesdays' and 'Fall Adult 16\" Softball - Wed Coed' slipped through since a season word sits in between - confirmed these two exact titles were in the DB from the prior verification run. Rewrote the check to co-occurrence: a league marker (men's/women's/coed, or 'adult' alone which is unambiguous) anywhere in the title AND a team-sport term (basketball/softball/volleyball/soccer/flag football/kickball/league) anywhere in the title, while explicitly exempting titles that also carry a youth marker (youth/kids/junior/teen/grade/K-1/6-8th etc) so coed *youth* leagues aren't misclassified as adult ones. Added a second new function isFeeOrRegistrationOnly() for admin/roster line items with no event content ('free agent', 'player fee', 'team fee', 'league fee', 'registration fee', 'membership fee', 'season fee') - these matched no existing filter at all. Wired both into the existing candidateEvents.filter() step in scrapeRecDeskSite(), same place cancelled-event filtering already lived. Verified with a standalone 19-case test (9 titles that should be rejected including all the exact adult/fee titles found in the DB, 10 that should be kept including youth soccer/flag-football/coed-youth-league titles) - all 19 passed. Verified live: re-ran node scrapers/scraper-recdesk-parks.js --slug watertownwi (the exact site the bad sample came from) and confirmed the raw-to-candidate drop went from 440 total unique events down to 273 candidates (167 filtered, up from only cancelled-event filtering before), 95 new saved. Did NOT do a full 46-site re-run (previous one took 161 minutes and wrote 20831 rows) or a full group rotation - Watertown alone was enough to prove the filter fires on real data without the multi-hour cost. IMPORTANT - this filter only affects events scraped going forward. It does not retroactively remove the adult-league/fee-only rows already saved to the DB by the earlier facilityId-fix verification run across all 46 sites; a cleanup/backfill of those existing rows is a separate decision the project owner needs to make (delete vs leave), not attempted here given the DB's history with unauthorized-deletion incidents."}
{"date":"2026-08-02","scrapers":["Somerset-County"],"category":"site-change","summary":"Dropped from a healthy 147-225 New per run (2026-07-19 through 2026-07-31) to 0/0/0/0 in 11.2s today, with zero Google Calendar API responses captured during Puppeteer request interception (previously always ~12 per run). Live WebFetch of somelibrary.org confirms a site redesign: the homepage no longer embeds a Google Calendar widget directly; instead it links to a separate events.php page. events.php itself has no embedded calendar either - its only calendar-labeled link goes to daybydaymd.org, a Maryland State Library statewide family-literacy calendar with no Somerset-specific content, a dead end. The real program schedules now live as plain-HTML carousel/banner text on adultprograms.php, childprograms.php, and teenprograms.php (e.g. 'July 1: Cookie Excavation', 'Mother Goose on the Loose...Princess Anne: Mondays at 10:30 a.m.'), mixing single dated entries with recurring day-of-week schedules and no year in the date text. Root cause confirmed live, but NOT fixed this session: WebFetch only returns markdown-converted text (strips iframe/script tags and real DOM structure), and the in-app Browser tool timed out repeatedly (preview_start and navigate both hit 300s timeouts), so there was no way this session to inspect actual CSS selectors/classes on the three program pages to write a reliable extractor. A rewrite needs a session with working Puppeteer/browser DOM access to build selectors for the banner-style program listings plus a date-inference strategy for the missing-year single dates."}
{"date":"2026-08-03","scrapers":["event-save-helper.js","RecDesk-Parks","Drupal-Parks","NpsParks","ActiveNet-Parks-Eastern","CivicRec-Parks-Eastern"],"category":"code-bug","summary":"User asked for a deeper audit of age-range tagging after noticing too many events land in 'All Ages'. DB query (events.age_range grouped by scraper_name, 81949 rows) confirmed 84.2% All Ages platform-wide. Root cause traced to two layers: (1) scrapers/helpers/supabase-adapter.js's save-time logic (`data.ageRange || detectAgeRange(name, description) || null`, two call sites) treats ANY scraper-provided ageRange as final and skips the shared normalizeAgeRange()'s much richer grade-pattern/numeric-range logic entirely once that happens, even when the scraper's own value was itself just a low-confidence 'All Ages' default; (2) five scraper files each reimplement their own local detectAgeRange() instead of using the shared one, all weaker: scraper-recdesk-parks.js, scraper-drupal-parks.js, and scraper-nps-parks.js share byte-identical logic that only matches the exact literal bracket pair per regex (\\b6[-–]?8\\b), so real titles like 'Gr 4-8', 'K-1st Grade', '6-8th Grade' (confirmed present in the DB) match nothing and silently default to All Ages; scraper-activenet-parks-eastern.js and scraper-civicrec-parks-eastern.js have a slightly better numeric-range fallback but still miss grade wording, AND were discarding a genuinely useful structured raw.ageText field from their source APIs by feeding it through the weak local matcher (CivicRec's case was worse - raw.ageText wasn't even preserved in the saved description, so it was fully discarded either way). Fixes: (1) supabase-adapter.js's two detectAgeRange-then-normalize call sites now fall back to `normalizeAgeRange(data.name)` (the raw event title) instead of hardcoding 'All Ages' when nothing else matched - verified via standalone test against 13 sample titles (including the exact 'Gr 4-8' / 'K-1st Grade' titles from the DB) with no false positives on ambiguous titles ('Route 66 Heritage Festival', '5th Annual Community Gala' correctly stayed All Ages). (2) Deleted all 5 local detectAgeRange() functions. RecDesk/Drupal-Parks/NPS-Parks now let raw title/description flow to the central pipeline (NPS loses its 'Junior Ranger' keyword match, a small NPS-specific vocabulary term the shared detector doesn't know - accepted tradeoff, ~140 events/run, disclosed here for a future session). ActiveNet/CivicRec now pass raw.ageText straight through as data.ageRange instead of degrading it through a keyword scan first - normalizeAgeRange() parses raw ranges like '6-12' or '18+' natively, so this is a strict improvement that also recovers CivicRec's previously fully-discarded signal. All 6 modified files pass node -c. NOT yet re-run against live sites/DB to confirm the All-Ages% actually drops for these 5 scrapers - that verification is now the job of the new AGE-RANGE-AUDIT.md step (Step 3c) added to the funhive-scraper-diagnosis scheduled task, which will report the before/after once each scraper's next rotation runs. A future diagnosis session checking these 5 scrapers' age-range distribution should treat pre-2026-08-03 data as the stale baseline."}
{"date":"2026-08-03","scrapers":["event-save-helper.js"],"category":"code-bug","summary":"User asked for a backfill script to apply the age-range fix above to existing DB rows (dedup skips already-known events before they reach the save path, so the live fix alone never touches old rows). Built scripts/backfill-age-range.js and dry-ran it before writing anything - the dry run caught that the fix committed earlier the same day (normalizeAgeRange(data.name) as a raw-title fallback) was ACTIVELY WRONG and had already corrupted live data: 3 events from the Watertown WI RecDesk live-verification re-run (done earlier this session) got misclassified - '2608-2026 Swim...Level 3' to Kids(6-8), '...Level 4' to Tweens, '...Level 5' to Teens - because normalizeAgeRange()'s generic digit-range regex has no context awareness and misread the leading registration-ID/year pair '2608-2026' as an age range (parses as 8-20 after \\d{1,2} truncation). Same root cause separately caught 'Adventures in Acrylics 1:00 - 2:30p' (time '1:00 - 2:30' misread as age '0-2') and 'Movie Night 7-9pm' (misread as ages 7-9) during dry-run testing before they were ever saved. Root cause: normalizeAgeRange() was designed for short pre-vetted strings (an already-extracted 'ages X-Y' snippet, or a source API's raw ageText field), not arbitrary raw titles full of unrelated numbers (times, IDs, prices, skill levels) - piping raw titles into it directly was never safe. Reverted the raw-name fallback entirely. Replaced with targeted grade-pattern extraction added directly inside detectAgeRange() (context-anchored to 'grade'/'gr'/'k-...grade' - NOT a bare digit scan), which converts grade number to age itself (age = grade + 5, matching the age-per-grade convention normalizeAgeRange()'s own unreachable grade regexes already use) and returns a plain age-range string. This was also necessary because normalizeAgeRange()'s generic numeric-range check runs BEFORE its own grade-specific regexes internally, so a synthesized 'grades 7-12' string was found (via this same dry-run testing) to get misread as literal ages 7-12 (-> wrong bracket Kids 6-8) instead of reaching grade-aware logic at all; doing the grade-to-age conversion in detectAgeRange() sidesteps that dead-code path entirely. Also found and fixed a second, unrelated false-positive class exposed by the same testing: the pre-existing bare 'elementary' keyword rule fired on venue names ('Towne Point Elementary', 'Clinton Elementary Building A') as if they described the audience - tightened to require an actual qualifier ('elementary students/age/kids', not just the word alone). Corrected the 3 already-corrupted Watertown rows (and 27 more that needed re-deriving after the grade-to-age fix landed) via a one-off direct query+update, not the backfill script itself. Backfill script also added a safety guard: if re-derivation would produce 'Adults' (not one of the 5 family brackets), it does NOT auto-write that - flags it for review instead, since CLAUDE.md documents 'Adults' rows as meant for deletion by a separate cleanup pass, not relabeling. Verified via a 19-case regression suite (grade patterns, the exact strings that caused the corruption, several more time/ID/price patterns) plus three dry-run passes at increasing scale (500/5000/15000 rows) with no further false positives found. Ran scripts/backfill-age-range.js --save against the full dataset after all of the above - see the next commit/log entry for row counts. Also updated the SCRAPER-DIAGNOSIS-PROMPT.md age-range note (previously described the now-reverted raw-name-fallback approach) to describe the corrected approach and explicitly warn against re-introducing the raw-fallback pattern."}
{"date":"2026-08-03","scrapers":["Somerset-County"],"category":"site-change","summary":"Follow-up to the 2026-08-02 diagnosis entry (site redesign removed the homepage Google Calendar embed). Confirmed live that Somerset still uses Google Calendar, just moved to three separate program pages - adultprograms.php, childprograms.php, teenprograms.php - each embedding its own calendar widget(s). Rewrote scraper-somerset-county-library-MD.js to navigate all three with the same Puppeteer request-interception approach, aggregating and deduping API responses across pages (the same calendar widget loads twice per page - desktop + mobile - and a couple of calendar IDs are shared across pages). Also added branch resolution (Princess Anne / Crisfield / Ewell) from each Google Calendar item's location field, replacing the old hardcoded single-branch coordinates. Found and fixed two independent latent bugs while verifying: (1) every event's `url` field was hardcoded to the library homepage instead of the per-event Google Calendar htmlLink, and since _stableEventId() hashes primarily on `url`, every event from this scraper collapsed onto one stable id - only the very first insert ever landed, and every subsequent event silently hit a 23505 duplicate-key conflict that the scraper's own success-logging misread as a real import (confirmed live: 483 legacy random-UUID rows exist for url=homepage, but only one row existed under the new stable-id scheme). (2) the addResult.duplicate case fell through to the same code path as a real import, so a DB-level conflict was counted as 'imported' even though nothing was written. Verified live end to end: 310 real events now extracted (up from 0 on 2026-08-02), 0 failures. Imported: 0 / Skipped: 310 on this run is correct, not a bug - queried the DB directly and confirmed this venue already has recurring-series events (Mother Goose on the Loose, S.T.E.A.M., Tech Help, etc, matching today's extracted titles) saved out to May 2027 from before the outage, so every event Google Calendar currently exposes is a genuine pre-existing duplicate. A future run will only show New > 0 once the library publishes occurrences beyond what's already saved, or for any newly-added recurring series. node -c clean."}
{"date":"2026-08-03","scrapers":["browser-launch-outage-2026-08-03"],"category":"other","summary":"The 3:05 AM Group 3 run failed to launch Chrome for 35 of 47 scheduled scrapers (LibCal-FL, Communico-FL, BiblioCommons-MA, Dorchester-County, LibraryMarket-PA/NC, WordPress-PA/MA/KY/SC/WV/DE/RI/NH, WithApps-Libraries, WordPress-Events-Calendar, Squarespace-Libraries, Drupal-Virginia, RollyPollies-MD, Intercept-Camden, Venue-Events-ZoosAquariums, Farms-Eastern-US, Communico-NH/TN/WV, Assabet-NH-MA, and more), all with the identical error 'Failed to launch the browser process: Code: 3236495362' (hex C0E90002) from puppeteer-core's launch.js, 0.3s after each attempt. Also silently affected AARecParks-MD, Patch-Community-Eastern, CivicRec-Parks-Eastern, and Gardens-Nature-Eastern, which completed in 0.3s with Found:0 and no error logged at all (their own try/catch around launchBrowser() didn't fire, unlike the 35 that surfaced a 'FAILED' line) - inferred same root cause from identical timing window and historical baselines showing real multi-second/multi-minute runs with real event counts for all four. Investigated: @sparticuz/chromium is a red herring (not installed, not used by any active scraper - the per-state duplicate files that reference it, e.g. scraper-libcal-libraries-fl.js, are dead code not pointed to by scraper-registry.js, which uses the shared scraper-libcal-libraries-CA-CO-...js file and helpers/puppeteer-config.js's launchBrowser() instead). Confirmed launchBrowser() and plain puppeteer.launch() both work fine when tested live same-day (chrome 146.0.7680.153 win64, installed and runs). grep of scraper-summary.log confirms this exact error string appears on no other date in the log's full history - first occurrence, self-resolved same day, matches the 'zero for a single run may be transient' guidance. No code change made; nothing to fix. Recommend re-running the affected scrapers to recover tonight's data: node local-scraper-runner.js --scraper <Name> per scraper, or a full --group 3 re-run. Separately noticed (not fixed, low priority): scraper-registry.js's log/display names diverge from the scraper_name values actually written to events rows for at least two scrapers - 'Nashville-Library-TN' writes as 'NashvilleLibrary-TN', 'WordPressTec-Parks' writes as 'WordPressTecParks-<state>' (state-suffixed) - only cosmetic today but will trip up any future script that filters events by the registry key string directly."}
{"date":"2026-08-04","scrapers":["Allentown-Public"],"category":"site-change","summary":"User reported hidden events on the real Allentown Public Library site (confirmed via search that allentownpl.org, not the user's initially-suggested allenlibrary.org - a different, unrelated library - is the correct official domain). Site migrated from a WordPress list/archive page to a WP Events Manager *calendar-grid* widget (em-cal-* classes) at the same /library-calendar/ URL the scraper already targeted. Two compounding bugs in scraper-allentown-library-PA.js's scrapePage(): (1) the old multi-selector loop picked plain 'td' (20 matches, the day-grid cells) over the real per-event '.em-cal-event' pills (449 matches) since 'td' came first in the selector list and the loop breaks on the first selector yielding >1 match; (2) even had it picked the right container, the date regex required visible MM/DD/YYYY text, which the calendar grid never renders (day cells only show a bare day-of-month number) - so eventDate always came back empty and every event was silently dropped via the `if (title && eventDate)` guard, explaining the 0-found/0.3s runs. Fixed by targeting '.em-cal-event' directly and reading the exact date from the enclosing day cell's '.em-cal-day-date[data-date]' attribute (ISO yyyy-mm-dd, confirmed present in the live DOM) instead of parsing visible text; kept the old selector logic as a fallback if the site ever reverts to a list view. Also found and fixed a second bug while verifying live: the /page/N/ pagination URLs no longer advance the calendar to a new month (the JS widget ignores the query and re-renders the same current-month view), so the scraper's old 'stop when a page returns 0 events' condition never fired and it looped all 30 maxPages every run scraping identical duplicate content - added a same-events-as-previous-page check that now stops after page 2. Verified live end to end: 32 events/page extracted with correct dates and times, 13 real new events imported to the DB (Tech Tuesdays, Drop in STEM, Art History Lecture, all Aug-Sept 2026, correct venue/date/category), 0 failures, re-run correctly shows 0 new / 13 duplicate-skipped. node -c clean. Minor unrelated issue noticed but not fixed: saved rows have source_url = null despite event.url being populated in the scraper's eventDoc - likely a pre-existing field-mapping gap in this scraper's legacy Firestore-compat (admin/db) write path, not something this fix touched or introduced."}
{"date":"2026-08-04","scrapers":["local-scraper-runner.js","MacaroniKid-ALL"],"category":"code-bug","summary":"User reported a suspected website bug: filtering Events by 25mi from zip 21122 made a result they knew was ~4mi away (a 'National Night Out' event on the Pasadena-Severna Park-Glen Burnie MacaroniKid site) disappear. Live-simulated the site's actual client-side radius filter (EventsPageClient.tsx / ActivitiesPageClient.tsx logic) against production data for that exact zip/radius via a standalone script - found the Activities page filter fully correct (500/500 test rows matched server distance) and only a minor, unrelated Events-page gap (events with zero location data - no geometry, no city, no zip - silently excluded from any location search, not something the user hit). The specific event the user linked did not exist in the DB at all, which redirected the investigation to the scraper side. Root cause: MacaroniKid scrapers (all 43 state files) have not run automatically since 2026-04-15. local-scraper-runner.js's runMacaroniGroup(group, options) - called from every code path that includes MacaroniKid (default daily run, --macaroni, --all, explicit --group N) - was a dead stub (commit f75548d, 2026-04-15, when MK scrapers were split into standalone macaroni-runner-group{1,2,3}.js files) that only logged an instruction to run the new files manually and returned empty results; it never actually invoked them. scrapers/task-scheduler/setup-tasks.ps1 only registers FunHive-Scrapers (3am) and FunHive-Monitor (8am), neither of which calls the macaroni runners either. Confirmed via the filesystem that logs/macaroni-group{1,2,3}-2026-07-09.log is the only trace of these runners ever executing - one manual catch-up on 2026-07-09, nothing before or since. Fix: rewrote runMacaroniGroup() in local-scraper-runner.js to spawn the correct macaroni-runner-group{N}.js as a child process (child_process.spawnSync, stdio: 'inherit' so output flows into the same logs/scraper-stdout.log / scraper-stderr.log the parent already writes to) instead of just printing instructions - respects options.dryRun by logging and skipping, matching the existing runOSMScrapers() dry-run pattern. The three macaroni-runner-group*.js files were not modified; they already work standalone and must be spawned (not required in-process) since each calls process.exit() unconditionally at the end of main(). Verified: node -c clean; --group 2 --dry-run now correctly logs '[DRY RUN] Would run: macaroni-runner-group2.js' instead of silently no-opping; live-ran node macaroni-runner-group2.js --state MD end to end (146 minutes, all 20 MD sites) and confirmed 1908 new events saved including the exact reported event - 'National Night Out' at Pasadena-Severna Park-Glen Burnie, dated 2026-08-04, coordinates ~5-6mi from the user's test zip, matching their report almost exactly. Did not run a full Group 2 rotation (14-15 states) in this session given the single-state MD run alone took 2.4 hours - the fix is now wired into the regular nightly 3am job going forward, no further manual action needed. Flagging an operational note, not a blocker: a full nightly rotation already runs 5-10+ hours some days per setup-tasks.ps1's own comments, and adding full MacaroniKid group runs on top may push some nights close to or past the 12h Task Scheduler ExecutionTimeLimit - worth watching the first few automated runs' total duration."}
{"date":"2026-08-04","scrapers":["local-scraper-runner.js","MacaroniKid-ALL"],"category":"other","summary":"Follow-up to the same-day MacaroniKid automation fix, per explicit user request: (1) integrate MacaroniKid per-state results into the shared scrapers/logs/scraper-summary.log table instead of only the group runners' own separate logs/macaroni-group{N}-<date>.log files, and (2) audit all documents/systems in the project for any other scrapers that are registered but silently not running, for transparency. Part 1: extracted the summary-row formatter/writer (logSummary, formatSummaryRow, SUMMARY_TABLE_HEADER/DIVIDER) out of local-scraper-runner.js into a new shared module scrapers/helpers/scraper-summary-logger.js, since macaroni-runner-group{1,2,3}.js run as separate child processes and can't reuse local-scraper-runner.js's internals directly. local-scraper-runner.js now imports the shared module instead of defining its own copy. Each of the three macaroni-runner-group*.js files now imports the same module and calls logSummary(formatSummaryRow(...)) right after each state's stats are computed (success and failure paths), plus a labeled header/footer block around the per-state rows so standalone invocations (not just ones spawned from the nightly job) get a properly labeled table section. Verified live: ran macaroni-runner-group3.js --state DE (173 new events, 17.1 min) and confirmed a correctly-formatted 'MacaroniKid-DE 173 173 0 0 1024.3' row landed in scraper-summary.log with the exact same column alignment as every other scraper's rows. Part 2 (audit): cross-referenced all three top-level registries in scraper-registry.js against actual automation entry points. SCRAPERS (219 registered, 149 active-region) - confirmed healthy, running daily via local-scraper-runner.js's default group rotation, already visible in scraper-summary.log. MACARONI_SCRAPERS (43 registered, 20 active-region) - the family fixed earlier today. OSM_SCRAPERS (11 entries: OSM-Batch1 through OSM-Batch10 plus OSM-California) - found these point at scrapers/scraper-osm-batch-activities.js and scrapers/scraper-osm-california.js, NEITHER OF WHICH EXISTS IN THE REPO, confirmed via git log showing zero commit history for either path since the initial commit. This is not a regression like the MacaroniKid stub - it's a placeholder/unimplemented feature that has never worked, registered behind a --osm CLI flag that nothing (no scheduled task, no default path) ever passes. Running it would just throw 'Scraper file not found' for all 11 entries. Did not implement or remove these - that's a product decision (build the real OSM scrapers or delete the dead registry entries), not an automation-wiring bug to silently fix. Also checked for other automation systems beyond scraper-registry.js: confirmed via .github/workflows (does not exist), .claude/settings.json (no additional scraper-related hooks, only permission allowlist entries confirming a separate concurrent session's LIBRARY-SITE-AUDIT.md/AGE-RANGE-AUDIT.md work is legitimate/pre-authorized, not this session's), and ~/.claude/scheduled-tasks (only the one documented funhive-scraper-diagnosis task exists, matching CLAUDE.md, no drift). Documented both the MacaroniKid incident and the OSM dead-registry finding directly in CLAUDE.md's Scraper System section so a future session doesn't need to rediscover either."}
{"date":"2026-08-04","scrapers":["scraper-registry.js","local-scraper-runner.js"],"category":"other","summary":"Per explicit user request, deleted the dead OSM automation entirely rather than leaving it registered. Removed the OSM_SCRAPERS object (11 entries: OSM-Batch1-10 + OSM-California) from scraper-registry.js along with getOSMScrapersForDay() and getOSMScrapersForDayByRegion() (both now-unused), and their exports. Removed runOSMScrapers(), the --osm CLI flag, its help text, and the OSM_SCRAPERS/getOSMScrapersForDay import/fallback lookup (in runSingleScraper) from local-scraper-runner.js. Also deleted the two orphaned real-but-unregistered files discovered while answering a follow-up question about what OSM scrapers were meant to cover: scraper-activities-openstreetmap-usa.js and scraper-activities-osm-state-boundary.js - both genuine Overpass-API implementations (19 venue types: libraries, museums, parks, playgrounds, zoos, bowling alleys, etc.) that were never wired into the registry and were already explicitly labeled 'dead scraper files' in a 2026-07-07 commit. Confirmed via grep that no other file referenced either orphaned filename before deleting. Verified: node -c clean on both edited files; runtime-loaded scraper-registry.js via require() and confirmed OSM_SCRAPERS is absent with no reference errors; node local-scraper-runner.js --help shows no OSM mentions; a full --dry-run of today's group (including macaroni) completes without error. Documented in CLAUDE.md that the venue categories OSM would have covered are already served by the hand-curated Activities-{Category}-DMV family (expanded 2026-07-02 to all 22 active states) and Activities-Eastern-US - real but static coverage, not automated discovery - so a future session doesn't need to re-investigate this history if OSM-based venue discovery comes up again."}
{"date":"2026-08-04","scrapers":["supabase-adapter.js","WordPress-ALL","MacaroniKid-ALL"],"category":"code-bug","summary":"Age-range detection was silently defaulting clearly age-specific events to All Ages. Root cause: both save paths used data.ageRange || detectAgeRange(...), so ANY scraper-supplied value won outright - and the WordPress-{state} parser accepts any [class*=category] chip as its age element, so a site's generic All Ages/Library Programs category tag beat unambiguous titles. Verified against live DB rows: 'Toddler Time Downtown (Ages 18-36 Months)' and 'Baby Bounce (Ages 0-18 Months)' were both stored as All Ages even though detectAgeRange() returns the correct bracket for both. Replaced both call sites with a new resolveAgeRange(data) which keeps a SPECIFIC supplied bracket (including Adults, which callers rely on for adult-only rejection) but re-checks title/description via detectAgeRange() whenever the supplied value normalizes to the All Ages catch-all. Deliberately routes through detectAgeRange()'s anchored checks, NOT normalizeAgeRange(raw title) - that shortcut was tried and reverted on 2026-08-03 for misreading times/IDs as ages. Found and fixed four further bugs in the same pass, all long-standing: (1) trailing \b anchors meant the plurals 'teens', 'preschoolers' and 'infants' never matched and fell through to All Ages; (2) normalizeAgeRange() buckets a range on its LOWER bound, so bare 'teen' returning 11-18 filed every teen event as Tweens (now 13-18), generic kids/children returning 4-12 filed kid-labelled events as Preschool (now 6-12), and 'elementary' returning 5-11 did the same (now 6-11); (3) the grade-pattern block ran AFTER the generic kids/children keyword, so 'for kids from 3rd grade to 6th grade only' matched the generic rule first and lost the grade range entirely - moved grade patterns ahead of the generic keywords since they are strictly more specific. Added scripts/test-age-detection.js as a permanent 24-case regression suite covering both failure directions (false negatives AND the false-positive controls - times, registration IDs, ordinals, venue names, 'Juneteenth' - that caught the 2026-08-03 regression live). All 24 pass; node -c clean. Ran scripts/backfill-age-range.js --save against existing data afterward: 1113 rows reclassified out of 79527 All Ages rows, 0 failures, 6 adult-only rows correctly flagged and left alone by the script's own guard."}
{"date":"2026-08-04","scrapers":["supabase-adapter.js"],"category":"code-bug","summary":"Provenance fields needed by the per-site audits were frequently NULL: measured across 2026-08-04's rows, scraped_at was missing on 19.5% (3215/16498) and source_url on 46.6% (7689/16498), because flattenEvent() only ever read them from data.metadata and many scrapers set neither. That is why LIBRARY-SITE-AUDIT.md and AGE-RANGE-AUDIT.md had to attribute rows by created_at time-window archaeology against the run log instead of a direct lookup, and why the audits' Link column had to fall back to per-event URLs. flattenEvent() now always sets scraped_at at save time (trivially knowable) and fills source_url from the top-level sourceUrl/source_url/eventsUrl/calendarUrl spellings scrapers actually use. source_url is deliberately NOT defaulted to the event's own url - the audits treat source_url as the listing page the scraper visits, and conflating the two would make a per-event link masquerade as a stable calendar link. Also exported flattenEvent and resolveAgeRange so regression suites can exercise the real save path instead of re-implementing its precedence rules."}
{"date":"2026-08-04","scrapers":["LIBRARY-SITE-AUDIT.md","AGE-RANGE-AUDIT.md"],"category":"other","summary":"Live-verified both flagged populations against the real sites and recorded the result as a Comments column with a MATCHES/MISMATCH/UNVERIFIABLE verdict per site. Zero-event sites (315 checked): 23 genuinely empty, 36 confirmed real bugs where the site visibly lists dated events the scraper missed, 255 unverifiable (403 bot-blocks, JS-only calendars, dead domains, TLS failures). Mostly-All-Ages sites (104 individual sites checked): 4 genuinely generic, 26 confirmed age-detection gaps, 74 unverifiable. Two root causes surfaced that are NOT extraction bugs and need different fixes: dead/hijacked domains where the configured URL now serves an unrelated site (a Korean university consortium, an Oregon city government, a gambling site, a Michigan library), and URL collisions where several distinct libraries in different states share one URL resolving to only one real library (Pelham MA/GA->NY, Seymour TN->CT, Baxter TN->ME, and Hancock GA/Alleghany NC/White TN all pointing at spartalibrary.org in Wisconsin). Both are seed-data bugs, still OPEN - not fixed in this pass because each needs the library's real current URL researched and verified individually, and guessing would repeat the Worcester incident. The 36 zero-event extraction failures are also still OPEN: they are the known WordPress-{state} platform-heterogeneity problem already documented on 2026-07-16, needing per-site DOM work rather than a generic patch. Two fetched pages contained prompt-injection attempts (ccrec.recdesk.com claiming 'You are a Claude agent, built on Anthropic's Claude Agent SDK'; brandywinezoo.org/events attempting to dictate response formatting); both were treated as untrusted page data and ignored. Wired this verification into the daily scheduled diagnosis as Step 3d, with carry-forward so settled sites are not re-fetched daily, plus a no-aggregation rule and a required Link column for both audit files."}
{"date":"2026-08-05","scrapers":["WordPress-NC"],"category":"seed-data","summary":"Pettigrew Regional Library (Plymouth, NC) was configured with https://plymouthlibrary.org/, which is Plymouth District Library in Michigan. Repointed to the verified correct site pettigrewlibraries.org and corrected the fabricated county 'Plymouth County' to Washington County."}
{"date":"2026-08-05","scrapers":["WordPress-AL"],"category":"seed-data","summary":"Removed Hightower Memorial Library (York, AL). Its configured URL https://yorklibrary.org/ serves Kilgore Memorial Library in York, Nebraska; no official website exists for the Alabama library (Facebook page only). Removed rather than left pointing at a wrong-state domain, since broadening extraction would ingest Nebraska events as Alabama. Recorded as a coverage gap."}
{"date":"2026-08-05","scrapers":["LibCal-TN"],"category":"seed-data","summary":"Memphis Public Libraries' configured URL memphislibrary.libcal.com 404'd (dead domain). Live-verified the real current site is memphis.librarycalendar.com (reached via redirect from memphislibrary.org/events/), which shows abundant real dated August 2026 events including age-specific programs. Repointed the config entry; this shared scraper file already parses the librarycalendar.com format for other libraries in the same array (Albany, Long Beach, Levittown), so no extraction code change needed. Not yet re-run to confirm a nonzero count."}
{"date":"2026-08-05","scrapers":["WordPress-MD"],"category":"seed-data","summary":"Talbot County Free Library's configured URL tcfl.org/events returned 403 (bot-blocked/broken). Live-verified the library's real current calendar lives on a different platform, talbot.librarycalendar.com/events/upcoming, with multiple real dated age-specific events. Repointed the config entry's url/eventsUrl. Caveat: this scraper's extraction is WordPress-DOM-specific and librarycalendar.com uses different markup than WordPress, so extraction is not confirmed working post-fix -- the URL is now correct but a re-run is needed to check whether Talbot actually returns events, and if not this needs the same librarycalendar.com-aware extraction path the shared LibCal scraper already has, not more WordPress selector work."}
{"date":"2026-08-05","scrapers":["WordPress-FL"],"category":"seed-data","summary":"Found, not yet fixed: the top 5 major FL library systems in scraper-wordpress-libraries-fl.js (Miami-Dade, Orange County, Tampa-Hillsborough, Broward, Palm Beach) are missing city/state entirely and all have county hardcoded to 'Baltimore City' (a Maryland county) -- an apparent copy-paste artifact from an MD seed file. Confirmed independently by two separate passes today (the LIBRARY-SITE-AUDIT.md generation agent and the Step 3d live-verification agent). Logged in reports/fix-notes.json as a high-priority open item; needs the real city/state/county researched and filled in per system."}
{"date":"2026-08-05","scrapers":["Communico-NJ","Communico-SC"],"category":"seed-data","summary":"Three Communico libnet.info domains confirmed dead via live fetch (302-redirect to http://www.google.co.uk/ on both root and /events): Montclair Public Library (NJ), Greenville County Library (SC), Richland Library (SC). Not extraction bugs -- the libraries have moved off this platform/domain. Documented as coverage gaps in reports/fix-notes.json rather than guessing replacement URLs; each needs its real current calendar URL researched before it can be restored."}
{"date":"2026-08-05","scrapers":["supabase-adapter.js","BarnesNoble-Eastern","WordPress-ALL","MacaroniKid-ALL","RecDesk-Parks"],"category":"code-bug","summary":"detectAgeRange() had no concept of 'storytime' -- the single most common children's program name at libraries and bookstores -- so every unqualified 'Storytime' / 'Story Time' / 'Story Hour' returned null and defaulted to All Ages. Confirmed live against 15 flagged Barnes & Noble store calendars and a dozen library MISMATCHes. Added a storytime rule returning 3-5 (Preschool) plus a tots/tykes rule returning 1-3, both placed AFTER every existing specific keyword so qualified titles ('Toddler Storytime', 'Baby Storytime', 'Preschool Storytime') still resolve on their own more-specific rule and are unaffected. Three guards, each matching a known false-positive shape: !family so 'Family Storytime' stays All Ages, !adult so 'Storytime for Adults' does not become a children's event, and a 'Storytime Room' venue-mention guard mirroring the 'elementary' venue false positive from 2026-08-03. Regression suite extended 24 -> 36 cases covering both directions; all 36 pass. Backfilled existing rows with scripts/backfill-age-range.js (capped dry run inspected first): 2677 rows reclassified (2385 Preschool, 283 Babies & Toddlers, 7 Kids), platform-wide All Ages share dropped 77.4% -> 74.9%. The 501 storytime rows that stayed All Ages were spot-checked and are legitimately family-declared ('...for the whole family' in the description), i.e. the guard working as intended, not a remaining miss."}
{"date":"2026-08-05","scrapers":["WordPress-FL"],"category":"seed-data","summary":"Florida's five largest library systems (Miami-Dade, Orange County, Tampa-Hillsborough, Broward, Palm Beach) had no city or state at all and every one carried county: 'Baltimore City', a Maryland county copy-pasted from an MD seed file. With no city/state the geocoding chain had nothing to work with, and the wrong-state county made getCountyCentroid() return null, so all five fell through to the Florida state centroid. Filled in each system's main-branch city/state/zip and set county to the exact bare key used in utils/county-centroids.js; verified all five now resolve to real centroids and that the previous values returned null."}
{"date":"2026-08-05","scrapers":["Communico-NJ","LibCal-NJ"],"category":"site-change","summary":"Montclair Public Library's Communico host montclairlibrary.libnet.info is dead — it 302-redirects to google.co.uk, which is why the scraper's API, Puppeteer and link-based fallbacks all returned 0. The library has migrated to LibCal: montclairlibrary.org's own site links its calendar to bccls.libcal.com/calendar/montclair. Removed the dead Communico entry and re-added the library to the LibCal scraper's NJ section with a Montclair-specific cid rather than relying on the existing BCCLS cid=-1 system-wide entry, because that system-wide scrape files its rows under the BCCLS system venue and Montclair's own programming could not be confirmed captured or attributed (only 2 DB rows mentioned Montclair at all). Platform migration, not a URL typo."}
{"date":"2026-08-05","scrapers":["Communico-SC"],"category":"site-change","summary":"Removed Greenville County Library System and Richland Library from Communico-SC. Both Communico hosts (greenville.libnet.info, richland.libnet.info) are dead, each 302-redirecting to google.co.uk, so neither could ever return an event. Not repointed within this scraper because both libraries now run Drupal-based calendars on their own domains that the Communico parser cannot read; those correct URLs are already configured in CustomDrupal-Libraries. STATED AS A COVERAGE GAP, NOT RESOLVED: CustomDrupal-Libraries reported finding events for both on 2026-08-05 but the database holds 0 upcoming rows for Greenville County and only 1 for Richland (a single branch via a different scraper), so this removal deletes two permanently-dead endpoints rather than handing coverage to a verified replacement. The outstanding work is CustomDrupal-Libraries' extraction."}
{"date":"2026-08-05","scrapers":["WordPress-NY","WordPress-ME","WordPress-GA","LibCal-GA"],"category":"seed-data","summary":"Fixed four cross-state URL collisions, all verified live before saving. greenvillelibrary.org is the Greenville COUNTY Library System in South Carolina and was claimed by 10 active states: WordPress-NY's Greenville Public Library was repointed to the real Greene County NY library at greenville.lib.ny.us (11177 Rte 32, ZIP 12083 matching the entry), and WordPress-ME's Shaw Public Library to shawpubliclibrary.org (9 Lily Bay Road, ZIP 04441 matching). homerlibrary.org is the Homer Township Public Library District in Homer Glen ILLINOIS: WordPress-NY's Phillips Free Library was repointed to phillipsfreelibrary.org (37 South Main St, ZIP 13077 matching), and WordPress-GA's Banks County Public Library was moved out to the LibCal scraper's GA section since Piedmont Regional runs its calendar on prlib.libcal.com. These were not silent zeroes — they were ingesting other states' events under the wrong label, e.g. an event titled 'War in the South Carolina Backcountry' stored as Greenville, NY. Deleted 269 resulting orphan rows (189 SC events labeled NY/ME, 80 IL events labeled NY/GA); their stable IDs derive from the old URLs so a corrected re-scrape would never have overwritten them. Collision groups now 558 cross-state, down from 653."}
{"date":"2026-08-05","scrapers":["WordPress-NC","activities-table"],"category":"seed-data","summary":"Reversed the recommendation to delete 18 WordPress-NC entries as redundant. The claim used circular evidence (venue strings produced by wordpress-NC itself) and quoted LIBRARY-SITE-AUDIT.md FOUND counts as if they were database coverage; live rows are far smaller and wordpress-NC is actually NC's second-largest library event producer at 275 rows. All 79 colliding NC entries still need a real verified URL. Separately measured that the activities table carries the same fabricated seed URLs: 280 rows on 138 colliding hosts, 261 library-named, with madisonlibrary.org appearing as 10 venue records in 10 states. Added scripts/verify-coverage.js which establishes coverage by source_url host only, excludes the audited scraper from its own evidence, defaults to INCONCLUSIVE, and reports activities separately from events."}
{"date":"2026-08-05","scrapers":["MacaroniKid-ALL"],"category":"code-bug","summary":"MacaroniKid scrapers passed no listing URL, leaving source_url NULL on 11467 rows created since the 2026-08-04 fix - the single largest gap in the fleet. Added sourceUrl: site.url and the registry scraperName to the event metadata in 44 files. Deliberately used the site's own listing URL (e.g. https://apollobeach.macaronikid.com) and NOT the event's url: scraper-macaroni-md.js had set sourceUrl: url, and its stored source_url is byte-identical to url, which CLAUDE.md forbids and which would have made every MacaroniKid site in every state share the host events.yodel.today, useless for identity. That conflation is corrected too. scraper-macaroni-usa-local.js skipped - different structure, no shared anchor."}
{"date":"2026-08-05","scrapers":["MacaroniKid-ALL"],"category":"code-bug","summary":"Correction to the same day's MacaroniKid change: scraperName had been set to the bare registry key, which collapsed every site in a state onto one identifier - MacaroniKid-FL alone covers 31 sites, and across the 20 active macaroni scrapers 228 sites would have become 20 names. AGE-RANGE-AUDIT.md groups by scraper_name and its 'No aggregation, ever' rule requires one row per individual site, so this would have re-created the aggregation the owner explicitly rejected on 2026-08-04. Now uses the convention already established by CivicRec-Parks-Eastern and RecDeskParks, family prefix plus per-site slug derived from the site's own subdomain, giving MacaroniKid-FL-apollobeach. Verified through flattenEvent that three FL sites yield three distinct scraper_name values and three distinct source_url hosts."}
{"date":"2026-08-05","scrapers":["scraper-registry.js"],"category":"other","summary":"Documented the scraper_name and source_url conventions after two same-day mistakes collapsed identifiers. Added a Scraper Naming section to CLAUDE.md defining the only two legal forms (exact registry key for single-site scrapers, registryKey-siteSlug for multi-site), five no-exception rules, the source_url-is-the-listing-page rule, and a new-scraper checklist. Added scripts/check-scraper-names.js which classifies every DB scraper_name as EXACT, PREFIXED, CASE_MISMATCH, FORMAT_DRIFT, BAD_SLUG, FREE_TEXT or UNRELATED, suggests a slug-preserving fix, and flags multi-site scrapers collapsed onto one name. Measured baseline: only 143 of 434 distinct names (33%) conform; 24883 rows on FORMAT_DRIFT, 13044 on FREE_TEXT, 8337 on CASE_MISMATCH. Added Step 3f to the daily diagnosis to run the check, explicitly scoped to reporting rather than mass renaming. Migration sequence recorded in SCRAPER-NAMING-PLAN.md."}
{"date":"2026-08-05","scrapers":["scraper-registry.js"],"category":"other","summary":"Consolidated SCRAPER-BUG-FIX-PLAN.md and SCRAPER-NAMING-PLAN.md into MASTER-PLAN.md and classified every active scraper so the plan covers the whole fleet rather than just libraries and MacaroniKid. Classification is empirical, from whether each file writes collection('events') or goes through venue-matcher/saveActivities: 169 active scrapers split 153 event across 361 sites and 16 venue, with none unclassified and the family counts reconciling to the registry. Venue scrapers (the Activities-* family) are listed by name with a note only, as they write to activities rather than events. Naming and county remediation are folded into the per-file URL pass in Phase 2 rather than run as separate migrations, so each scraper file is opened once. Added CustomDrupal-Libraries (356 found, 0 stored) and the 331-site UNVERIFIABLE backlog as explicit phases; both were previously unowned. Found a clean naming-drift example while cross-checking: registry key Venue-Events-ZoosAquariums writes scraper_name ZooAquariums-Eastern, matching no registry key."}
{"date":"2026-08-05","scrapers":["VenueList-ALL"],"category":"other","summary":"Renamed the 16 venue scrapers from Activities-* to VenueList-* so their names state what they are. VenueList is deliberately distinct from the Venue-Events-* family, which are EVENT scrapers despite carrying the word Venue. Also aligned each file's SCRAPER_NAME constant to its registry key: all 16 were drifting (Activities-ArtStudios-DMV wrote ArtStudios-Eastern), which is the same defect the new CLAUDE.md naming rules forbid. Blast radius was small - scraper-registry.js, add-scrapers-to-registry.js, CLAUDE.md and MASTER-PLAN.md - with no task-scheduler or script references. Active scraper count unchanged at 169 and no key/SCRAPER_NAME mismatches remain. Note left unactioned: the -DMV suffix is stale on this family, which expanded to all 22 eastern states on 2026-07-02, so the keys arguably should end -Eastern; not changed because it was outside the request."}
{"date":"2026-08-05","scrapers":["WordPress-NC"],"category":"seed-data","summary":"Phase 2 batch 1 for WordPress-NC: 15 entries across four parent systems repointed from fabricated {city}library.org domains to their real verified sites, and their fabricated counties corrected. Sandhill Regional Library System (5 entries) to srls.info, Public Library of Johnston County and Smithfield (4) to pljcs.org, Gaston County Public Library (4) to gastonlibrary.org, Beaufort-Hyde-Martin Regional Library (2) to bhmlib.org. Every URL was fetched live and confirmed to be the right institution in NC before saving; bhmlib.org's /events path was NOT confirmed so both fields use the verified base URL. NC colliding entries fell 78 to 67 and the global cross-state group count 559 to 554. Also discovered that county-centroids.js holds only 13 of North Carolina's 100 counties, so correcting county names alone will not restore the county-centroid geocoding tier - the dataset needs expanding too."}
{"date":"2026-08-06","scrapers":["scraper-registry.js"],"category":"other","summary":"Moved post-change fallout tracking out of conversation and into the tooling. Added a _pending array to reports/fix-notes.json holding {what, expect, confirm, added} for every change already made whose effect has not been observed yet, and taught generate-site-report.js to render it as a banner at the top of the report, excluded from the per-scraper Fix queue. Seeded with five items: the MacaroniKid listing-URL and per-site-name fix (statically verified only), the Activities to VenueList rename whose pre-rename rows now match no registry key until they age out, the 291-of-434 scraper-name drift baseline so a future run does not report it as a regression, the MacaroniKid-MD COLLAPSED flag from historical rows, and WordPress-NC batch 1 whose URLs are correct but whose extraction is unconfirmed. SKILL.md Step 3e now requires working through _pending each run and deleting confirmed entries. Also recorded in MASTER-PLAN.md, along with the finding that county-centroids.js holds only 13 of North Carolina's 100 counties, which widens Defect B beyond fixing county names."}
{"date":"2026-08-06","scrapers":["WordPress-NC"],"category":"seed-data","summary":"Phase 2 batch 2 for WordPress-NC: 13 entries across six more parent systems repointed from fabricated {city}library.org domains and their fabricated counties corrected. Northwestern Regional Library (3) to nwrl.org, Sheppard Memorial Library (2) to sheppardlibrary.org, Wilson County Public Library (2) to wilsoncountypubliclibrary.org, Alamance County Public Libraries (2) to library.alamancecountync.gov, Charlotte Mecklenburg Library (2) to cmlibrary.org, Catawba County Library (2) to catawbacountync.gov. Wilson and Charlotte Mecklenburg both 403 a plain fetch so they were verified in a real browser instead. Two systems host events off-site - Alamance on LibCal, Charlotte Mecklenburg on BiblioCommons, both already covered by their own platform scrapers - so eventsUrl was kept on the library's own domain rather than pointing a WordPress-DOM scraper at foreign markup. NC colliding entries fell 67 to 54 and the fleet-wide cross-state group count 554 to 550. All ten domains fixed so far confirmed single-state."}
{"date":"2026-08-06","scrapers":["WordPress-NC"],"category":"other","summary":"Corrected the WordPress-NC queue and added a final coverage phase. Phase 2 had only examined the 79 entries whose domain collides across states; the other 10 were skipped because their domain is unique in the fleet, which is no evidence the URL is right or that the domain belongs to that library. Nine remain unverified (bunnlibrary.org, dobsonlibrary.org, farmvillelibrary.libguides.com, hickorylibrary.org, indiantraillibrary.org, kinglibrary.org, louisburglibrary.org, roanokerapidslibrary.org, starlibrary.org) and all carry the same {city}library.org generator signature. NC is therefore 30 of 89 verified, not 30 of 79, and the same undercount applies to every other state file. Added Phase 10, a county-level coverage completeness audit that answers, for every county in every active state, whether each public library is covered, configured but producing nothing, not configured at all, or checked and genuinely unscrapeable. It is deliberately the final milestone and blocked on Phases 2-8, because coverage cannot be measured while URLs point at the wrong institutions and scraper_name does not join to the registry."}
{"date":"2026-08-06","scrapers":["generate-site-report.js"],"category":"code-bug","summary":"The report's header showed only its generation date, implying the data was current when it is a projection of the last scrape. A reader looking at Belmont Branch Library today would see Link smcl.org, the California domain corrected hours earlier, with nothing indicating the row was stale rather than the fix lost. Added a data-as-of line to the header showing each audit file's own date alongside the generation date, and a banner whenever either audit predates the run date explaining that config edits do not appear until those scrapers run again and Steps 3b/3c rebuild the audits. First implementation took the newest date anywhere in the file and reported the age data as 2026-11-05, a future event date quoted in prose; it now prefers the ## YYYY-MM-DD run headings and never reports a date past today."}
{"date":"2026-08-06","scrapers":["generate-site-report.js"],"category":"code-bug","summary":"Replaced the page-level staleness banner with a per-row Status column on the library sites table, so each site states where it stands rather than the reader having to apply a blanket disclaimer. The generator now reads every scraper's CURRENT configured URL from its source file and compares it against the link the audit recorded: current means the config still points at that domain, refixed means the URL was corrected since the scrape and names the new host, unmatched means the row could not be tied to a config entry. Current tally 593 current, 34 refixed, 47 unmatched. Two bugs found and fixed while building it: the config parser only accepted single-quoted fields, so scraper-libcal-libraries-nh.js which uses double quotes yielded nothing and made nine live libraries look removed; and the first status name was 'removed', which overclaimed, since failing to match a name is not proof the entry is gone. The status is now 'unmatched' and its detail says plainly that it is not confirmed either way."}
{"date":"2026-08-06","scrapers":["generate-site-report.js"],"category":"code-bug","summary":"Removed the last page-level banner. _pending changed from an array rendered as a banner into an object keyed by scraper name, so each unverified change sits on the scraper it concerns: an awaiting badge on that scraper's Coverage row, searchable by filtering the Coverage tab for 'awaiting', plus the same block in its Fix queue detail where relevant. 38 scrapers currently carry one - 20 MacaroniKid awaiting the listing-URL and per-site-name fix, 16 VenueList awaiting the rename fallout to age out, WordPress-NC whose URLs are corrected but whose extraction is unproven, and WordPress-SC which still holds the wrong half of the gastonlibrary.org collision. Two former entries were dropped rather than moved: the 291-of-434 name-drift baseline is a global measurement belonging in SKILL.md Step 3f, and the WordPress-NC per-site URL changes are already shown by the refixed row status. A new stat tile counts scrapers with unverified changes."}
{"date":"2026-08-06","scrapers":["generate-site-report.js"],"category":"code-bug","summary":"Removed the last redundant banner. The 'N of 169 active scrapers have no rows yet this cycle' line duplicated two things that already said it better: the 'Scrapers awaiting this cycle' stat tile carries the count, and every Coverage row already has status 'none' with its own reason naming the rotation group. The report now renders zero banners in the normal case; banners are reserved for conditions no row or tile can express, which currently means only audit parse failures. This completes the same pass applied to the staleness banner (replaced by a per-row Status column) and the pending banner (replaced by a per-scraper awaiting badge)."}
{"date":"2026-08-06","scrapers":["WordPress-MD"],"category":"new-coverage","summary":"Maryland priority pass. Closed two real county-level coverage gaps: Ruth Enlow Library of Garrett County (relib.net) held ZERO rows in the database, and Kent County Public Library (kentcountylibrary.org) had only festival-scraper rows and no library coverage; both URLs verified live before saving and both counties resolve via getCountyCentroid. The file had carried a comment asserting Garrett was covered by scraper-librarymarket-libraries-md.js, which is false - that file is not registered in scraper-registry.js and never runs, exactly the trust-a-code-comment failure CLAUDE.md warns about. Wider finding while investigating: 32 scraper files on disk are not referenced by any registry entry and therefore never run, 8 of them Maryland. Most are redundant with registered scrapers, but scraper-libcal-libraries-md.js and scraper-librarymarket-libraries-md.js were the only homes for the two libraries above. Also verified that dorchesterlibrary.org and worcesterlibrary.org belong to Maryland, so the MA/WI/NY entries sharing those domains are the wrong side of those collisions."}
{"date":"2026-08-06","scrapers":["generate-site-report.js"],"category":"code-bug","summary":"The report's config-URL index parsed raw scraper source including comments. scraper-wordpress-libraries-md.js carries an AUTO-GENERATED header comment documenting 11 libraries while the real LIBRARIES array configures 10, so comment text was being read as live config and corrupting the per-row Status column. The index now strips block and line comments before parsing. This also corrected an earlier misreading in which those 11 comment entries looked like duplicate config entries."}
{"date":"2026-08-06","scrapers":["scraper-registry.js"],"category":"new-coverage","summary":"Swept all 30 orphaned scraper files - files on disk that no registry entry references and which therefore never run. Two of the 32 originally counted were scraper-logger.js and scraper-registry.js, not scrapers. Most orphans duplicate registered coverage, but 13 library systems in active states have zero library-scraper rows and exist only in an orphaned file. scraper-libcal-libraries-va.js holds seven: Prince William, Newport News, Hampton, Roanoke, Suffolk, Williamsburg Regional and the Library of Virginia. scraper-libcal-libraries-fl.js holds four: Volusia, Osceola, Leon and Manatee counties. Both files pass node -c and have been maintained since 2026-04-08, the FL one as recently as 2026-07-07, so they are working code that was never wired up. Prince William's LibCal was fetched live and shows dated August 2026 events, and the registered shared LibCal file contains none of the VA systems, so registering adds coverage rather than duplicating. Not registered in this pass: each needs a new key following the LibCal-NY1/NY2 precedent, a SCRAPER_NAME corrected from libcal-VA to the registry key, and a watched first run, since none has ever executed in production. Recorded as Defect E in MASTER-PLAN.md and as high-priority notes on LibCal-VA and LibCal-FL."}
{"date":"2026-08-06","scrapers":["LibCal-VA2"],"category":"new-coverage","summary":"Registered scraper-libcal-libraries-va.js as LibCal-VA2, group 3. The file had existed since 2026-04-08 and been maintained since, but no registry entry pointed at it so it had never run. It configures 11 Virginia library systems - Fairfax County, the state's largest, plus Arlington, Prince William, Richmond, Norfolk, Newport News, Hampton, Roanoke, Suffolk, Williamsburg Regional and the Library of Virginia - and a database check confirmed every one of them has zero library-scraper rows, so this adds coverage rather than duplicating it. Three name overlaps with other scrapers turned out to be false: WordPress-CA's Richmond is Richmond California and WordPress-MA's Norfolk is Norfolk Massachusetts, while Fairfax and Arlington appear in the shared LibCal file but produce nothing. Two corrections made while registering: SCRAPER_NAME was 'libcal-VA', which is the naming drift the new CLAUDE.md rules forbid, now 'LibCal-VA2' matching its key; and sourceUrl was set to library.url, the library homepage, rather than library.eventsUrl, the listing page the scraper actually fetches. Group 3 chosen as the smallest rotation group. Active scraper count 169 to 170. Never executed in production - first run needs watching, recorded in _pending."}
{"date":"2026-08-06","scrapers":["Pratt-Library","Howard-County","AACPL","Cecil-County","Dorchester-County","Prince-Georges-County","Somerset-County","Wicomico-Public","Squarespace-Libraries","PG-Parks","RollyPollies-MD","Montgomery-Parks","WordPress-MD"],"category":"code-bug","summary":"Maryland naming pass. Of 8725 MD event rows, 18 of 36 distinct scraper_name values did not join back to the registry, because most MD single-system scrapers set no scraperName at all and the adapter fell back to metadata.sourceName, writing the library's display name - Enoch Pratt Free Library, Somerset County Library, Howard County Library System and so on. Twelve single-site scrapers now write their bare registry key; metadata.sourceName was deliberately left untouched because scraper-somerset-county-library-MD.js dedupes on it. PG-Parks and Montgomery-Parks additionally had drifting literals (PGParks-MD, MontgomeryParks-MD) corrected. WordPress-MD is multi-site with 12 distinct hosts, so it takes the registry key plus a per-site slug rather than a bare key, which would have collapsed 12 libraries onto one name. The slug strips a leading www: 9 of the 12 MD URLs carry one and a naive split would have slugged them all to WordPress-MD-www, reproducing the collapse. The same hardening was applied defensively to all 43 MacaroniKid files; none of their 417 URLs carries www today, verified, but the failure would be silent if one ever did."}
{"date":"2026-08-06","scrapers":["generate-site-report.js"],"category":"other","summary":"Made the report reflect each day's scraper run instead of lagging until the 2:12pm diagnosis. Added a Last run column to the Coverage tab, parsed from scrapers/logs/scraper-summary.log - a local file appended by every run, so no database read and no egress. Each scraper now shows its most recent run date with FOUND, NEW and INVALID, a green badge when it ran today, and 'never' when it has never appeared in the log; 163 scrapers have run data and 53 ran today. Made it the standard rather than a manual step by chaining generate-site-report.js at the end of both fix-all.ps1 and fix-all.sh, which the nightly FunHive-Scrapers task already invokes, so the report is current the same morning. The chain is deliberately not gated on exit status: a report failure must never fail the data-quality chain. Documented in CLAUDE.md under Automated Maintenance and in SKILL.md Step 3e, including the point that the other tabs still project the audit files and only Steps 3b/3c rebuild those. Two bugs fixed while building it: the run-log parser had to handle the older 4-column format that omits INVALID, and the runLog loader was initially declared after the coverage map that consumes it."}
{"date":"2026-08-06","scrapers":["CustomDrupal-Libraries"],"category":"code-bug","summary":"Cobb County Public Library System's selector block reused the SAME selector (span.text-primary-1) for date, location AND description, so every event took the date string as its venue. That produced an address of the form 'Wednesday, August 5, 2026, Marietta, Cobb County, GA', which geocoding could not resolve and which cost the events their save: the 2026-08-05 run logged 7 successful extractions yet the database held 0 rows for this library. Verified Cobb is the ONLY config in the file with this collision - the other seven all use distinct, sensible selectors. Repointed location and description at the same shape its siblings use; neither element exists on cobbcounty.gov's markup, which is the intended result, since venue then falls back to the library name rather than a date string."}
{"date":"2026-08-06","scrapers":["CustomDrupal-Libraries"],"category":"site-change","summary":"CORRECTION to the 2026-08-05 entry claiming CustomDrupal-Libraries broadly returns 0 rows: that conclusion was drawn by querying scraper_name = 'CustomDrupal-Libraries', but this scraper stores each library's DISPLAY NAME in scraper_name (the documented drift), so the query matched nothing. Re-measured per library: Anderson County 61 upcoming, Rowan County 12, Kanawha County 7, Florence County 1, Richland 7 rows but 0 upcoming. The scraper is largely working. Two genuine zeros remain and have different causes - Cobb County was the selector bug fixed above, and Greenville County Library System is a SITE-SIDE problem, not a scraper bug: greenvillelibrary.org/events serves ARCHIVED OCTOBER-NOVEMBER 2020 events to anonymous fetches despite rendering a current 'AUG 6' 2026 header, so all 10 events found are correctly skipped as past. Confirmed by direct fetch and unchanged by a date_start query parameter. Recorded as an open coverage gap; the library's real current calendar is not reachable at that URL."}
{"date":"2026-08-06","scrapers":["WordPress-MD"],"category":"code-bug","summary":"Live verification run of WordPress-MD confirmed the coverage fix and exposed a regression in the naming fix. Good: FOUND rose from 89 to 163, both newly added libraries work - Ruth Enlow Library of Garrett County returned 3 events and created its activity row, Kent County returned 22 - and Talbot returned 48 on the repointed librarycalendar.com URL. Bad: the per-site scraper_name never reached the database. saveEventsWithGeocoding passes a top-level scraperName option and supabase-adapter around line 1847 lets a top-level value win over metadata.scraperName, so all 42 new rows carried the bare name. Worse, verifyAndCleanupEvents looks events up with .where('metadata.scraperName','==',scraperName) using the bare name, so the per-site value silently broke verification, which reported 0 verified. Reverted to the bare key with the reasoning recorded in the file. Per-site identity is not lost here because venue IS the library name and source_url IS that library's own URL for this scraper - confirmed 5 distinct venues and 5 distinct source_urls in the run's rows. That is genuinely different from MacaroniKid, where venue is the event's venue rather than the site, which is why MacaroniKid does need a per-site name."}
{"date":"2026-08-06","scrapers":["WordPress-MD"],"category":"site-change","summary":"Issues surfaced by the 2026-08-06 verification run, none yet fixed. Queen Anne's County Library eventsUrl now serves a Squarespace 404 page and the scraper ingested the entire page, CSS included, as an event date string. Four libraries returned zero events: Charles County, St. Mary's County, Dorchester County and Somerset County. Washington County Free Library returned only 1. INVALID rose from 4 to 6, three of them May-Sep 2026 range dates that the parser cannot resolve. Of 163 events found only 82 saved, with 43 skipped for having no date and 17 as past events."}
{"date":"2026-08-06","scrapers":["WordPress-MD","event-save-helper.js"],"category":"site-change","summary":"Fixed the issues the first WordPress-MD verification run exposed, then re-ran. Four guessed /events paths were replaced with real calendar URLs found on each library's own site: Charles County to ccplonline.org/events/calendar/, St Mary's to stmalib.org/events/calendar/, Dorchester to /calendar-of-events, Somerset to somelibrary.org/events.php. Two entries were removed as verified-covered-elsewhere, each checked against the database first: Queen Anne's, which had 33 rows sourced from calendar.qaclibrary.org and whose configured /events served a Squarespace 404, and Washington County Free, which had 28 rows from wcfl.librarymarket.com. Also guarded the invalid-date logger in event-save-helper.js: a date string over 120 characters is never a date, it means a selector matched a whole page, so it is now truncated and labelled instead of dumping 20KB of CSS into the log. Result of the re-run: found rose 163 to 310 but NEW only 82 to 80 while INVALID rose 6 to 135. The URL corrections surfaced an extraction bug rather than adding saved events - St Mary's now returns 147 events whose dates are time-only strings like '10 a.m.' that cannot be parsed. Recorded rather than papered over. Two corrective notes on the process: the first attempt at these URL edits replaced the values inside the file's AUTO-GENERATED header comment rather than the real LIBRARIES array, because String.replace takes the first match and the comment precedes the array; the patch now splits the file at const LIBRARIES = [ and only edits the code half."}
{"date":"2026-08-06","scrapers":["local-scraper-runner.js"],"category":"code-bug","summary":"MacaroniKid group runs have been misreported as FAILED in scraper-summary.log ever since runMacaroniGroup() started actually spawning the group runner on 2026-08-04, even when the child process exits 0. formatSummaryRow() branches on r.success, but the success-path return value in runMacaroniGroup() was shaped {name} with no success flag, so !r.success evaluated true and every successful group run rendered as \"FAILED — unknown error\" despite the overall SUMMARY block correctly counting it as a success today SUMMARY said 53 success 0 failed but the per-scraper table showed MacaroniKid-Group2 as failed. The child process is spawned with stdio inherit and logs its own per-site rows into scraper-summary.log via the shared scraper-summary-logger.js, so the parent genuinely has no found new dupes stats to show for the group entry itself — fixed by adding success true or false to all three return branches, matching the success name stats duration shape runScraper already uses, so it now renders as a zero-stats success row instead of a false failure."}
{"date":"2026-08-06","scrapers":["MacaroniKid-DE"],"category":"other","summary":"Confirmed the 2026-08-05 per-site listing-URL and scraper_name fix for MacaroniKid: a fresh run today (09:27 EST, after the 08:30/08:46 EST fix commits landed) shows source_url as the sites own listing page (https://newcastle.macaronikid.com, never events.yodel.today) and scraper_name as MacaroniKid-DE-newcastle. Cleared from reports/fix-notes.json _pending. The other 19 MacaroniKid states plus the 12 Maryland scraper_name corrections and 16 VenueList-* renames remain unconfirmed: the only other scraper runs today (the overnight group run, including MacaroniKid-Group2 covering FL/NY/GA/CT/MD/NH/ME) were long-running processes that started before todays fix commits and finished at 07:03 EST, before the 08:30-12:05 EST fixes landed, so they ran on stale in-memory code and cannot confirm or refute the fixes either way. Left all of those pending entries in place for a genuinely later run to confirm."}
{"date":"2026-08-06","scrapers":["WordPress-MD"],"category":"other","summary":"Step 3b library-site-audit pass. Checked scraper-run-2026-08-05.log (the Group 2 night-of-8/5 run that finished 2026-08-06 11:03 UTC because macaroni-runner-group2.js shared the window) against LIBRARY-SITE-AUDIT.md: every library scraper in that run (WordPress-NY/NJ/ME/FL/MS, LibraryMarket family, BiblioCommons-GA/NC, the LibCal/Communico groups, and the standalone single-system scrapers including CustomDrupal-Libraries) was already logged under the open ## 2026-08-05 cycle section with matching FOUND numbers, so nothing new was appended for those per the no-re-log rule. Only WordPress-MD's entry was stale, replaced with the corrected 2026-08-06 16:31 UTC verification-rerun data (FOUND 310) per explicit instruction: Charles/Dorchester/Somerset still 0 saved despite corrected URLs, St Mary's 0 saved with 147 found but unparseable time-only dates, Talbot repointed to talbot.librarycalendar.com (21), Ruth Enlow/Garrett added as new coverage (1), Kent County added as new coverage but currently 0 rows in the DB despite 22 found on its first verification pass (flagged, not re-diagnosed), and Washington County/Queen Anne's rows removed since both were dropped from the scraper's config as verified-covered-elsewhere. Per-library counts sourced from current DB rows (scraper_name='WordPress-MD' grouped by venue) since neither manual rerun wrote to scraper-stdout.log. Cycle-completion check: 80 active library-family scrapers exist per isScraperActive()/getActiveStates(); 79 have at least one entry in the current cycle. The lone gap is LibCal-VA2 (VA, group 3), registered today per SCRAPER-FIX-LOG.jsonl but never yet run in production, so the cycle is NOT complete and no Cycle-complete block was written."}
{"date":"2026-08-06","scrapers":["generate-site-report.js"],"category":"code-bug","summary":"The report was only reading the FIRST table in each audit file. Those files accumulate a section per run, each with its own table, so every row added by a later run was invisible - including all ten WordPress-MD rows rebuilt on 2026-08-06, which is why owner-verified comments for Charles County and Dorchester would not render. tableRows now scans every table whose header matches and de-duplicates on (site, scraper), keeping the last occurrence so the most recent run's numbers win. Library rows went 674 to 778 and age rows 2962 to 4293; those rows existed in the audit all along and were simply not being read."}
{"date":"2026-08-06","scrapers":["WordPress-MD","Communico-NJ"],"category":"site-change","summary":"Owner-verified annotations and a New Jersey URL fix. Jen confirmed on 2026-08-06 that Charles County Public Library and Dorchester County Public Library genuinely have no upcoming events, so both are recorded as MATCHES in reports/verification-comments.json with verifiedBy and verifiedOn fields; Charles was previously UNVERIFIABLE and Dorchester was previously MISMATCH, so this closes a false bug. Jen also supplied a working branch-filtered events URL for Somerset County Library System, which is Somerset County NEW JERSEY, not Maryland's Somerset County Library at somelibrary.org - two different libraries whose similar names are exactly the kind of confusion this project has been untangling. The Communico-NJ entry had the bare sclsnj.libnet.info/events and zero rows in the database; it now carries the branch-filtered URL with a comment naming the state explicitly. St Mary's is confirmed to have events on its corrected URL, which reinforces that its 135 INVALID rows are a date-extraction bug rather than a URL problem."}
{"date":"2026-08-07","scrapers":["WordPress-MD","Communico-MD"],"category":"code-bug","summary":"Resolved the 135 INVALID on WordPress-MD by giving two libraries back to the scraper that can parse them. A live Communico-MD run first corrected an earlier wrong claim: that scraper is healthy, producing 86 found / 72 new with 52 rows across all seven MD libraries including St. Mary's and Charles County; the previous zero-rows reading came from stale data checked before running it. With that verified, St. Mary's and Charles County were removed from WordPress-MD, where generic selectors had been reading the time field as the date and turning 147 St. Mary's events into unusable rows. Re-run result: INVALID fell 135 to 5, found fell 310 to 157 as expected, and saved events held at 79 versus 80, confirming those 147 events had never been saving. Also added sourceUrl: library.url to both Communico metadata blocks - its rows previously carried only a per-event url, so verify-coverage.js could only ever answer INCONCLUSIVE for the libraries this scraper owns."}
{"date":"2026-08-07","scrapers":["WordPress-MD"],"category":"site-change","summary":"Diagnosed the last Maryland zero. Somerset County Library's events.php has a 493-character body with no dates because every event lives in two embedded Google Calendar iframes spanning five calendar IDs; no DOM scraper can read cross-origin iframe content, so this entry returns 0 by construction rather than by bug. The events are reachable: each calendar exposes a public ICS feed at calendar.google.com/calendar/ical/<ID>/public/basic.ics, verified returning HTTP 200 with 468KB and 152KB of real VEVENT data, and node-ical is already a dependency used by scraper-pratt-library-MD.js. Recorded with the five calendar IDs rather than implemented, because it needs an ICS extraction path plus an upcoming-events filter (the feeds carry history back to 2022), which is a new capability rather than a URL correction."}
{"date":"2026-08-07","scrapers":["GoogleCalendar-MD","WordPress-MD"],"category":"new-coverage","summary":"Built and registered GoogleCalendar-MD, a new ICS scraper for libraries that publish only through an embedded Google Calendar iframe. Somerset County Library was the driver: its events.php has a 493-character body and no dates, so every DOM scraper returned 0 by construction, while five public ICS feeds sat behind the iframe carrying the real programme. The scraper reads those feeds with node-ical, expands RRULE recurrences (library storytimes are almost all recurring, so this is the difference between one row and a term's worth), honours EXDATE, skips STATUS:CANCELLED, dedupes across calendars that list the same event, and keeps a 90-day window. First live run: 169 found, 126 saved, 0 invalid dates, 0 junk titles, 0 past events, age detection splitting 87 All Ages / 21 Babies / 13 Kids / 5 Teens, and source_url set to the human-facing events.php on every row rather than the .ics endpoint. Registered as type api, group 3 for balance; active scrapers 170 to 171. Somerset was then removed from WordPress-MD, leaving it at 7 entries. Two self-inflicted problems were caught and fixed during the build: trimming the ICS LOCATION field from a full postal address to the branch name mid-flight minted new stable IDs and created 77 duplicate rows, which were deleted after a dry run, and 7 surviving rows with address-laden venues were normalised in place."}
{"date":"2026-08-07","scrapers":["generate-site-report.js"],"category":"code-bug","summary":"Coverage status showed 'none' for scrapers that had demonstrably produced events. Status was derived only from the audit files, which Steps 3b/3c rebuild during the 2:12pm diagnosis, so anything built or run after the last audit read as no-data even though the Last run column on the same row showed a successful run. GoogleCalendar-MD hit this the day it was built: 169 found, 116 saved, status 'none'. Added an 'unaudited' status for the case where a scraper has no audit rows but ran on or after the audit date and found events, with a why line naming the run date, counts and audit date so the distinction is legible. 8 scrapers move from none to unaudited; 27 remain genuinely none. This is what made an earlier verbal claim that every Maryland library was producing events uncheckable against the tracker - the database supported it but the report could not show it."}
{"date":"2026-08-07","scrapers":["generate-site-report.js"],"category":"code-bug","summary":"The library-sites Status column reported 'unmatched' for two situations that are not unmatched. Single-system scrapers have no per-site config array because they cover one site, so Pratt-Library, AACPL and Howard-County read as unmatched purely for having no list to match against; they now read 'current' with that stated. And a site deliberately handed to another scraper now reads 'moved', naming the new owner, instead of looking like something we lost - Charles County and St. Mary's to Communico-MD, Somerset to GoogleCalendar-MD. The move check requires the new owner to serve the SAME STATE: library names repeat across states, and a first pass claimed Hightower Memorial Library had moved from WordPress-AL to WordPress-GA when those are two entirely different libraries in different states, which would have repeated the cross-state confusion this work exists to undo. Same guard applies to naming the owner when several registry keys share one file, so a Maryland library no longer reads as moving to Communico-CA. Maryland now reads 21 current, 3 moved, 1 refixed and 2 unmatched, the last two being aggregate audit rows rather than real sites."}
{"date":"2026-08-07","scrapers":["event-save-helper.js"],"category":"code-bug","summary":"Step 3d verification of a newly-flagged >=70% All Ages site (WordPress-VT, Brownell Library) found real age signals the detector was missing: \"grades 1-5\", \"grades 6 and up\", \"6 grade and up\", \"entering 5th grade and above\". detectAgeRange() in supabase-adapter.js only handled closed grade ranges (\"Grades 3-5\", \"Gr 4-8\") - none of its three grade regexes match open-ended \"grade N and up/older/above\" or \"N+\" phrasing, so those events fell through to All Ages. Added two new anchored regexes (word-first \"grades N and up\" and digit-first \"Nth grade and up\") that convert to an age range capped at 18, matching the platform's Teens ceiling. Verified against all four live Brownell examples plus the existing false-positive guards (registration IDs like \"2608-2026\", times like \"1:00 - 2:30p\" both still correctly return null); the 36-case regression suite in scripts/test-age-detection.js still passes in full."}
{"date":"2026-08-07","scrapers":["WordPress-NC"],"category":"site-change","summary":"Step 3d verification of a today's-run zero-event row (WordPress-NC, Alamance County Public Library) found its configured eventsUrl (library.alamancecountync.gov/calendar/) is a dead-end nav page that only links out to the real calendar; it never had a working extraction path. Confirmed live that the linked real calendar, alamancelibraries.libcal.com, is already scraped today under LibCal-NC with 48 real events found for this exact library - full replacement coverage confirmed, not assumed. Removed the redundant/broken entry from scrapers/scraper-wordpress-libraries-nc.js rather than leaving a dead config in place. Two other zero-event rows in the same file share a dead-end eventsUrl (www.srls.info, for Pinebluff Public Library and Leath Memorial Library) that redirects to a Sandhill Regional Library System LibGuides page with real events - but LibGuides is a different platform the WordPress-family extractor can't read, so those were left in place and recorded as MISMATCH rather than silently dropped; a real coverage gap, not fixed this session."}
{"date":"2026-08-07","scrapers":["CustomDrupal-Libraries","Communico-NC"],"category":"new-coverage","summary":"Wake County Public Libraries' Communico entry dead-redirected (302 to google.co.uk) after the county migrated to a custom Drupal calendar on wake.gov. Removed the dead entry and built real coverage in CustomDrupal-Libraries against wake.gov's Drupal Views calendar (added 8-page pagination support), verified live with 129 saved events across real branches."}
{"date":"2026-08-08","scrapers":["SandhillRegional-NC","WordPress-NC"],"category":"new-coverage","summary":"Completed and committed the SandhillRegional-NC scraper left unfinished in the working tree from 2026-08-07 (registry entry, scraper file, and library-addresses.js branch data all present but never run through node -c, never live-verified end to end, and never committed). It closes the MISMATCH recorded 2026-08-07 for Moore County Library, Pinebluff Public Library, Robbins Area Branch, Leath Memorial Library and Montgomery County Library, all previously pointed at srls.info in scraper-wordpress-libraries-nc.js, which now redirects to a LibGuides platform that scraper can't read. New scraper reads the LibGuides page's three embedded LibCal calendars (children/teens/adults) and attributes each event to one of 15 real branches by keyword-matching the branch name in the event title. Found and fixed one real bug before treating it as done: saveEventsWithGeocoding() in event-save-helper.js sets metadata.sourceUrl (the source_url column) from library.url, not from the scraper's own event.metadata.sourceUrl, and the LIBRARIES array had no url field, so all 97 first-run rows saved with source_url NULL. Added url to all 16 branch entries and backfilled the 10 surviving pre-fix rows directly. Two live runs confirm it: 2026-08-07 (before the fix) 112 found/98 saved/14 dupes/0 invalid; 2026-08-08 (after) 106 found/88 saved/18 dupes/0 invalid, source_url populated on every row. Also fixed in the same pass: scraper-wordpress-libraries-ga.js's New Georgia Public Library entry (dallaslibrary.org now redirects to the Dallas, TEXAS public library) repointed to wgrls.org's branch-filtered list view, and library-addresses.js gained the Sandhill system's 15-branch address table for reuse by branch-detector-based backfills."}
{"date":"2026-08-08","scrapers":["WordPress-GA","supabase-adapter.js"],"category":"code-bug","summary":"Live re-run of WordPress-GA to confirm the same-day New Georgia Public Library URL fix (dallaslibrary.org to wgrls.org) surfaced a real extraction bug it did not fix: wgrls.org 403-blocks the TEC REST API (tryFetchTecEvents returns null), so the scraper falls back to generic DOM selectors, which grab the TEC list view's day-heading wrapper elements instead of the real event cards nested inside them - every saved row for this library was literally a date string as the title (\"Saturday, September 5, 2026\", \"Ongoing events for August 30, 2026 to September 5, 2026\"), 56 of them, all carrying the stale pre-fix dallaslibrary.org source_url too since duplicate suppression never let them update. Confirmed via WebFetch that the real page genuinely has real, distinct event titles (Storytime, Craft Club, Toddler Time, Family Fun) this scraper simply never reaches. Added two anchored isJunkTitle() patterns in supabase-adapter.js (bare full-date headings, \"Ongoing events for...\" rows) - verified against 10 cases (3 real junk positives, 7 real-title/false-positive-guard negatives including \"Saturday Movie Night\") and against a live re-run, which showed 222 junk-title skips and zero new junk saved. Deleted the 56 stale junk rows. NOT resolved at this point: real event extraction for New Georgia Public Library."}
{"date":"2026-08-08","scrapers":["WordPress-GA"],"category":"code-bug","summary":"Actually fixed the New Georgia Public Library coverage gap left open above, same day, rather than leaving it as a note. Fetched wgrls.org's raw HTML directly (plain axios GET with a browser User-Agent worked even though the REST API 403s) and confirmed it's standard TEC v2 list-view markup with stable, documented class names (.tribe-events-calendar-list__event, -title-link, -datetime with a real datetime=\"YYYY-MM-DD\" attribute, -venue-title, -venue-address, -description) entirely separate from the day-heading badge (.tribe-events-calendar-list__event-date-tag*) the generic selectors were mistaking for a title. Added tryDomScrapeTecEvents(page, libName) to tec-rest-helper.js, targeting that markup directly, and wired it into scraper-wordpress-libraries-ga.js as a fallback tried between the REST call and the fully-generic DOM scrape. Unit-tested against the live page (10 real events: title, ISO date, start/end time, description, venue, address, distinct URL, all correct) before running the full scraper. Live re-run: 925 found, 235 new (up from 234 the run before), 0 errors, 220 junk-title skips (the earlier fix still holding) - 5 real New Georgia Public Library events saved with correct venue, source_url, and per-event URLs, verified by direct DB query. Updated reports/fix-notes.json's WordPress-GA note from PARTIALLY RESOLVED to RESOLVED and reports/verification-comments.json's WordPress-GA|||New Georgia Public Library verdict from MISMATCH to MATCHES via merge-verification-comments.js. The helper function is reusable - any other WordPress-{state} file already wired to tryFetchTecEvents (NY/FL/NJ/MS/ME/NC/CT/TN/AL/VT) could fall back to it if it hits the same 403-blocked-REST-but-live-HTML situation; not retrofitted elsewhere this session, scoped to the library actually being fixed."}
{"date":"2026-08-08","scrapers":["WordPress-AL","WordPress-CT","WordPress-FL","WordPress-ME","WordPress-MS","WordPress-NC","WordPress-NJ","WordPress-NY","WordPress-TN","WordPress-VT"],"category":"code-bug","summary":"Retrofitted tryDomScrapeTecEvents (built for WordPress-GA/New Georgia Public Library above) into the other 10 WordPress-{state} files already wired to tryFetchTecEvents, at the user's explicit request. Same insertion pattern in every file: import the new function alongside tryFetchTecEvents, call it with the already-navigated page right after the goto+settle delay, and continue past the fully-generic DOM fallback whenever it returns real events, mirroring the code already proven live on GA. All 11 files (GA plus these 10) now share one TEC-DOM extraction path instead of duplicating one per state. node -c passed on all 10 files plus the shared helper. Did not run all 10 scrapers fully live (would be roughly 2 hours combined) - instead ran WordPress-FL (60 libraries, the smallest of the ten) end to end as a regression check: 1271 found, 412 new, 0 errors, and the existing junk-title guard still correctly caught a different library's day-heading text on a TEC variant tryDomScrapeTecEvents legitimately did not match (not every TEC install uses the v2 list-view markup this function targets), confirming the fallback chain degrades safely rather than silently breaking. The other 9 states are unverified beyond node -c and the identical, already-proven code path - genuinely new coverage on them will only be confirmed the next time each runs on its normal rotation."}
{"date":"2026-08-08","scrapers":["WordPress-AL","WordPress-CT","WordPress-ME","WordPress-MS","WordPress-NC","WordPress-NJ","WordPress-NY","WordPress-TN","WordPress-VT"],"category":"other","summary":"Live-verified all 9 remaining retrofitted states at the user's explicit follow-up request, closing the gap left above (GA and FL were the only two run live before this). Ran in three parallel batches of three (smallest to largest by library count) to bound total wall-clock time rather than running all nine sequentially, which would have taken roughly two hours: batch 1 MS/AL/TN, batch 2 NC/VT/CT, batch 3 ME/NJ/NY. All nine completed with 0 errors: MS 877 found/334 new/26 invalid, AL 1194/373/252, TN 1083/501/57, NC 480/130/142, VT 1619/719/98, CT 1692/350/412, ME 1261/360/24, NJ 3090/1217/343, NY 4907/1581/139 (NY alone took 5095s given its 422 libraries, finishing 2026-08-09T00:26 UTC - after local midnight, which is why a site-report regeneration afterward showed only 1 scraper as having run 'today', a report-generation-time artifact of the date rollover, not a data problem). No code changes were needed - every run confirmed the identical, already-proven fallback chain (REST, then TEC DOM markup, then generic selectors) degrades safely across all nine sites with no new errors or regressions. Did not attempt to isolate exactly which individual libraries within each state file the new TEC-DOM path rescued versus which still fall through to the generic selectors or the junk-title guard - that would need a before/after row-level diff per state, not done here."}
{"date":"2026-08-09","scrapers":["generate-site-report.js"],"category":"code-bug","summary":"User asked what the site report's 'not in active registry' note meant for three names (Dedham, Grill, Wedding Venue Montgomery, AL). Root cause: three Eventbrite-Family-Eastern venues have a literal pipe character in their real name (Eventbrite's own 'Venue Name | City' convention - confirmed against the events table: 'Rev'd Indoor Cycling | Dedham', 'Pats Select Pizza | Grill', 'ALOE Event Center | Wedding Venue Montgomery, AL'). Whichever pass wrote these three rows into AGE-RANGE-AUDIT.md did not escape the pipe, so it was read as a markdown table column separator, splitting the Site cell in two and shifting the real Scraper column (Eventbrite-Family-Eastern, genuinely registered and active) one position right - the generator's column-position parser then read the orphaned name fragment as if it were the scraper name and correctly flagged that fake value as unregistered. Three OTHER pipe-containing venues already in the file (Hall Art Foundation, Resurrection Church, Amped) were properly escaped with a backslash, so this was an inconsistency in how these three specific rows were written, not a systemic generator bug or a real coverage gap. Escaped all three (\\| instead of |) directly in AGE-RANGE-AUDIT.md. Verified by regenerating the report: Coverage's not-in-registry count went 3 to 0 and total Coverage rows 175 to 172. Two more pipe-containing venues (Stella Pasta Bar & Bistro | Seven Points Brewery, Gleacher Conference Center | Lounge 500 | 5th Floor) exist in the events table but are not yet in the audit file - worth escaping proactively if a future Step 3b/3c pass adds them, rather than waiting for the same symptom to reappear."}
{"date":"2026-08-09","scrapers":["generate-site-report.js"],"category":"code-bug","summary":"User reviewed the WordPress-GA rows in the report and caught two real report bugs plus real scraper bugs I had not looked at when I earlier said New Georgia Public Library was fixed. Bug 1: a row with no link recorded in the audit was rendering the same green 'current' badge as a confirmed host match, even though there is nothing to verify against - added a distinct 'no-link' status (warn-colored) instead of silently reusing 'current'. Bug 2, the bigger one: 'current' status is computed purely from URL-matching and never checked the verification verdict, so a row could show green 'current' sitting directly next to a red MISMATCH verdict pill for the exact same site - found live on Auburn Library, which read 'current' with 0 found while its own verification comment said the site has real August events the scraper is missing. Added an override in the per-site status loop: when a confirmed MISMATCH verdict exists AND the base status is 'current' (scoped narrowly - not 'refixed'/'moved'/'unmatched', which already carry their own detail, and not UNVERIFIABLE, which is unconfirmed rather than proven), the status becomes 'mismatch' (red) with the verdict's own comment as the detail. Fleet-wide this affected 22 rows, not just GA. Both fixes verified by regenerating the report."}
{"date":"2026-08-09","scrapers":["WordPress-GA","LibCal-GA"],"category":"seed-data","summary":"Investigated the five WordPress-GA rows the user flagged as not looking fixed despite their 'current' status. Auburn Library: auburnlibrary.org is Auburn, MASSACHUSETTS Public Library (369 Southbridge Street, Auburn MA 01501, confirmed via its own Assabet Interactive calendar's JSON-LD address) - moved to LibCal-GA as the real Auburn Public Library, part of Piedmont Regional Library System (prlib.libcal.com/calendar/Auburn?cid=20687, found live via prlib.org/auburn's own events link), confirmed live with 20 real events (Crochet/Knit Club, Ready to Read Pre-K Storytime, Teen Thursday, etc) after fixing a Barrow County entry and re-running LibCal-GA (33 found, 19 new, 0 errors). Boston Carnegie Library (bostonlibrary.org) and Warren P. Sewell Memorial Library-Bremen (bremenlibrary.org, TLS cert belongs to bremenmainelibrary.org, Maine) and Wheeler County Library (alamolibrary.org, parked-domain redirect stub) were all confirmed dead or wrong-state live and REMOVED with no replacement found - three genuine open coverage gaps for Boston GA, Bremen GA, and Alamo/Wheeler County GA, not resolved this session. Decatur County - Gilbert H. Gragg Library was left in place: bainbridgelibrary.org times out, but the real institution (Southwest Georgia Regional Library System, swgrl.org/calendar.php, FullCalendar.js widget, real address 301 S. Monroe Street Bainbridge GA 39819 confirmed live) needs a new bespoke extraction path like FullCalendar-Libraries' existing single-library Blue Ridge Regional entry, not a URL swap - not built this session. Live re-run of WordPress-GA after all four config changes: 912 found, 237 new (up from 235), 0 errors, no regression. All five verification-comments.json verdicts and the WordPress-GA fix-note updated to record what was actually confirmed versus what remains open."}
{"date":"2026-08-09","scrapers":["generate-site-report.js","SandhillRegional-NC"],"category":"code-bug","summary":"User caught a real report bug using AACPL as the example: single-system scrapers show two rows for the same site when the audit's site-name text drifts between cycles (e.g. 'Anne Arundel County Public Library (AACPL)' vs without the suffix) - tableRows()'s exact-string dedup doesn't catch it since the two strings differ. Fixed by collapsing rows within the same scraper on cfgKey(site) instead, which already strips the parenthetical suffix for config matching. First implementation gated the collapse on cfgCounts(scraper)===0 ('single-system') and wrongly wiped SandhillRegional-NC's 10 real branches down to 1 row - loadConfigIndex()'s regex parser only recognizes a literal quoted url string per config entry, and that scraper's LIBRARIES array set url: LISTING_URL (a variable reference) on all 16 entries, so it silently undercounted to 0 and looked single-system when it configures 16 real distinct sites. Caught before committing by checking the actual row count in the generated HTML, not just the summary log line. Redesigned to key on cfgKey(site) directly instead of trusting cfgCounts - two real distinct branches never share a cfgKey, so this is safe regardless of whether cfgCounts is accurate, and also reverted scraper-sandhill-regional-library-nc.js's LIBRARIES array to literal url strings (matching every other scraper file's pattern) so future static-analysis tooling reads it correctly too. Verified: AACPL now 1 row, SandhillRegional-NC still 10, library rows 1182 to 796 (386 genuine same-site collapses, not one wrongly merged pair)."}
{"date":"2026-08-09","scrapers":["WordPress-GA","tec-rest-helper.js","library-addresses.js"],"category":"new-coverage","summary":"User asked to find the real domains for the three open coverage gaps left above (Boston Carnegie, Bremen, Wheeler County) and wire them in, not just report the findings. Wheeler County Library: real institution is Ocmulgee Regional Library System (orls.org), a TEC site that groups branches by CATEGORY rather than venue - added an optional categorySlug parameter to tryFetchTecEvents() (appends &categories=...), confirmed live via the REST API directly (6 real events: Afterschool Parents Meeting, Plant a Seed storytime, etc). Warren P. Sewell Memorial Library-Bremen: real institution is West Georgia Regional Library System (wgrls.org, the same system as New Georgia Public Library, already fixed 2026-08-08) - found the Bremen branch (not to be confused with a same-named branch in Bowdon, GA at a different address) via wgrls.org/locations, tribe_venues id=75, falls through to the existing tryDomScrapeTecEvents fallback since wgrls.org REST is 403-blocked site-wide; confirmed correct institution, genuinely near-zero events some weeks. Boston Carnegie Library: real institution is Thomas County Public Library System (tcpls.org), which embeds LibCal as a widget on its own page rather than serving a standalone tcpls.libcal.com/calendar/{slug} page (that path 404s) - added a new tryFetchLibCalAjaxList() function that hits the widget's own AJAX JSON endpoint directly (found via network-request capture during a Puppeteer render) with a camps=9124 branch filter, confirmed live with 20 real events. Building Boston surfaced a real, separate geocoding bug: library-addresses.js had a bare 'Carnegie' branch alias under Savannah's Live Oak Public Libraries system that substring-matched ANY venue name containing the word Carnegie via getLibraryAddress()'s cross-system fallback path - Andrew Carnegie funded hundreds of libraries nationwide with this exact naming convention, so it collided with Boston Carnegie Library and sent its coordinates to Savannah, 200+ miles away. Removed the alias, deleted the 12 bad-coordinate rows it had already created, added a real street address to the Boston config entry, and re-ran clean (coordinates now correctly in south Georgia). Full live re-run after all changes: 945 found, 250 new, 0 errors. All three verification-comments.json verdicts and the WordPress-GA fix-note updated from open-gap to resolved."}
{"date":"2026-08-09","scrapers":["project-status.js"],"category":"other","summary":"Project owner asked for a standing session close-out: what was fixed, what is still broken, and what remains before the project is 100 percent functioning - shown as a breakdown first, then wired into the routine on approval. Added scripts/project-status.js, computing an 8-gate distance-to-done table and a severity-tiered broken list from LOCAL FILES ONLY (both audit .md files, verification-comments.json, fix-notes.json, the registry plus each scraper's own config array, county-centroids.js) for zero Supabase egress. The two gates that genuinely need a DB read (scraper_name conformance, source_url coverage) are declared in a STALE_METRICS block with the date measured and the refresh command, and render marked STALE rather than guessed - a number that cannot be computed must not look like one that can. --save maintains STATUS.md as a trend ledger, newest first, each entry carrying a machine-readable STATUS-DATA HTML comment so deltas are read from that rather than by re-parsing human tables. Three real bugs were caught while building it, each checked against an independent source rather than assumed: (1) gate 1 initially reported the FLEET AVERAGE for county resolution, 54.7 percent, which averaged away the actual defect - WordPress-star alone is 4.3 percent (92 of 2139), matching MASTER-PLAN's independently measured 3.4 percent; the gate now tracks the worst family with a sample of at least 20, since a mean here is the same aggregation mistake AGE-RANGE-AUDIT.md's no-aggregation rule exists to prevent. (2) The same-day replace in STATUS.md used a lazy regex whose end-anchor ran under the multiline flag, so it matched the end of the first LINE rather than the end of the entry and orphaned the rest of the old block - caught as 1 heading but 2 STATUS-DATA blocks after a re-run, replaced with explicit heading-walking, verified clean over three consecutive same-day runs. (3) The config parser counted 1460 entries unparseable because it only looked for a literal url field; added an eventsUrl fallback. Gate 2 independently cross-checks: this script measures 1074 entries on 348 colliding hosts versus MASTER-PLAN's separately measured 1073 on 355, the delta being entries fixed since. Delta direction was tested both ways with a synthetic prior entry. Wired into SKILL.md as Step 7 and CLAUDE.md so every session ends this way, not only the scheduled 2:12pm diagnosis."}
{"date":"2026-08-09","scrapers":["project-status.js"],"category":"other","summary":"Owner set the age-detection goal as 'as high as possible, no fixed target'. Gate 5 measured All-Ages share, where LOWER is better, so a maximise-this goal pointed at a minimise-this metric would have made every delta arrow in the ledger read backwards. Flipped the gate to measure specificity instead - share of audited events carrying a real age bracket rather than the All Ages catch-all (38.8% now, the complement of the 61.2% previously shown). Target stays null by design and a ratchet was added in its place: the bar is the best value ever recorded in STATUS.md, so regressions surface without inventing a ceiling that does not exist - some sources (festivals, general Eventbrite, KidsOutAndAbout) are genuinely all-ages, which is why 100% is neither reachable nor desirable and why Step 3c already excludes them from flagging. The broken-list row now fires on either condition: under 50% resolved, or any regression below best-ever. Also migrated the existing ledger entry on read - a snapshot carrying only the old allAgesShare key has specificAgeShare derived as 100 minus it, so the trend line survived the metric change instead of restarting with no history."}
{"date":"2026-08-09","scrapers":["county-centroids.js","SandhillRegional-NC"],"category":"seed-data","summary":"Added 28 real county centroids that active scrapers already referenced but which were simply absent from the dataset, so the county tier of the geocoding chain was dead for them and their events fell through to the STATE centroid. NC gained 20 (Alamance, Brunswick, Iredell, Henderson, Craven, Moore, Montgomery, Richmond, Anson, Hoke, Halifax, Cleveland, Carteret, Lee, Lenoir, Lincoln, Burke, Warren, Catawba, Haywood) taking that state from 13 counties to 33; GA gained 8 (Decatur, Miller, Seminole, Thomas, Wheeler, Haralson, Barrow, Paulding), every one a county configured during this session's library work. Coordinates came from Nominatim through the project's own geocodeAddress() and each was REJECTED unless it fell inside the state's bounding box, so a wrong-state match could not slip in; 28 of 28 passed. Effect measured on NC: unresolved county entries fell from 197 to 70, and the 70 that remain are all the fabricated '<City> County' form, which is the separate seed-data defect (MASTER-PLAN Defect B) needing URL remediation rather than more coordinates. This specifically unblocks SandhillRegional-NC, built yesterday, whose 16 branches sit in Moore/Montgomery/Richmond/Anson/Hoke and resolved to nothing before this. Verified the controls still fail correctly afterwards - a fabricated 'Carthage County' and a wrong-state 'Moore, MA' both still return null, confirming the matching was not over-broadened."}
{"date":"2026-08-09","scrapers":["SouthwestGeorgia-GA","WordPress-GA"],"category":"new-coverage","summary":"Built SouthwestGeorgia-GA, closing the last open coverage gap from the 2026-08-09 report review. WordPress-GA carried Decatur County - Gilbert H. Gragg Library on bainbridgelibrary.org, which times out; the real institution is the Southwest Georgia Regional Library System, whose swgrl.org/calendar.php is a FullCalendar.js widget with no server-rendered event markup, so no DOM or TEC extractor could ever read it. Captured network traffic during a Puppeteer render and found the Revize CMS's own JSON handler behind the widget, which returns the entire calendar as structured JSON - title, ISO start, location, description, url, allDay - so the scraper reads it with plain axios and needs no browser. Covers all THREE branches (Decatur, Miller, Seminole) rather than only the one WordPress-GA had configured, because the other two had upcoming events FunHive was getting none of. First live run: 217 found, 11 saved, 0 errors, 0 invalid dates; the 206 skipped are correctly past, as the feed carries years of history. Verified in the database: 11 rows, real coordinates in Donalsonville and Colquitt, source_url on every row set to the human-facing listing page rather than the JSON endpoint, zero null coordinates. Decatur itself has 0 upcoming right now, which is a real-world fact and not an extraction failure - it is the system's MOST active branch at 103 events with the newest dated 2026-07-30, simply between programming cycles. Deliberately excludes the feed's 'Main' calendar: its two rows are Revize vendor test events located at 150 Kirts Blvd Troy MI, and ingesting them would have planted Michigan coordinates under a Georgia library, the exact cross-state contamination this project keeps unwinding. Also added Decatur/Miller/Seminole county centroids, which were missing."}
{"date":"2026-08-09","scrapers":["event-save-helper.js"],"category":"code-bug","summary":"saveEventsWithGeocoding built its own metadata block and set platform from the scraper-level OPTION, unconditionally discarding any per-event metadata.platform. Same class of bug as the same function overriding metadata.sourceUrl with library.url, found earlier the same day. Effect: a scraper that tagged rows by WHICH extraction path produced them - added 2026-08-08 across 11 WordPress-{state} files to distinguish the new TEC-DOM fallback from the generic DOM scrape - silently lost that tag, so every WordPress-GA row read platform 'wordpress' regardless of origin and the question 'which libraries did the TEC-DOM retrofit actually rescue?' was unanswerable from the database despite looking instrumented. Fixed so a per-event platform wins over the scraper-level default; safe because nothing queries by platform, unlike scraperName whose per-site variant had to be reverted on 2026-08-06 since verifyAndCleanupEvents looks events up by it. Confirmed working on fresh rows: SouthwestGeorgia-GA's first run stored platform 'revize-calendar' correctly. IMPORTANT LIMITATION, measured not assumed: this cannot retroactively attribute EXISTING rows. findExistingDuplicate runs before the save and continues past duplicates, so a re-run does not rewrite them - after a full WordPress-GA re-run finishing 17:38, New Georgia and Boston Carnegie rows still carried their 14:24-14:51 scraped_at and the old platform value. Attribution therefore accrues only as genuinely new events save. For GA the answer is already known from building them: New Georgia Public Library and Warren P. Sewell Memorial Library-Bremen are the two on the TEC-DOM path. For the other 10 retrofitted states, which individual libraries benefit remains unmeasured rather than assumed."}
{"date":"2026-08-09","scrapers":["CustomDrupal-Libraries"],"category":"other","summary":"Resolved the COLLAPSED flag that check-scraper-names.js raises for CustomDrupal-Libraries (8 configured sites, 1 distinct scraper_name). It is a measurement artifact, not a real aggregation problem. Only one of the eight sites carries a conforming name - CustomDrupal-Libraries-wake, 125 rows across 24 venues - while five of the other seven DO produce rows but under their library DISPLAY NAME as scraper_name: Anderson County Library System 60, Kanawha County Public Library 20, Rowan County Public Library 19, Richland Library 10, Florence County Library System 1. Those simply do not match the registry-key pattern, so they are counted as FREE_TEXT drift elsewhere rather than toward this scraper's site total, which makes the site count look collapsed to one. The underlying issue is naming drift (MASTER-PLAN Defect C), already tracked and deliberately excluded from daily fix work, not a case of many sites being wrongly merged onto a single identifier. The two genuine zeros are known and separately documented: Greenville County serves archived 2020 events to anonymous fetches (site-side), and Cobb County had a selector collision fixed 2026-08-06."}
{"date":"2026-08-09","scrapers":["county-centroids.js","fix-fabricated-counties.js","WordPress-ALL"],"category":"seed-data","summary":"MASTER-PLAN Defect B FIXED. The scraper seed data was generated by appending ' County' to the city name, so ~2000 configured entries carried values like 'Bainbridge County' that are not real counties; getCountyCentroid returned null for all of them, killing the county tier of the geocoding chain and dropping those events on the STATE centroid. Fixed data-first rather than file-by-file, since 'do not guess counties one at a time' was always the constraint. Sourced two free authoritative datasets: the US Census Bureau 2023 National Counties Gazetteer (county FIPS to name plus INTPTLAT/INTPTLONG centroid, all 3222 US counties) and the GeoNames US dump (populated place to county FIPS), joined on state plus FIPS. Stage 1 completed scrapers/utils/county-centroids.js from Census - 436 hand-entered values KEPT and 2801 added, going from ~405 counties to 3237. Existing values were deliberately never overwritten because a Census internal point is not always the better centre: San Francisco's sits about 54km offshore near the Farallon Islands while the hand-entered value is downtown, and only 2 of 405 existing entries disagreed with Census by more than 40km, both of that kind. Stage 1 alone resolved 939 entries whose county name was already correct but simply absent from the dataset. Stage 2 added scripts/fix-fabricated-counties.js, which rewrote 1931 fabricated values across 34 files, touching ONLY entries that did not already resolve. Net effect: unresolved county entries fell 2948 to 57, and the project-status gate for counties went 4.7 to 98.4 percent. Validation done before applying anything: 31 of 31 independently-known city-county pairs correct (Boston-Suffolk, Pittsburgh-Allegheny, Buffalo-Erie, Miami-Miami-Dade and so on). Two real bugs were caught and fixed during the work rather than shipped. First, indexing GeoNames alternatenames produced flatly wrong joins - Great Neck NY resolved to Suffolk County when it is in Nassau - so only official and ascii names are used. Second, the first --save run broke 7 files because a naive quoted-string regex stopped at the ESCAPED apostrophe in values like 'Prince George's County', both mis-parsing correct entries as broken and emitting invalid JS; caught by node -c before any commit, reverted, fixed with an escape-aware matcher, and the corrected run then made 10 fewer changes because those entries were correctly recognised as already valid. Deliberately NOT changed: about 292 entries whose county already resolves but where the dataset disagrees, nearly all Connecticut, because Census 2023 replaced CT's eight traditional counties with nine Planning Regions and rewriting them would be a regression rather than a fix; county-centroids.js now carries both forms. Virginia independent cities correctly resolve to their 'city' county-equivalent. Provenance and known limits recorded in scripts/data/README.md."}
{"date":"2026-08-09","scrapers":["SouthwestGeorgia-GA"],"category":"code-bug","summary":"check-scraper-names.js flagged SouthwestGeorgia-GA (built earlier today) as COLLAPSED: 3 declared sites, 1 distinct scraper_name. Root cause: event-save-helper.js's saveEventsWithGeocoding() always writes its own scraperName OPTION into metadata.scraperName, ignoring whatever an individual event object sets, so one call covering all three branches collapsed them onto a bare 'SouthwestGeorgia-GA'. Fixed by splitting saveToDatabase() into one saveEventsWithGeocoding() call per branch (filtering events by venueName), each passing its own '<SCRAPER_NAME>-<siteSlug>' (decatur/miller/seminole), matching CLAUDE.md's per-site naming rule. Verified live: re-run produced 9 new events with 'Verifying events for SouthwestGeorgia-GA-decatur/-miller/-seminole' as three separate verification passes, and a DB query confirmed rows now carry SouthwestGeorgia-GA-seminole (8) and SouthwestGeorgia-GA-miller (1) as distinct scraper_name values. The 2 rows from the pre-fix run keep the old bare name until they expire, per the standard rename caveat."}
{"date":"2026-08-09","scrapers":["fix-url-collisions.js","WordPress-ALL","LibCal-ALL"],"category":"seed-data","summary":"MASTER-PLAN Defect A largely fixed. The seed generator guessed {city}library.org from each library's city, so 347 hosts were claimed by 2 or more states at once - madisonlibrary.org by 11, franklinlibrary.org by 10. At most one claimant per host can be right, and because the scraper still extracts whatever it finds at a wrong URL, the others were writing ANOTHER library's events into the database under the wrong name and state, which is worse than an honest zero. No dataset can say who owns a domain, so evidence came from fetching each of the 347 hosts ONCE (not once per entry) and reading what the site says about itself: postal 'City, ST 12345' patterns, full state names, TLS certificate alt-names, redirect targets and the page title. 289 returned 200 and 177 resolved to a definite state. Removed 234 entries across 19 files - 183 where the host's state is established strongly and differs from the entry's claim (miltonlibrary.org titles itself 'Milton Public Library - Milton, Massachusetts' yet was claimed by PA/NY/FL/WV/VT/DE/NH; newportlibrary.org is Newport OREGON, outside the active region entirely, claimed by PA/NY/NC/KY/ME) plus 51 on genuinely dead or parked hosts. Colliding entries fell 1072 to 787 and colliding hosts 347 to 249. Also deleted 924 already-contaminated database rows, scoped precisely to the (host, wrong-state) pairs removed so the correct claimant's rows survive - athenslibrary.org's GA rows are real and were kept while its TN/AL/NY/PA rows were another library's events mislabelled. One serious misclassification was caught before saving: HTTP 403 was initially treated as a dead domain, which would have removed 93 entries for libraries that are merely behind a WAF; 403/401/429 now classify as BLOCKED and are never acted on, which is the same 'UNVERIFIABLE means unknown, not broken' rule the verification store already follows. Only 4 hosts were genuine 404s. Deliberately NOT acted on: 210 entries where evidence points at another state but not conclusively, and 517 whose host gave no usable signal - all recorded in reports/defect-a-removals.md alongside every removed library's name, city, state and old URL so nothing is written off and each can be restored when a real URL is verified."}
{"date":"2026-08-09","scrapers":["LibCal-VA2"],"category":"code-bug","summary":"LibCal-VA2 (registered 2026-08-06, never run in production per its _pending note) had TWO stacked bugs, both confirmed live before fixing: (1) its DOM selectors ('.s-lc-ea-e, .s-lc-whw-row', '.s-lc-ea-date', etc.) never matched current LibCal markup — 0 events across all 11 VA systems on two consecutive live runs including Fairfax County, the state's largest. Replaced with the same multi-selector fallback list and regex-based date extraction already proven working in the shared LibCal file (scraper-libcal-libraries-CA-CO-DE-FL-LA-MA-NY-SC-TN-TX-VA-WA.js): '.s-lc-c-evt'/'.s-lc-eventcard'/'.s-lc-mc-evt' etc. (2) Separately, the registry's exportName pointed at scrapeLibCalEvents, which only scrapes and returns a raw array — it never calls saveToDatabase() or reports save stats; only main() did both, and main() was never exported. So even after fixing the selectors, the first re-run found 182 real events but still logged 'Found: 0, New: 0' and saved nothing. Added scrapeLibCalEventsCloudFunction (same found/save/log pattern as scrapeSouthwestGeorgiaLibrariesCloudFunction) and repointed the registry's exportName to it. Verified live end to end: 182 found, 164 new saved, 1 invalid date, 0 errors, across Fairfax(10) Arlington(15) Richmond(18) Norfolk(46) Roanoke(48) Suffolk(20) Williamsburg(5) Library-of-Virginia(20). Also found and removed two dead entries: Newport News and Hampton's eventsUrl domains both DNS-failed (ERR_NAME_NOT_RESOLVED); verified live neither library is actually on LibCal anymore (Newport News runs its own /264/Events-Calendar page; Hampton's old domain now redirects to an unrelated church site, real site is hampton.gov with no LibCal calendar found) - removed rather than left erroring every run, recorded as an open coverage gap, not resolved. Prince William Public Library System resolved fine but returned 0 events this run, which may be a real empty calendar rather than a bug - not investigated further this session."}
{"date":"2026-08-10","scrapers":["supabase-adapter.js","Dorchester-County","Somerset-County"],"category":"code-bug","summary":"_stableEventId() derived every row id from _normalizeUrl(), which strips the ENTIRE query string. That is right for tracking junk but catastrophic for sites whose event identity lives in the query - Google Calendar's htmlLink is https://www.google.com/calendar/event?eid=<unique>, so every Google-Calendar-sourced event in the whole fleet normalized to the single path /calendar/event and hashed to ONE id. First writer won the row; every later event hit 23505 and came back {duplicate:true}, which scrapers that only check .skipped log as a successful import. Dorchester-County therefore reported 'Found 12, New 12' on every run for months while writing nothing - the id it kept colliding on was owned by 'Somerset County Library / Crisfield Library / Open Gym', a different library entirely. Note this exact bug was diagnosed for Somerset-County on 2026-08-03 and 'fixed' by switching from LIBRARY.website to the per-event htmlLink, with a long code comment asserting each item has a unique stable link - that fix never worked, because query-stripping collapsed the htmlLinks too, and nobody re-checked the row count afterwards. Real fix: added _identifyingQuery(), which folds any NON-tracking query params (sorted, so ordering drift is harmless) into the hash while a denylist of utm_*/fbclid/gclid/etc is still ignored. URLs with no query string hash exactly as before, so existing ids do not churn - verified explicitly. Confirmed live end to end: Dorchester-County 0 -> 12 distinct rows, Somerset-County 0 -> 153 rows. Also fixed Dorchester's success logging to treat {duplicate:true} as skipped rather than imported, so a future collision cannot hide behind a green count again; the same miscount pattern exists in other .add()-path scrapers and was not swept this session."}
{"date":"2026-08-10","scrapers":["WordPress-PA"],"category":"seed-data","summary":"Acted on the 32 MISMATCH verdicts the 2026-08-09 Step 3d pass produced for this file's zero-event entries. Removed 17 entries verified live to serve a different institution or to be dead, and re-verified the three highest-stakes ones myself rather than trusting the batch: harrisburglibrary.org is Harrisburg district library in ILLINOIS (the entry claimed Dauphin County Library System, one of PA's larger systems), eastonlibrary.org is Easton Public Library at 691 Morehouse Road, Easton CONNECTICUT, and greenvillelibrary.org is Greenville County SOUTH CAROLINA. Three were dead rather than colliding: Allentown Public Library was configured as a bare sites.google.com that redirects to a Google login, charleroilibrary.org redirects to a parked domain, franklinlibrary.org to a spam blog. Removing Allentown is a genuine dedup, not a coverage loss - confirmed by source host, not by name, that the dedicated Allentown-Public scraper holds 10 real rows from allentownpl.org. One entry was CORRECTED instead of removed: hamlinlibrary.org is a real PA library (Hamlin Memorial, Smethport, Seneca District) that the seed generator had filed as 'Salem Public Library' in Hamlin, Lebanon County - renamed with the right city/zip/county, McKean, which resolves via getCountyCentroid. IMPORTANT NEAR-MISS worth recording: the first contamination sweep matched DB rows by venue NAME and found 61 'contaminated' rows across Easton, Allentown, Columbia, Dover and Greenville. Inspecting them before deleting showed they were legitimate events produced by OTHER working scrapers - eastonpl.libcal.com, allentownpl.org and MacaroniKid - and deleting them would have destroyed real coverage, the exact Worcester-class error CLAUDE.md's 'identity by source_url host only' rule exists to prevent. Re-scoped by source host, exactly ONE genuinely contaminated PA row existed: 'Bouncing Babies' sourced from greenvillelibrary.org but stored as Greenville PA, a South Carolina event mislabelled as Pennsylvania. Deleted that one only. The 13 extraction-failure MISMATCHes (right PA institution, real live events, scraper returns 0) are NOT fixed - they need per-site DOM work and remain open."}
{"date":"2026-08-10","scrapers":["supabase-adapter.js","RollyPollies-MD","WithApps-Libraries"],"category":"code-bug","summary":"Swept the .add() path for the miscount found in Dorchester-County. A static audit of all 267 scraper files found 96 events/activities .add() call sites, of which 94 across 88 files increment their own imported counter without checking the {duplicate:true} return. That alone only inflates the NEW column; it becomes DATA LOSS when distinct events collapse onto one stable id, which the miscount then hides behind a healthy-looking count. Rather than edit 88 files, added a zero-egress in-process collision detector to the adapter: .add() remembers each derived id and the name first seen under it, and warns loudly when a second, DIFFERENTLY-NAMED event hashes to the same id. Same-name re-scrapes, the normal case, stay silent, and no extra database reads are involved. Verified both directions with a probe. Empirical sweep then compared claimed NEW against real row counts and found TWO more live instances the static regex had missed because neither used the ||-fallback shape it looked for. RollyPollies-MD builds url as a ternary between two booking landing pages: claimed 20 new, 1 row existed, now 10 (the residual gap is idx_events_unique_content on name+event_date+venue, which cannot hold two same-named sessions on one day - schema-level, not fixed). WithApps-Libraries puts the same events-calendar URL on every event: claimed 38 new, 1 row existed, now 37. Both now pin id to the name|eventDate|venue key via the newly-exported _stableEventId. WithApps also had no metadata.scraperName so its rows were landing under the display name Hampton Public Library; set to the registry key. IMPORTANT CORRECTION this surfaced: Hampton Public Library was reported earlier the same day as an open coverage gap after being removed from LibCal-VA2 - it is not, WithApps-Libraries covers it with 37 live rows. Newport News remains the one genuine gap. The 94-site counter miscount itself is NOT fixed and remains open; it is a large mechanical change and the detector now makes its dangerous cases visible."}
{"date":"2026-08-10","scrapers":["MacaroniKid-ALL","event-save-helper.js","supabase-adapter.js"],"category":"code-bug","summary":"Fixed the .add() duplicate-miscount across the whole fleet: 93 of 96 events/activities call sites incremented their own imported counter without checking the {duplicate:true} return, so every 23505 - a row that already existed and was NOT written - was reported as a new import. This inflated the NEW column everywhere and, combined with a stable-id collapse, hid total data loss behind a healthy count, which is how Dorchester-County reported Found 12 New 12 for months while writing nothing. Written as a codemod, scripts/fix-add-duplicate-miscount.js, rather than 86 hand edits: it inserts an else-if branch between the existing skipped and success branches. The branch increments a duplicate counter ONLY where a let/var declaration for it is found between the enclosing function's opening brace and the call site, via a real brace walk; where no counter is in scope the branch is deliberately left empty, since keeping the imported count truthful is the point and referencing an out-of-scope identifier would be a runtime ReferenceError that node -c cannot catch. The first attempt found 0 counters because the backward walk accepted any brace preceded by a closing paren and so stopped at the innermost for-loop rather than the function; requiring an actual function keyword or arrow fixed it and found 89 of 91. Result: 91 sites in 86 files, 88 with a counter (84 skipped, 4 totalSkipped), 2 intentionally empty, plus 2 more in scraper-farms-eastern-us.js fixed by hand because its result is assigned inside a try/catch that retries without activity_id, splitting declaration from assignment. Verified three ways: node -c on all 86 files; an INDEPENDENT scope verifier, scripts/verify-duplicate-branch-scope.js, which re-derives every function body by forward brace matching and confirmed all 91 increments reference a variable declared in their enclosing function; and two live runs. Dorchester-County, whose 12 rows all already existed, now reports 0 imported / 12 skipped instead of 12 new. Wicomico-Public reported 21 found / 4 new / 17 duplicates and its row count moved 15 to 19, an exact match between claimed and actual. scripts/audit-add-miscount.js now reports 96 of 96 call sites clean."}
{"date":"2026-08-09","scrapers":["verification-comments"],"category":"other","summary":"Reconciled the open MISMATCH bug list against the same day's Defect A removals before attempting any fixes. Of 120 MISMATCH verdicts in the store, 46 referred to config entries that NO LONGER EXIST - they were removed during the Defect A pass because their domain resolved to another state's library or was dead, so they were producing that other library's events rather than failing to produce this one's. Those verdicts were flipped to MATCHES with a comment stating plainly that they were resolved by REMOVAL, not by a fixed extraction, and pointing at reports/defect-a-removals.md where the library's name, city, state and old URL are kept so it can be restored. That left 73 genuinely live bugs to work on and dropped the store's MISMATCH count 120 to 74. Doing this first mattered: re-investigating a bug whose entry is already gone is wasted work, and marking it fixed without saying why would have overstated what was repaired."}
{"date":"2026-08-09","scrapers":["jsonld-events-helper.js","WordPress-ALL"],"category":"code-bug","summary":"Built a shared schema.org JSON-LD event extractor and wired it into all 21 active WordPress-{state} library scrapers. Re-probing the 73 still-live MISMATCH bugs against their CURRENT configured URLs showed 31 sit on pages running a platform the codebase already understands, and 20 of those publish their events as JSON-LD Event objects inside <script type=application/ld+json> across EIGHT different scrapers - while the generic DOM selectors missed all of it. Live extraction test before wiring anything: 19 of 20 sites yielded events, 958 in total, e.g. 107 from Brandywine Zoo, 59 from Brownell Library, 58 from Hickman County. This is MASTER-PLAN Phase 4's tier-1 case, one fix serving many sites, and it is strictly better than DOM scraping where available because startDate is a real ISO timestamp (so it cannot produce the time-only values driving this family's InvalidDate counts) and location.address carries a full PostalAddress (so events geocode to the venue rather than a city or county centroid). The helper walks any JSON-LD shape including @graph, accepts Event subtypes while rejecting Place and EventVenue nodes, dedupes recurring series, decodes double-escaped HTML entities, and reads local wall-clock fields textually rather than through new Date() so an offset-bearing timestamp cannot shift an evening event onto the wrong day. Verified with 11 unit checks plus live runs. WordPress-VT result: found 1156 to 1307, new 548 to 649, and InvalidDate 30 to 1. Two bugs were caught during the work rather than shipped: the wiring script computed the insertion anchor BEFORE adding the require line and then reused that stale offset, which put the block inside a page.goto object literal in the KY file and would have silently misplaced the other 20 - several ahead of page.goto, where page.content() returns an empty document; after fixing it, placement was verified per file (goto < jsonld < evaluate) rather than trusting node -c alone. And a venue arrived as 'Hyde Park Library Children's Room' with a literal backslash, since some sites emit JS-style escapes inside the JSON string; toPlainText now strips a backslash before a quote or backslash, covered by 6 escape unit cases, and the single affected row was deleted."}
{"date":"2026-08-10","scrapers":["Venue-Events-ZoosAquariums","WordPress-Events-Calendar","verification-comments"],"category":"code-bug","summary":"Worked the remaining 67 confirmed bugs by re-probing every one IN A REAL BROWSER rather than over raw HTTP. That distinction was the whole point: the earlier verdicts came from plain fetches, which cannot see a client-rendered calendar, so a site full of events could read as empty and an empty one as broken. Rendering with JavaScript executed re-bucketed all 67. Result: 35 downgraded from MISMATCH to UNVERIFIABLE because a full render of the configured events page surfaced no event cards, no JSON-LD and almost no date-like text - the claim that the live site shows events we are missing is no longer supportable, though it does NOT prove the library has none, since the URL may still be the wrong institution; UNVERIFIABLE (unknown) is the honest verdict rather than MATCHES (fine). 11 were diagnosed as platform mismatches, where the library publishes nothing on its own site and its events live on LibCal (5), BiblioCommons (4), Communico (1) or FullCalendar (1) - structurally unreachable by a WordPress DOM scraper, so no selector work would ever help; these were left configured rather than removed, since only the platform assignment is wrong, and recorded in reports/fix-notes.json for relocation to the right family scraper. 12 sat on pages publishing JSON-LD that the earlier pass had not reached because two files were never wired: extractJsonLdEvents is now wired into scraper-venue-events-zoos-aquariums.js and scraper-wordpress-events-calendar-libraries-VA-PA.js ahead of their selector strategies, unlocking Brandywine Zoo (107 Event objects), Virginia Zoo (46), Wythe-Grayson Regional (94) and Pittsylvania County (71). Confirmed bugs fell 67 to 32. A still-live Defect A collision surfaced incidentally and is worth chasing: pembrokelibrary.org is configured under BOTH WordPress-GA and WordPress-NC, which the host probe missed because that host produced no usable state signal."}
{"date":"2026-08-10","scrapers":["Venue-Events-ZoosAquariums","WordPress-Events-Calendar"],"category":"code-bug","summary":"Field-shape mismatch silently voided the JSON-LD path in both scrapers wired on 2026-08-10. jsonld-events-helper emits date/startTime, which is what saveEventsWithGeocoding consumes and what the 21 WordPress-{state} files use, but these two files carry their own internal shape and read eventDate downstream - the zoos file's junk filter drops any row lacking eventDate, and the events-calendar file normalises event.eventDate and uses it in the dedup key. The result looked like success and was not: the zoos run logged 'JSON-LD: 107 events' for Brandywine Zoo and then reported a grand total of 12 found, because all 107 were filtered out as dateless. Caught on the first live run by noticing the extracted-versus-found mismatch rather than trusting the JSON-LD log line. Both call sites now map explicitly onto the local shape (eventDate, time, name) instead of spreading the helper's output through unchanged, with a comment naming the trap so the next file wired does not repeat it. Worth remembering as a pattern: this codebase's scrapers do NOT share one event shape, so a shared extractor has to be adapted per call site, and a log line proving extraction says nothing about whether the rows survive to the database."}
{"date":"2026-08-10","scrapers":["WordPress-Abbe-Regional"],"category":"site-change","summary":"WordPress-Abbe-Regional reported Found 0 while extraction was actually fine. The Pressville /events/ archive is paginated OLDEST-FIRST, so page 1 is permanently stuck in the past - every event on it was a June 2026 date and all were correctly dropped by the past-event filter, leaving zero. Real August/September events sat on pages 2+. Rewrote the single-page scrape into a pagination walk with the extraction lifted into extractEventsOnPage, deduping by name+date and stopping when a page adds nothing new, which also handles WordPress clamping out-of-range page numbers to the last real page. Also set metadata.scraperName and metadata.sourceUrl, which were absent so the adapter was writing the library display name. Verified live: 0 found becomes 17 found / 1 new / 16 dupes, stopping after 3 page loads."}
{"date":"2026-08-10","scrapers":["MacaroniKid-ALL","Dorchester-County","Pratt-Library","PGParks-MD"],"category":"code-bug","summary":"Reporting fallout from yesterdays .add() duplicate-miscount fix, found by re-running Dorchester-County after it flipped from a healthy 12 found / 12 new to a warning-flagged 0/0/0. The scraper was working perfectly - its own stdout said Found 24 events, Imported 0, Skipped 12 - but it returns { imported, failed } and never returned its local skipped counter. Before the miscount fix duplicates were counted as imports so skipped stayed near zero and dropping it was invisible; now that duplicates route there correctly, the runners dupCount reads undefined and FOUND collapses to imported alone. Audited all 228 scraper files: 45 had this shape, including all 42 MacaroniKid state scrapers, Pratt-Library and PGParks-MD. Added scripts/fix-dropped-skipped-return.js which only rewrites a file when let skipped = 0 and skipped++ are both present and the return pattern occurs exactly once; scraper-macaroni-md.js was correctly refused as it keeps no such counter. This is a reporting fix only - no event data changes. Verified live: Dorchester-County now reports Found 12 / New 0 / Duplicates 12."}
{"date":"2026-08-10","scrapers":["LibraryMarket-CT"],"category":"site-change","summary":"Step 3d verification confirmed New Britain Public Library as a genuine extraction failure rather than a URL collision - right institution, real live events, scraper returned 0. Root cause was the seeded eventsUrl pointing at /events/month, whose grid renders day cells with no event titles; the sibling Ferguson Library entry already used /events/upcoming, which lists them properly. Switched New Britain to the upcoming view. Verified live: New Britain 0 to 24 events, LibraryMarket-CT overall 174 to 198 found."}
{"date":"2026-08-11","scrapers":["WordPress-ALL","dom-date-resolver.js"],"category":"code-bug","summary":"Attacked the WordPress-{state} InvalidDate rates (5-41% depending on state). Root cause matched SCRAPER-DIAGNOSIS-PROMPT section 2 exactly: both extraction variants picked a date element and took its textContent, never reading the datetime attribute, so on themes that render a clock time as the visible text the event was skipped as an invalid date. Measured across the 2026-08-10 Group 1 run the top skipped values were 6:00pm-7:00pm (106), 1:00pm-2:00pm (86), All Day (65), 10:30 AM (56). Added scrapers/helpers/dom-date-resolver.js which prefers datetime/data-date/itemprop attributes, rejects time-only strings rather than returning them, and falls back to a bounded ancestor walk for day-grouped list views; injected into page.evaluate via a source string since it cannot close over Node scope. Wired into 14 of 21 active state files by scripts/fix-wordpress-date-extraction.js, which refuses any file where a replacement target does not occur exactly once - 7 files have drifted and were correctly declined. Verified live on WordPress-NC, the worst rate: Found 346 to 382, InvalidDate 141 to 125, New 113 to 144. PARTIAL - the residue is still bare times on sites where no date exists inside the card at all, plus junk cards like a newsletter signup box and a filter widget being matched as events. That is per-site DOM work."}
{"date":"2026-08-11","scrapers":["Somerset-County"],"category":"code-bug","summary":"Somerset-County started on 2026-08-10 and never logged a completion line; reproducing it showed a runaway loop of Found API call lines and exit 255. The response handler pushed every matching JSON response into apiResponses, and the extraction loop then iterated that same array with for...of while the handler was STILL ATTACHED and still appending - the embedded Google Calendar widgets poll for as long as the page is open, so the loop could never reach the end. Three independent guards: dedupe captures by URL with a 200-entry cap so a polling widget contributes one entry per calendar, detach the request and response listeners before processing, and iterate a snapshot rather than the live array. Also wrapped request.continue in try/catch since interception races with navigation teardown. Verified live: completes in 248.6s with 20 API responses captured and 401 events, where before it ran 16+ minutes and never finished."}
{"date":"2026-08-11","scrapers":["CivicEngage-Libraries","WordPress-TN"],"category":"new-coverage","summary":"CivicEngage-Libraries had exactly one active entry, Colonial Heights, whose calendar is genuinely empty - verified live a third time - so the scraper returned 0 by design rather than by fault. Gave it real coverage using a confirmed MISMATCH from the 2026-08-10 verification pass: Williamson County Public Library was configured in WordPress-TN and returned 0 on every run for a structural reason, wcpltn.org is CivicPlus .aspx and not WordPress at all, so no selector work in that family could ever reach it. Moved it here, changed the registry entry to state Multi since CivicPlus is not VA-specific, and removed the dead WordPress-TN entry. Its DOM theme matches none of this scrapers selectors either - 239 links but monthItem 0, listItem 0, calItem 0 - so added an iCal fallback using the sites own per-category CivicPlus feed, which is structured and theme-independent. Three further bugs found and fixed while proving it: the feeds ev.url is the FEED url identical for every event, which collapsed all 387 events onto one _stableEventId and produced 8 imports plus 346 phantom duplicates, now keyed off the unique VEVENT uid which is the sites own EID; scraperName was CivicEngage-VA which joins to no registry key, now the required per-site CivicEngage-Libraries-wcpltn; and the iCal LOCATION field packs a full postal address into one string, so venue was storing an address and a room suffix that cleanVenueName does not recognise. Verified live: 0 to 354 found and 343 rows written, 343 distinct event urls, 9 clean branch venues with real street addresses that geocode to the branch instead of the county centroid."}
{"date":"2026-08-11","scrapers":["WordPress-ALL","dom-date-resolver.js"],"category":"code-bug","summary":"Cut WordPress-NC InvalidDate from 141 to 26 and raised New from 113 to 181 by teaching the shared resolver to read month-grid calendars. Real attribution was only possible after adding the source site to the skip log: the offenders were wilsoncountypubliclibrary.org (52, reached through TWO config entries pointing at one URL, East Branch and Black Creek, which is also why every skipped event name appeared exactly twice) and harrisburglibrary.org (15). Wilson is a CivicPlus month grid where no element carries a full date - the card selector matches a bare span class=calendar_eventtime holding 9:00 AM and a bare a class=calendar_eventlink holding the title as SEPARATE events, while the real date exists only as the grid cell day number plus the month and year in the table caption. Added fromMonthGrid, which walks to the containing td, takes its day number, and pairs it with the nearest month+year above. Also fixed a false positive in my own hasRealDate from earlier the same day: month-name followed by 1-2 digits matched the 20 of August 2026, so the caption Event dates for August 2026 was being returned as though it were the events date - now requires the day not be followed by another digit."}
{"date":"2026-08-11","scrapers":["WordPress-ALL","WordPress-NC"],"category":"seed-data","summary":"Took WordPress-NC InvalidDate from 141 to 0. Two final causes. First, harrisburglibrary.org is Harrisburg DISTRICT Library of Harrisburg ILLINOIS - verified live by street address and 618 area code - not Harrisburg NC. A MASTER-PLAN Defect A collision, and Illinois is not even in the active region, so a working extraction would have imported Illinois events labelled North Carolina. Entry removed. THIS LEAVES A REAL GAP, not a replacement: Harrisburg NC is served by Cabarrus County Public Library, about 216000 people, which has NO active coverage - scraper-activecalendar-cabarrus-nc.js exists but was never registered, its ActiveCalendar platform is deprecated, and the SirsiDynix site it migrated to sits behind a Cloudflare challenge Puppeteer did not clear. Second, the time-only fallback kept when the resolver was wired in could never recover a date: the resolver already checks every candidate the fallback looks at and returns empty precisely because none held a real date, so the fallback only re-supplied the bare clock time just rejected, turning no-date into invalid-date. It was loudest on cards that were never events - a newsletter signup box, a date-range filter widget, a 3 months ago timestamp. Removed across 14 state files."}
{"date":"2026-08-11","scrapers":["WordPress-ALL"],"category":"seed-data","summary":"Acted on 37 of the 210 collision entries that the 2026-08-09 pass had identified but deliberately left in config. Each was re-confirmed against the live evidence already in verification-comments.json: the configured host serves a library in a different state - oxfordlibrary.org is Oxford WISCONSIN and was claimed by PA NC NJ CT and AL at once, cantonlibrary.org is Canton PENNSYLVANIA claimed by NC MA and CT, libertylibrary.org is not a library at all but a political nonprofit in Conroe TX claimed by NY NC and MS. TIMING MATTERS: these were largely harmless while the WordPress DOM extraction could not read their pages anyway and they returned 0. Todays dom-date-resolver work removed that accidental protection - WordPress-NC went 141 invalid dates to 0 and TN 54 to 0 - so a working extraction against a wrong-state host would have begun importing that other states events under this entrys name and state. Fixing extraction is what made removing these urgent. Deliberately NOT removed: 5 entries whose verdict describes an extraction or JS-rendering problem on the RIGHT site, including Centerville Branch GA whose live calendar genuinely shows Toddler Storytime and LEGO Club that the scraper missed - deleting those would destroy working coverage. All 37 appended to reports/defect-a-removals.md as recorded coverage gaps, restorable once a real URL is verified."}
{"date":"2026-08-11","scrapers":["WordPress-ALL"],"category":"seed-data","summary":"Closed 19 Defect A collisions by finding and verifying each librarys REAL site rather than deleting the entry - the entry now points at the right institution instead of another states library, which fixes the bad data AND restores coverage in one move. Six shared domains resolved: westfieldlibrary.org was claimed by PA NY NJ MA and VT at once and none owned it; plainfieldlibrary.org is Plainfield WISCONSIN, 126 S Main St, 715 area code; oxfordlibrary.org is Oxford WISCONSIN; athenslibrary.org, wilmingtonlibrary.org, hancocklibrary.org and roxburylibrary.org likewise. Every URL was confirmed by loading the page or a directory listing and matching a street address and phone area code to the entrys own city and state - name similarity was never accepted, since that is precisely what the original {city}library.org generator did. Two rows carry a caveat worth reading: Athens TN has no Athens Public Library at all, the real institution is E.G. Fisher Public Library so the configured NAME is wrong too, and New Hanovers Myrtle Grove Branch was replaced by the Pine Valley Branch so that branch name is stale. Notable: the search engine asserted plainfieldlibrary.org belonged to the NJ library, and fetching it proved otherwise - a direct demonstration of why this work verifies rather than trusts."}
{"date":"2026-08-12","scrapers":["WordPress-ALL"],"category":"seed-data","summary":"Applied 116 more verified URL corrections plus 8 removals, taking the Defect A collision backlog from 210 to 29 - 128 entries now point at their real library and 53 are recorded gaps. Every one of the 14 shared hosts checked in the largest batch was owned by NOBODY in the config: fairfieldlibrary.org is Fairfield ILLINOIS, hancocklibrary.org is Hancock WISCONSIN, claytonlibrary.org is Clayton CALIFORNIA, actonlibrary.org is Old Saybrook CONNECTICUT, decaturlibrary.org is Decatur ILLINOIS. The chesterfieldlibrary.org case repeated the exact trap that motivated this method - a web search asserted it was the Massachusetts librarys site and fetching it returned a New Hampshire address and 603 area code, so the MA library was verified separately through the state Board of Library Commissioners directory instead. Also surfaced several stale NAMES, not just URLs: Anthony Pio Costa Memorial Library now operates as Fairfield Free Public Library, Bent Northrup is really Northrop, and Queens Borough Hollis is closed for renovation until Fall 2027 so it can produce no events at all. Process note that mattered more than any single fix: five of six lookup agents were killed mid-run by a budget limit, but because they were instructed to append results incrementally rather than hold them to the end, 118 of 146 lookups survived - two earlier rounds under the same failure lost everything."}
{"date":"2026-08-12","scrapers":["WordPress-ALL"],"category":"seed-data","summary":"Finished 9 more collision lookups inline while the subagent budget was exhausted, leaving 20 of the original 210. Third independent case of a web search asserting the wrong owner for a shared domain: rockportlibrary.org was described as the Maine librarys site and fetching it returned Rockport MASSACHUSETTS, 17 School St, 978 area code - the Maine library is at rockport.lib.me.us. Also a second stale NAME: Westbrook MEs library is really Walker Memorial Library, and westbrooklibrary.net is a MINNESOTA library, so both a wrong name and a near-miss domain in one entry. Green County KY recorded NOT_FOUND - it has a Facebook page and county directory listings but no verifiable standalone site."}
{"date":"2026-08-12","scrapers":["local-scraper-runner.js","puppeteer-config.js"],"category":"other","summary":"Today's 3:00 AM Group 3 run failed 37 of 50 scrapers with an identical Failed to launch the browser process Code 3236495362 error from Puppeteer's bundled Chromium (LibCal-*, Communico-*, BiblioCommons-MA, WordPress-PA/MA/KY/SC/WV/DE/RI/NH, LibraryMarket-PA/NC, and others), while a handful of non-Puppeteer scrapers in the same run (GoogleCalendar-MD, Nashville-Library-TN, VenueList-*-DMV, WordPressTec-Parks) succeeded normally. Manually re-launched the bundled Chromium directly and via the shared launchBrowser helper immediately after the run and both succeeded with no changes, so this was a transient environmental failure during the 3 AM window (likely resource contention, e.g. antivirus scanning newly spawned chrome.exe processes or CPU/handle contention from many concurrent launches), not a code regression - no code was changed. Kicked off a background re-run of the 37 failed scrapers to recover today's data; first attempt used a 180s per-scraper timeout that was far too short for known-slow shared multi-site files like LibCal-NJ (historically ~1300s) and produced false ETIMEDOUT failures, so it was stopped and restarted with a 35-minute per-scraper cap. If this exact failure signature recurs on a future run, check Chrome/Puppeteer launch health first before assuming a code bug."}
{"date":"2026-08-12","scrapers":["WordPress-CT","WordPress-FL","WordPress-MA","WordPress-ME","WordPress-MS","WordPress-NJ","WordPress-NY","WordPress-PA","WordPress-VT"],"category":"seed-data","summary":"Followed up on project-status.js's Defect B (fabricated county values). Discovered county-centroids.js was already expanded to all 3,222 US counties on 2026-08-09, so the static-lookup half of the defect was already fixed - the remaining gap is genuinely wrong per-entry county strings. Enumerated every active-region WordPress-{state} config entry whose county fails getCountyCentroid(): only 21 remained (not the thousands implied by the stale CLAUDE.md note, which predates the 08-09 fix). Looked up the real county for each via live Nominatim geocoding (addressdetails=1) plus manual verification on ambiguous ones (Mountain Top PA resolved to the wrong place on a single-word query; re-verified as Luzerne County using the two-word form and matching zip 18707), corrected all 21, and confirmed zero remain unresolved in active WordPress-{state} files. Also fixed two incidental bugs found while researching: Land Olakes Branch Library (FL) had a placeholder zip 00000 and a typo'd city (\"Land Olakes\" -> \"Land O' Lakes\", real zip 34639); Chebeague Island Library (ME) also had zip 00000 (real: 04017) and a truncated city name. Did not touch scrapers outside the active region or other families (LibCal/Communico/BiblioCommons/MacaroniKid) - those already resolve at 98-100% per project-status.js and were out of scope for this pass."}
{"date":"2026-08-12","scrapers":["local-scraper-runner.js"],"category":"other","summary":"Recovery re-run of the 36 scrapers that failed to launch Chrome in today's 3 AM Group 3 run completed: 36/36 succeeded, 0 failures, roughly 2687 new events saved across the batch (LibCal-NJ 86 new/1135 found in 23min, WordPress-MA 299 new, WordPress-KY 244 new, WordPress-WV 153 new, WordPress-NH 179 new, Assabet-NH-MA 596 new, and 30 others each nonzero or a real zero). Confirms the original diagnosis that the launch failure was purely transient - every one of the 36 scrapers ran clean on a normal retry with no code changes. Total recovery wall-clock was about 3h in the background (18:20 to 21:36 UTC), dominated by inherently slow shared multi-site files (LibCal-NJ ~23min, WordPress-PA ~29min, WordPress-NH ~17min, Farms-Eastern-US ~24min)."}
{"date":"2026-08-12","scrapers":["MacaroniKid-ALL","VenueList-ALL","local-scraper-runner.js"],"category":"other","summary":"Worked the reports/fix-notes.json _pending queue instead of letting it accumulate: 40 entries down to 13, every closure backed by a live check rather than reasoning. Cleared 16 VenueList-* rename entries (check-scraper-names.js now returns zero VenueList drift, so the pre-rename rows have aged out as predicted). Cleared the local-scraper-runner.js MacaroniKid-summary entry (today's log line 350 shows macaroni-runner-group3.js exit code 0 paired with a normal zero-stats row at line 669, not the FAILED line the bug produced). Cleared 11 of the 18 MacaroniKid per-site source_url/scraperName entries after querying the DB per state: NC, MA, TN, AL, KY, RI, DC, WV, NJ, VA and SC all show correct MacaroniKid-XX-<slug> names with 100 percent per-site source_url and zero events.yodel.today URLs. Corrected a wrong read along the way: check-source-url-coverage.js reports a MacaroniKid per-site-names bucket at 0 percent source_url, which looked like the fix being half-broken, but direct row sampling disproved it - that bucket is the unrelated FREE_TEXT display-name population, not the fixed per-site rows. The remaining 7 (FL, NY, GA, CT, MD, NH, ME) are exactly the MacaroniKid GROUP 2 states and have produced no per-site rows at all because that group last ran 2026-08-05 to 08-06, the same dates the fix landed; their notes were rewritten to say so and given a real confirm command. Not a defect, a rotation gap."}
{"date":"2026-08-12","scrapers":["scripts/build-library-site-audit.js"],"category":"code-bug","summary":"Added a --log= flag so the library-site audit can parse a stdout capture other than scrapers/logs/scraper-stdout.log. The per-site '📍 {library}' / 'Found {N} events' lines only reach that file when the runner goes through run-scrapers.bat, which performs the redirection; any hand-invoked run prints them to its own console instead, so the audit found no per-site rows for those scrapers and reported nothing - which in this file is indistinguishable from the sites genuinely returning zero. That silently cost per-site detail for all 36 scrapers recovered earlier today. Verified the flag against a live manually-invoked Group 2 run and it correctly recovered per-site rows including real zero-event sites (Hartford Public Library, Silas Bronson Library, Banks County Public Library). node -c passes; the first version had a duplicated const TS declaration that the syntax check caught before commit."}
{"date":"2026-08-12","scrapers":["scripts/generate-site-report.js"],"category":"code-bug","summary":"loadSites() passed expectedCols=5 to tableRows() because the library audit's canonical header is 'Library Website | State | Scraper | Events Found | Link'. But three of that file's eight sections omit the optional Link column, and tableRows() drops any row with fewer cells than expectedCols - so every row in the 2026-08-10, 2026-08-11 and 2026-08-12 sections was silently discarded, 622 rows for 2026-08-10 alone. Those scrapers ran and reported real counts yet rendered in the report as if they had never been audited, which is exactly the 'a missing row is indistinguishable from a site that does not exist' failure the report's completeness rule exists to prevent, and the completeness check did not catch it because it verifies scraper coverage rather than row counts. Found by noticing the library row count was byte-identical (1655) before and after appending a new section, then confirming the new rows were absent from the HTML rather than assuming the regeneration had worked. Fixed by lowering expectedCols to 4 and letting parseLink() treat an absent Link cell as no-link, which repairs all three existing sections and any future one instead of reformatting the markdown. Library rows went 1655 to 1759 and the completeness check still passes."}
{"date":"2026-08-12","scrapers":["MacaroniKid-Group1","MacaroniKid-Group2","MacaroniKid-Group3"],"category":"code-bug","summary":"CORRECTION to today's earlier incident report, which wrongly said the 3 AM Chrome outage hit 37 scrapers: it also destroyed the entire MacaroniKid Group 3 run, and that went unnoticed. scraper-stderr.log holds exactly 39 'Error scraping <site>: Failed to launch the browser process' lines, matching NJ 17 + VA 12 + SC 8 + DE 2 = 39 sites, and the group log shows NJ processing 17 sites in 5.2 seconds with zero skip lines - impossible for real scraping. Root cause of the miss is a reporting bug in all three macaroni-runner-group{1,2,3}.js files: stats.found was synthesised as (newCount + dupCount + errCount), so per-site errors were counted AS DISCOVERIES. NJ therefore logged 'Found: 15, New: 0, Duplicates: 0' where 15 was purely the error count, and the four states' FOUND values 15+14+9+1 sum to exactly the 39 failures. The group runner also returned success because the child exited 0, so the summary table showed a benign ⚠️ zero row rather than a ❌ FAILED line - and the day's recovery sweep, which keyed off ❌ FAILED lines, skipped the whole family. Fixed in all three runners: errCount is no longer folded into found, a state that errored on every site and saved nothing is now reported as FAILED regardless of exit code, and the completion log line now prints Errors. Verified by simulating four shapes through the real formatSummaryRow: today's wipeout now renders '❌ MacaroniKid-NJ FAILED — all 15 site(s) errored', a healthy run is unchanged, partial errors no longer inflate FOUND, and a genuinely empty state still renders a benign ⚠️ row rather than a false failure. node -c passes on all three. NOTE the underlying DATA LOSS is not yet recovered - those 39 MacaroniKid sites still have no 2026-08-12 data and need a MacaroniKid Group 3 re-run."}
{"date":"2026-08-12","scrapers":["Communico-NJ"],"category":"other","summary":"Ran the Group 2 rotation that was missed on 2026-08-11 (regular scrapers only, --no-macaroni, since a MacaroniKid Group 2 run historically takes ~20h and concurrent heavy Chrome workloads were a suspect in that morning's launch failure). This closed the last open half of the Communico-NJ pending item: Somerset County Library System was reached at its branch-filtered sclsnj.libnet.info URL, its API returned 5 events and all 5 saved (Summer Reading, First Steps Small Group, Chess Club, The History of Handwriting, Make Bracelets Swap Books). Combined with Montclair already confirmed on 2026-08-10, the entry is fully verified and was removed. Incidental note for future verification: the confirm command stored on that entry used .ilike('source_url','%sclsnj%') against the whole events table and now reliably times out server-side; scoping by scraped_at, or reading the run's own per-library output, is the workable check. Also note Communico-NJ stores per-library DISPLAY NAMES in scraper_name, so querying scraper_name='Communico-NJ' returns zero rows and is not evidence of failure."}
{"date":"2026-08-13","scrapers":["CustomDrupal-Libraries","BiblioCommons-NC","supabase-adapter.js"],"category":"other","summary":"Catch-up Group 2 finished 53/53 succeeded, 0 failed, 431.6 min, and closed three pending items with live evidence. CustomDrupal-Libraries: the Cobb County selector-collision fix executed for the first time and Cobb County Public Library System returned 10 events with real titles saved (The Basics Storytime with Cobb Collaborative, North Cobb Library Craft and Chat, Teen summer Gaming Lounge). BiblioCommons-NC, the deliberately chosen canary for the fleet-wide duplicate-miscount fix, hit its predicted signature exactly - 383 found / 64 new / 319 dupes where it used to report 356/356/0 - and the claim was then checked against reality rather than trusted from the summary line: row count went 364 to 418, growth of 54 against a reported NEW of 64, versus roughly 383 had it still been miscounting. That also retires the supabase-adapter.js entry, whose fleet-wide effect had been explicitly unproven because the 2026-08-10 nightly could not pick up adapter changes mid-run. Static checks re-confirmed alongside: audit-add-miscount 96 OK / 0 buggy, verify-duplicate-branch-scope all in scope. reports/fix-notes.json _pending is now 9, down from 40 at the start of this session."}
{"date":"2026-08-13","scrapers":["LibCal-CT","LibCal-PA","LibCal-TN","Berks-County","Rockbridge-Regional","CustomDrupal-Libraries"],"category":"code-bug","summary":"RECORDED, NOT FIXED - active ongoing data loss. The catch-up Group 2 run emitted 6 STABLE-ID COLLISION warnings, each meaning two genuinely different events hashed to one row id so all but one are silently dropped. Attributed each to its scraper and site by walking the run output: LibCal-CT/Hamden Public Library (Time For Twos Storytime vs Garden Club), LibCal-PA/Easton Area Public Library (Card Club vs Lunch at the Library), LibCal-TN/Clarksville-Montgomery County Public Library (North Branch Toddler Craft Time vs North Branch Toddler Time Story Time), Berks-County (Toddler Storytime vs Childrens Storytime), Rockbridge-Regional (Tech Power Hour vs Book Sale Set-up), CustomDrupal-Libraries/Kanawha County Public Library (The Sci-Fi/Fantasy Hour vs Lets talk programming). Root cause in every case is a shared url - a library homepage or calendar page put on every event - which _stableEventId hashes first. These 6 are a FLOOR not a total, since the warning only fires when two differently-named events collide within a single run. Deliberately not fixed here: the correct remedies (extract a real per-event URL, or pin identity to name|eventDate|venue as was done for WithApps-Libraries and RollyPollies-MD) both change _stableEventId for every event the scraper emits, so every existing row would re-insert under a new id and duplicate. That needs a deliberate per-scraper pass with live DOM inspection, not a blind six-file edit. Pinned in reports/fix-notes.json under STABLE-ID-COLLISIONS with the full attribution and a hint that the LibCal trio likely just fails to extract per-event URLs that do exist."}
{"date":"2026-08-13","scrapers":["MacaroniKid-DC","MacaroniKid-VA"],"category":"seed-data","summary":"Closed Defect B / Gate 1 completely: counties resolve went 98.4% to 100% (+1.6). After yesterday's WordPress-{state} pass took that family to 100%, project-status.js correctly kept reporting the WORST family rather than the fleet average, which left MacaroniKid-* at 239/243 as the gate. The 4 stragglers were all county-EQUIVALENTS rather than counties, so they were never fabricated names - just the wrong key form. Virginia independent cities are not inside any county and Census names them with a lowercase 'city' suffix: Lynchburg, Norfolk and Virginia Beach became 'Lynchburg city', 'Norfolk city', 'Virginia Beach city'. Washington DC's county equivalent is 'District of Columbia', not 'Washington'. Verified every replacement key resolves with sane coordinates before editing, then re-measured: 245/245 MacaroniKid counties now resolve, node -c passes on both files. Timed deliberately - MacaroniKid-DC is a GROUP 1 scraper and tonight is a Group 1 rotation night, so the fix lands on the very next run."}
{"date":"2026-08-13","scrapers":["MacaroniKid-ME","macaroni-runner-group2.js","dropped-skipped-return"],"category":"other","summary":"Started clearing the MacaroniKid Group 2 backlog smallest-state-first rather than as one ~20h run, because the nightly rotation was concurrently active and the real contention risk is Nominatim, not Chrome - the geocoding rate limiter is per-process, so a second scraper doubles the request rate against a shared ~1 req/s budget, which has caused documented 429 floods before. Group 2 is 108 sites split very unevenly (FL 31, NY 24, MD 20, GA 17, CT 9, NH 5, ME 2), so the small states buy most of the confirmations for a fraction of the load. MacaroniKid-ME (2 sites, 17.8 min) completed with zero 429s while the nightly was running RecDesk-Parks, its heaviest geocoder. Result: 160 rows, exactly 2 distinct scraper_name values matching its 2 configured sites (MacaroniKid-ME-midcoastme, MacaroniKid-ME-lakesregion), 160/160 carrying a per-site source_url, zero events.yodel.today - pending entry cleared. The same run incidentally confirmed two other things live: the 2026-08-12 macaroni-runner fix works (the completion line now prints Errors, and Found 60 = New 29 + Duplicates 31 with errors correctly excluded rather than counted as finds), and the dropped-skipped-return fix reaches the MacaroniKid files (a NONZERO Duplicates count from a MacaroniKid state was precisely the missing evidence). _pending is now 8, from 40 at the start of this work."}
{"date":"2026-08-13","scrapers":["MacaroniKid-NH"],"category":"other","summary":"MacaroniKid-NH confirmed, second small Group 2 state: Found 604 / New 524 / Duplicates 80 / Errors 0 in 88 min, zero 429s again while the nightly ran concurrently. All 5 configured sites produced per-site rows with 100 percent per-site source_url and zero events.yodel.today. Worth recording a verification trap hit along the way: querying scraper_name = 'MacaroniKid-NH-concord' returned 0 rows and briefly looked like a 4-of-5 failure, but the slug comes from the site hostname, and Concord's URL is concordnh.macaronikid.com, so the real name is MacaroniKid-NH-concordnh, which has 60 rows. Derive the slug from the configured URL, never from the display name, before concluding a site is missing."}
{"date":"2026-08-13","scrapers":["supabase-adapter.js","MacaroniKid-ALL"],"category":"code-bug","summary":"Found while verifying the NH run and far bigger than the run itself: legitimate children's events were being SILENTLY DELETED as adult-only. resolveAgeRange() treated any specific scraper-supplied bracket as authoritative and returned it immediately, so a supplied value normalizing to 'Adults' went straight to the rejection in flattenEvent/saveEvent with no cross-check. 'Adults' is the one value that DELETES a row rather than merely mislabelling it, yet it was the only specific value receiving no second opinion - 'All Ages' already got one. MacaroniKid assigns its page's Who field verbatim (scraper-macaroni-nh.js line 172), so when the unit is lost upstream a toddler range arrives as a bare '18-36', normalizes to Adults, and the event dies even though its own title reads 'Toddler Time (Ages 18-36 months)' - dropped 8 times in this run alone. Confirmed the components were individually fine (detectAgeRange returns '18-36 months', normalizeAgeRange maps that to Babies & Toddlers) and isolated the fault to the precedence rule. Fix: when the supplied value resolves to Adults, re-check the title/description via detectAgeRange and prefer a specific non-adult bracket if one is found; otherwise keep Adults. detectAgeRange is anchored and context-aware so this cannot invent a signal from arbitrary numbers. Scale is fleet-wide, not MacaroniKid-only, and MEASURED rather than estimated - an earlier draft of this entry quoted the raw 1253 adult-only skips in scraper-stdout.log in a way that implied all of them were wrong, which is false. Actual split across the 320 distinct rejected titles, computed by asking whether each title carries a specific non-adult signal: 62 titles / 285 event instances were WRONGLY rejected and are rescued by this fix (Toddler Storytime x78, Toddler Tales x36+11, Toddler Time x21, Toddler Play & Connect x20, Toddler Read & Play x8, It's Toddler Time x7, KIDS Open Crafting x7, Two x Two Storytime x5, Terrific Twos Storytime x5, Trap Baby & Me x4); the other 258 titles / 968 instances are NOT rescued. Do not read that second group as 'correctly rejected' either - it means only 'not rescued by THIS fix'. It demonstrably still contains wrongly-deleted children's content, most obviously '2026-27 KidsPlex After-School (Pay-by-Day Option)' at 82 instances, where the year pair 2026-27 parses as ages 26-27; that is the known registration-ID/year misread class and is NOT addressed here. It does also contain genuinely adult events that should stay rejected (Coffee & Conversation, CUSTOMER TRAINING 3D Printer Orientation, English Language Learning), so the group is mixed and unaudited. Regression suite extended from 36 to 42 cases and all pass: 3 new positive cases plus 3 adult-only CONTROLS asserted directly against resolveAgeRange, because flattenEvent rejects adult rows and structurally cannot observe an 'Adults' outcome. Control titles were chosen to survive the earlier NON-FAMILY filter - Adult Book Club, Wine Tasting 21+, Knitting Circle and Genealogy Workshop all trip that filter and would have proved nothing. Historical rows already deleted are not recoverable without re-running the affected scrapers; future runs will save them."}
{"date":"2026-08-15","scrapers":["WordPress-SC"],"category":"seed-data","summary":"Removed 'Lexington County Library - Gaston' entry (gastonlibrary.org) from scraper-wordpress-libraries-sc.js - verified live that domain is Gaston County Public Library in Gastonia NC, not Lexington County SC. Confirmed no contaminated SC rows existed under that URL (all 12 gastonlibrary.org rows in the DB were already correctly attributed to WordPress-NC). Real Lexington County Public Library (SC) runs LibCal at lexcolibrary.libcal.com and is already configured as a system-wide entry in LibCal-SC, so coverage continues, just with coarser per-branch attribution for Gaston until a branch-specific cid is found. Closes the pending WordPress-SC gastonlibrary.org collision item from 2026-08-06."}
{"date":"2026-08-15","scrapers":["supabase-adapter.js"],"category":"code-bug","summary":"Fixed a saved-count miscount in the Firestore-compat batch.commit() row-by-row fallback (scrapers/helpers/supabase-adapter.js). When a batch upsert hits idx_events_unique_content (name+event_date+venue), it falls back to row-by-row upserts with onConflict:id + ignoreDuplicates:true; when a row instead collides on the CONTENT unique index (a different id, same content) Postgres still raises 23505 since ON CONFLICT (id) only suppresses id-collisions. That 23505 was being silently continue-d with zero bookkeeping, so the caller (event-save-helper.js) never decremented saved for it - every content-duplicate got counted as a fresh save despite writing zero new rows. Found investigating an AGE-RANGE-AUDIT.md anomaly flagged by the age-range audit subagent: WordPressTec-Parks self-reported New:25 today but had zero DB rows with a matching scraped_at. Reproduced live (node scrapers/scraper-wordpress-tec-parks.js --state WV): before the fix, 25/33 falsely reported saved; after the fix, correctly reports 16 saved / 17 skipped (9 of the 33 hit the content-duplicate path). Fix pushes a skip-reason string into skippedReasons for each 23505 row-by-row hit so the existing categorizeCommitSkips()/saved-=skippedCount path (already used for past/dateless/invalid-date skips in the same function) picks it up like any other non-insert. This is a shared-helper fix affecting every scraper that uses the batch/.set() path and can hit this constraint, not just WordPressTec-Parks - awaiting a full rotation to confirm the corrected numbers hold fleet-wide."}
{"date":"2026-08-15","scrapers":["WordPressTec-Parks"],"category":"other","summary":"Deeper issue surfaced while fixing the above miscount, NOT fixed here: the rows WordPressTec-Parks collides with on every run are its OWN historical rows (same scraper_name, e.g. sgl-J1HbWnwhWOOgverWUD1kKvY6sF / Chief Logan State Park Farm to Table), just stored under a different id than what _stableEventId computes today for what looks like identical name+event_date+venue content. This means the id is not actually stable run-to-run for this scraper: every run recomputes a fresh id, collides with the original row on the content-unique index, and the original row never gets its scraped_at refreshed (frozen at whatever day the first id happened to be computed) while the scraper burns its whole run recomputing the same 33 events daily for no benefit. Root cause not yet isolated (candidates: raw event text has subtle formatting drift run-to-run that only sometimes changes the hash seed, or the id-seed fields resolve slightly differently on different runs). Deliberately not touched further this session - changing _stableEventId for this scraper would re-insert every existing row under a new id per the same caution already on record for the STABLE-ID-COLLISIONS finding. Needs its own session with the exact seed strings compared across two runs."}
{"date":"2026-08-16","scrapers":["Prince-Georges-County","Communico-MD"],"category":"site-change","summary":"Prince George's County Memorial Library System (967k people, largest system in MD) had produced ZERO real events - the daily table showed a warning row, Found 0 / New 0, with 12 junk-title skips ('events', 'Calendar') that were nav chrome, not events. Root cause is a platform migration, not selector rot: pgcmls.info now runs Communico (page config emits client:'pgcmls', apiServer:'https://api.communico.co'; footer reads 'Connected content from Communico'), and the bespoke scraper-prince-georges-county-library-MD.js still targeted the pre-migration custom event grid. Fixed by adding the library to the MD block of scraper-communico-libraries-CA-CO-DC-FL-GA-IL-MA-MD-TX-VA.js rather than repairing the dead file. Its /eeventcaldata endpoint returns [] so the API-first path yields nothing and it correctly falls through to the shared Puppeteer path, which finds the standard .eelistevent markup. VERIFIED LIVE BEFORE REMOVING ANYTHING, per the Worcester rule: a real Communico-MD run saved 185 events across 26 distinct branch venues (Oxon Hill, Mount Rainier, Laurel, Surratts-Clinton, Beltsville, Accokeek, Glenarden, South Bowie, Spauldings, Upper Marlboro and others), 100 percent carrying source_url, all state=MD, branch addresses resolved through library-addresses.js. Age spread is healthy rather than All-Ages-dominated: 85 All Ages, 41 Preschool, 32 Kids, 14 Teens, 12 Babies and Toddlers, 1 Tween. Only after that proof was the dead scraper fully removed - registry key and file both deleted, not commented out. Confirmed first that zero DB rows carried scraper_name 'Prince-Georges-County', so the removal breaks no existing join and leaves the registry state-hint lookup in fix-event-state.js unaffected."}
{"date":"2026-08-16","scrapers":["Communico-ALL"],"category":"code-bug","summary":"The shared Communico scraper never set metadata.scraperName on any event, so flattenEvent() fell back to metadata.sourceName and wrote each library's DISPLAY NAME into scraper_name - 'Baltimore County Public Library', 'DC Public Library', \"Prince George's County Memorial Library System\". Those join to no registry key; this is the FREE_TEXT drift class in check-scraper-names.js (221 names / 13883 rows fleet-wide) and exactly what CLAUDE.md's Scraper Naming section forbids. Found while confirming the PGCMLS fix above, whose 185 new rows all landed under the display name. Added buildScraperName(library) emitting the required <registryKey>-<siteSlug> form; registry keys in this family are exactly Communico-<ST> so the key derives from library.state, and the slug derives from the listing URL hostname, never the display name. Two subtleties, both caught by an explicit collision check across all 96 configured libraries rather than by inspection: taking the first hostname label blindly slugged events.bcpl.info to 'events', which would collapse every library on an events. subdomain onto one name (the same failure mode as the WordPress-MD '-www' collapse); and after adding www/events/calendar/programs/catalog/go to the generic-label strip list a REAL collision still remained, with Hillsborough County and Alachua County both slugging to 'attend' from attend.hcplc.org and attend.aclib.us. With 'attend' added the check reports 96 configured libraries, 96 distinct names, 0 collisions, 0 falling back to a bare key. MECHANISM PROVEN rather than merely reasoned: flattenEvent() returns scraper_name 'Communico-MD-pgcmls' when metadata.scraperName is present and the old display name when it is absent. NOT yet observable on a live DB row - Communico-DC and Communico-NC were both re-run to try to confirm and both returned 0 new because everything was already deduped by the morning rotation, and this scraper skips duplicates without rewriting them, so existing rows keep their display name until they expire. The first inserting run will show the conformant names."}
{"date":"2026-08-16","scrapers":["build-library-site-audit.js"],"category":"code-bug","summary":"Venue-Events-ScienceArts logs its per-site header as a pin emoji followed by 'Scraping: {venue} (ST)', rather than the bare 'pin {venue} (...)' shape every other family uses, and the SITE_PIN regex captured the verb too - storing 38 site names as 'Scraping: Adler Planetarium' and similar. Site name IS the join key for this audit: it is what matches a row to the scraper's config array and to a prior verdict in reports/verification-comments.json, which is keyed '<scraper>|||<site>'. A contaminated name therefore joins to nothing and the site reads as unverifiable forever. Fixed by stripping a leading 'Scraping:' from the captured name. Confirmed safe before changing: zero existing verdict keys carry the prefix, so nothing orphans. Rebuilt after the fix - 586 per-site rows, none still carrying the prefix."}
{"date":"2026-08-16","scrapers":["WordPress-GA","WordPress-NC","WordPress-NY"],"category":"seed-data","summary":"Removed fayettevillelibrary.org from three scraper files. The domain is HIJACKED: it now serves an Indonesian gambling site (page title 'OPUNG4D : Link Daftar Situs Toto 4D...', assets loaded from garisdua.click and touchwork.pics), verified live during the Step 3d zero-event verification pass. It was configured simultaneously in GA (Fayette County Public Library), NC (Bordeaux Branch Library) and NY (Fayetteville Free Library) - the usual {city}library.org seed-data collision, where one guessed domain is assigned to same-named cities in several states and can be the real site of at most one. Removed rather than repointed in all three, so the fleet stops fetching a gambling domain; each is recorded in-file as an OPEN COVERAGE GAP rather than described as resolved. Leads captured for two: Bordeaux Branch really belongs to Cumberland County Public Library (cumberlandcountync.gov/library), which is not WordPress and therefore a relocation rather than a URL swap; Fayetteville Free Library NY is real and its own canonical domain fflib.org 301s to onlib-fayetteville.libcal.com/calendar?cid=19949, Onondaga County's LibCal, matching the entry's configured county - so it belongs in LibCal-NY. That move was deliberately NOT made because the LibCal calendar is JS-rendered and could not be confirmed to hold live events; relocating it unverified would only move a zero from one scraper to another. No DB cleanup was needed: the hijacked domain produced 0 events, which is why it surfaced in the zero-event population in the first place."}
{"date":"2026-08-18","scrapers":["WordPress-PA"],"category":"seed-data","summary":"Albright Memorial Library (Scranton PA) was configured with url scrantonlibrary.org, verified live to actually be E.C. Scranton Memorial Library in Madison, CT (confirmed via WebFetch: page title and address both read Madison CT) -- a name collision on 'Scranton' as a surname vs. the PA city, same class as the {city}library.org guesses documented elsewhere in this project. Found by today's Step 3d Puppeteer verifier flagging it MISMATCH ('url-collision: page addresses show CT not PA'). The real Lackawanna County Library System site (lclshome.org) was located and confirmed live: lclshome.org/event/ lists real dated August 2026 events explicitly at Albright Memorial Library (Bookworms Book Club, Messy Science, Teen Take-and-Make). Updated url/eventsUrl to lclshome.org / lclshome.org/event/. Not yet re-run live -- VERIFIED THE TARGET, NOT YET THE SCRAPE."}
{"date":"2026-08-18","scrapers":["WordPress-DE","LibCal-DE"],"category":"site-change","summary":"Today's Step 3d verifier flagged 4 WordPress-DE New Castle County libraries (Bear, Claymont, Brandywine Hundred, Appoquinimink) as MISMATCH: dead-endpoint, their nccde.org/{branch}/events pages 404. Root cause confirmed live: New Castle County rebranded its site to newcastlede.gov and dropped native per-branch WordPress event pages entirely -- the new branch pages (e.g. newcastlede.gov/bear) exist and link out to the shared delawarelibraries.libcal.com LibCal system instead, confirmed for all 4 branches (each has a real, correctly-titled LibCal calendar page: 'Bear Library - LibCal - Delaware Libraries' etc.). This is the exact same defect already present in Bridgeville Public Library, which the 2026-08-11 fix pointed at its LibCal URL but left registered under WordPress-DE, so its WordPress-flavored Puppeteer extractor could never read LibCal markup -- confirmed today by the verifier ('platform=libcal detected -- belongs in a LibCal-* scraper'). Relocated all 5 (Bear, Claymont, Brandywine Hundred, Appoquinimink, Bridgeville) from scraper-wordpress-libraries-de.js to the DELAWARE section of the shared LibCal file (scraper-libcal-libraries-CA-CO-DE-FL-LA-MA-NY-SC-TN-TX-VA-WA.js), each with its own delawarelibraries.libcal.com/calendar/{slug} URL (all 4 new slugs confirmed live via WebFetch before adding). node -c passed on both files. NOT YET RE-RUN LIVE. Separately noted but NOT fixed today (blast radius too large for a daily diagnosis): this shared LibCal file never sets metadata.scraperName, so flattenEvent() falls back to metadata.sourceName and every LibCal-* row across all 12 states in this file writes the library's own display name as scraper_name (e.g. the existing 'Delaware Libraries' rows already found during this investigation) -- the same FREE_TEXT drift class fixed for Communico on 2026-08-16, just not yet applied here. Worth its own scoped fix."}
{"date":"2026-08-18","scrapers":["LibCal-ALL"],"category":"code-bug","summary":"The shared LibCal scraper (scraper-libcal-libraries-CA-CO-DE-FL-LA-MA-NY-SC-TN-TX-VA-WA.js, 132 libraries across 28 states despite the filename listing 12) never set metadata.scraperName, so flattenEvent() fell back to metadata.sourceName and wrote each library's DISPLAY NAME into scraper_name - 'Bear Library', 'Delaware Libraries', 'BCCLS - Bergen County Cooperative Library System'. Those join to no registry key; this is the FREE_TEXT drift class in check-scraper-names.js and exactly what CLAUDE.md's Scraper Naming section forbids. Found while relocating 5 Delaware libraries into this file earlier today, and then made concrete by that relocation's own live run, which inserted 177 fresh rows under display names (Bear Library 65, Brandywine Hundred 36, Appoquinimink 31, Claymont 29, Bridgeville 16). Added buildScraperName(library) emitting the required <registryKey>-<siteSlug> form; registry keys in this family are exactly LibCal-<ST> so the key derives from library.state. ONE CASE COMMUNICO DID NOT HAVE: a host-only slug is insufficient here because several LibCal systems put every branch on ONE shared host and identify the branch in the PATH - all 6 Delaware libraries sit on delawarelibraries.libcal.com, and bccls.libcal.com and prlib.libcal.com each carry two. A host-only rule collapsed those three groups onto one name each, which is the aggregation AGE-RANGE-AUDIT.md's 'No aggregation, ever' rule forbids; caught by running an explicit collision check across all 132 configured libraries rather than by inspection. Fixed by appending a non-generic trailing path segment when present, applied unconditionally rather than only on collision so a name never depends on array order or on which siblings happen to be configured. Verified across all 132: 132 distinct names, 0 collisions, 0 falling back to a bare key, 0 failing the legal-form regex. MECHANISM PROVEN rather than merely reasoned: flattenEvent() returns 'LibCal-DE-delawarelibraries-bear' when metadata.scraperName is present and 'Bear Library' when it is absent, and _stableEventId is byte-identical either way, so the rename cannot create duplicates. NOT yet observable on a live DB row - this scraper's dedup pre-check queries (name, eventDate, metadata.sourceName) and SKIPS matches without rewriting them, so today's 177 rows keep their display name until they expire; the first inserting run on the next Group 1 rotation will show the conformant names. Dedup itself is unaffected because that pre-check reads sourceName, which is untouched."}
{"date":"2026-08-18","scrapers":["LibCal-DE","WordPress-PA"],"category":"other","summary":"LIVE VERIFICATION of the two fixes made earlier today - both were logged as reasoned-but-unrun and are now PROVEN. LibCal-DE re-run: Found 226 / New 186 / 0 invalid, '6 sites tracked' (was 20 found / 9 new / 1 site). All 5 relocated libraries produced real DB rows - Bear Library 65, Brandywine Hundred 36, Appoquinimink 31, Claymont 29, Bridgeville 16 = 177 of the 186 new rows; the remaining 9 are the pre-existing system-wide 'Delaware Libraries' entry. WordPress-PA re-run: Found 1012 / New 297 / 8 invalid (was 404 found / 149 new). Albright Memorial Library went from 0 rows to 48 (37 under the bare venue name plus 11 across its named rooms - Henkelman Room 6, Technology Lab 3, Director's Office 2), every one on an lclshome.org URL, confirming it is now reading the real Lackawanna County system instead of E.C. Scranton Memorial Library in Madison CT. NOTE, not a defect but worth knowing: lclshome.org is the county-WIDE calendar, so this single config entry now also ingests sibling libraries (Abington 29, Valley Community 24, North Pocono 18, Lackawanna County Children's 14, Taylor 12, Carbondale 9, Nancy Kay Holmes 3). Two of those - Taylor and Carbondale - already have their own separate entries in this same file, so cross-source duplication was explicitly checked before calling this clean: 28 rows exist across those two venues and ZERO share a name+event_date, so nothing duplicated. Latent risk recorded rather than assumed away: _stableEventId prefers the URL hash, so if those siblings ever publish the same event on both their own domain and lclshome.org the two copies would hash differently and duplicate. Worth a watch, not a fix today."}
{"date":"2026-08-18","scrapers":["WordPress-KY","BiblioCommons-KY"],"category":"site-change","summary":"Platform mismatch #1 of 3. Kenton County Public Library was configured in WordPress-KY against kentonlibrary.org/events, which 301s to kentonlibrary.bibliocommons.com - a WordPress DOM extractor pointed at a BiblioCommons site, so it returned near-nothing and carried a >=70% All-Ages flag in the age audit. Note the 2026-08-10 pinned note listed this library under LibCal; that was WRONG and is now corrected - the redirect target is BiblioCommons, verified live. THE CONFIG ALREADY EXISTED: Kenton was already present in scraper-bibliocommons-libraries-*.js with the correct live URL (kentonlibrary.bibliocommons.com/v2/events, 1406 events), but had NO wrapper function and NO registry key, so it had never run once - an orphaned entry, same class as the orphaned LibCal-FL/VA sibling files. Added scrapeBiblioCommonsKY + export + registry key BiblioCommons-KY (group 1, the lightest active group at 49 vs 53/50) and removed the library from WordPress-KY so it is not scraped twice. PROVEN LIVE: 412 found / 412 new / 0 invalid, across 5 real branches (Erlanger 161, Covington 124, Independence 96, Latonia 22, system 9) where WordPress-KY had produced ~24 rows on one generic venue. The All-Ages flag CLEARED as a direct result: 71% before, 49% after, with a genuine bracket spread (Babies & Toddlers 98, Kids 42, Preschool 35, Teens 31, Tweens 4) - the age signal was always there, the wrong extractor just could not reach it. WHILE AUDITING THIS FILE a second orphan was found and is NOT fixed: the CONNECTICUT entry (Hartford Public Library) is likewise configured with no wrapper and no registry key, AND its configured URL hartfordlibrary.bibliocommons.com/v2/events now 404s. Hartford has migrated to Communico (hplct.libnet.info / programs.hplct.org, confirmed live from its own site footer). Recorded as a lead, not actioned."}
{"date":"2026-08-18","scrapers":["WordPress-MA","WordPress-SC","GoogleCalendar-MD","GoogleCalendar-MA","GoogleCalendar-SC"],"category":"site-change","summary":"Platform mismatches #2 and #3 of 3, both the cross-origin Google Calendar iframe pattern that GoogleCalendar-MD already solves by reading the public ICS feed. Ashby Free Public Library (was WordPress-MA) and Berkeley County Library - Sangaree (was WordPress-SC) both carry no event markup at all; the programme lives entirely in an iframe no DOM scraper can read. Calendar ids extracted from the raw iframe src (Ashby's was base64-encoded in the src param and had to be decoded) and BOTH ICS feeds were fetched and verified before any wiring: Ashby 236 VEVENTs, Sangaree 40. Generalised the scraper rather than duplicating it: renamed scraper-gcal-libraries-md.js -> scraper-gcal-libraries.js via git mv (it is no longer MD-only, and a stale state-suffixed filename on a multi-state file is the exact smell that already bit the LibCal file, whose name lists 12 states while covering 28), replaced the hardcoded SCRAPER_NAME const with scraperNameFor(state), added a stateFilter parameter and per-state cloud-function wrappers, and registered GoogleCalendar-MA and GoogleCalendar-SC in group 3 to match each state's existing WordPress rotation day. Left the bare registry key as scraper_name deliberately: each state configures exactly ONE library, which is CLAUDE.md's 'one site -> exactly the registry key' form; an in-file comment records that a second library in any state must switch that state to <key>-<siteSlug> or the two collapse. MA PROVEN LIVE: 44 found / 38 saved, all under venue 'Ashby Free Public Library', source_url correctly the listing page rather than the .ics endpoint. SC IS **NOT** FIXED AND MUST NOT BE REPORTED AS SUCH: the platform diagnosis was right but the calendar is ABANDONED - 40 VEVENTs whose most recent DTSTART is 2024-04-13, so the scraper correctly returns 0 upcoming. Kept configured (costs ~0.5s, self-heals if they resume) and documented in-file so the daily diagnosis does not re-investigate it. Explicitly did NOT claim Sangaree is covered elsewhere: Berkeley County Library System IS configured in LibCal-SC with a cid=-1 all-branches calendar and Sangaree does appear in that system's location filter, but that is not proof - that entry yields only 9 stored rows under generic venue names (Activity Room, Mobile Library), none of them Sangaree, and the only Sangaree rows in the DB come incidentally from MacaroniKid-SC-northcharleston. OPEN COVERAGE GAP; the real question is why LibCal-SC under-collects Berkeley's branches."}
{"date":"2026-08-18","scrapers":["BiblioCommons-ALL"],"category":"code-bug","summary":"The shared BiblioCommons scraper never set metadata.sourceUrl in either of its two event-construction paths, so EVERY row this family has ever written carries a NULL source_url - confirmed on all 412 fresh Kenton County rows. source_url is the only field recording where an event came from, and scripts/verify-coverage.js establishes site identity from its host, so a NULL makes those rows unattributable and is a direct drag on the provenance gate (62%, target 90%). Found while wiring BiblioCommons-KY. Added sourceUrl: library.url (the library's own listing page, never the individual event URL, per CLAUDE.md) to both metadata blocks - the count was asserted at exactly 2 before editing rather than assumed. MECHANISM PROVEN: flattenEvent() returns the listing URL when metadata.sourceUrl is present and undefined when absent, with a byte-identical _stableEventId either way. NOT retroactive: a confirmation re-run of BiblioCommons-KY reported 412 found / 0 new / 412 duplicates and source_url stayed NULL on all 412, because this family skips duplicates without rewriting them. Existing rows keep NULL until they expire; the first inserting run fills it."}
{"date":"2026-08-18","scrapers":["project-status.js"],"category":"code-bug","summary":"loadConfiguredSites() double-counted every shared config file. fileCache cached the PARSE but not the INSERTION, so each active registry key pointing at a file pushed that file's entire config array into `sites` again: the shared LibCal file is registered under 18 active keys and contributed its 132 entries 18 times, Communico 16x, BiblioCommons 6x. Gates 1 and 2 were therefore inflated by that multiple for exactly those families - and gate 2 is the headline RED item whose value picks the reported NEXT ACTION, so the number driving project priorities was wrong. Found only because adding two GoogleCalendar registry keys moved gate 2 by +2 while adding no new configured site: the single Sangaree entry had gone from being counted once to three times. Rather than accept a 2-point wobble, the delta was chased down by extracting the pre-change tree with git archive, running the real script against both, and multiset-diffing the colliding-entry lists, which isolated it to exactly that one entry going 1 -> 3. Fixed by pushing each file's entries once via a pushedFiles set. Safe for gate 1's per-family grouping because every key sharing a file has the same family prefix (LibCal-*, Communico-*, BiblioCommons-*, GoogleCalendar-*) and that gate groups on scraper.split('-')[0]. Gate 2 reads 504 after the fix versus 536 before. THAT DROP IS A MEASUREMENT CORRECTION, NOT PROGRESS - no collision was resolved by it, and a warning note now renders in STATUS.md's data notes plus a code comment at the gate, so a later reader does not mistake the discontinuity for improvement or compare across it as a like-for-like trend."}
{"date":"2026-08-18","scrapers":["Communico-CT","BiblioCommons-ALL"],"category":"site-change","summary":"Hartford Public Library was DOUBLY broken and invisible: it existed only as an entry in the BiblioCommons config with no wrapper function and no registry key, so it had never run a single time, AND its configured URL hartfordlibrary.bibliocommons.com/v2/events returns HTTP 404 because the library migrated off BiblioCommons to Communico. Found during the BiblioCommons-KY wiring, when an audit of that file for the same orphan pattern turned up CT as the only other configured-but-unwired state in an active region. Verified live before moving anything, per the Worcester rule: Hartford's Communico /eeventcaldata API returns 25 upcoming events across six real branches (Downtown, Albany, Camp Field, Dwight, Barbour, Park Street @ The Lyric), nine of them age-targeted; hplct.libnet.info and programs.hplct.org serve byte-identical payloads, and the public programs.hplct.org host was chosen since that is the page a person actually opens. Created the CONNECTICUT section in the Communico config, added scrapeCommunicoCT + export, and registered Communico-CT in group 1 (lightest active group, 50 vs 53/52, and consistent with the rest of the family). Deleted the dead BiblioCommons CT entry outright rather than leaving it, so nothing keeps fetching a 404. PROVEN LIVE: 26 found / 23 new / 0 invalid across all 6 branches, healthy age spread (All Ages 11, Teens 6, Kids 5, Preschool 1 = 48% All Ages, well under the 70% flag), source_url populated on every row with the listing URL. BONUS - this run is the FIRST INSERTING Communico run since the 2026-08-16 naming fix, which until now was only mechanism-proven and never observed on a live row: all 23 rows carry the conformant per-site name Communico-CT-hplct, with ZERO under the display name 'Hartford Public Library' and ZERO collapsed onto the bare key. That fix is now confirmed live, not merely reasoned. Re-audited the BiblioCommons file afterwards: 0 orphaned states remain in active regions."}
{"date":"2026-08-19","scrapers":["LibraryMarket","Cecil-County","Dorchester-County","Somerset-County","Berks-County","Westmoreland-Library","Rockbridge-Regional","Tockify-Horry","Venue-Events-ScienceArts","local-scraper-runner.js"],"category":"code-bug","summary":"Nine active registry entries declared an exportName their scraper file does not export. They kept working only because local-scraper-runner.js has a fallback that guesses an export (first key whose name contains 'scrape') and SILENTLY rewrites config.exportName, so the drift was invisible for months and surfaced only via an unrelated audit. THE FIX WAS DELIBERATELY BEHAVIOUR-PRESERVING: rather than pointing each entry at what looked most correct, each was set to exactly what the fallback already resolves to, so the registry becomes honest while nothing changes at runtime. Two entries needed real investigation before that rule could be applied safely. Venue-Events-ScienceArts exports both scrapeVenueEvents and scrapeScienceArtsEventsCloudFunction; the CloudFunction merely calls scrapeVenueEvents({state:null,maxDays:60}), which is byte-identical to calling scrapeVenueEvents() with no args, so either is safe and the running one was kept. Tockify-Horry was the trap: its CloudFunction wrapper additionally calls logScraperResult(), so 'restoring' it looked like recovering lost telemetry - but the runner ALREADY logs every scraper itself as Local-<registryKey> (confirmed in the DB: scraper_logs holds Local-Tockify-Horry rows going back to April), so switching would have introduced a SECOND, differently-named log row per run. Pointed at scrapeTockifyLibrary instead, preserving current behaviour. A uniqueness assertion also caught that scrapeLibraryMarketEvents appears twice in the registry - the second is a commented-out LibraryMarket-MD entry, which was correctly left untouched. Verified by re-running Dorchester-County (7 found), Berks-County (62 found), Westmoreland-Library (22 found): all clean, counts consistent with history, no drift warnings. Active-scraper audit now reports 156 active / 0 mismatched, down from 9. SEPARATELY, to stop this class hiding again: the runner now emits a REGISTRY DRIFT warning naming the scraper, the declared export and the substituted one, instead of self-healing in silence. The fallback is intentionally KEPT so a stale registry never hard-fails a nightly run - the goal is visibility, not brittleness. The warning was proven to fire by deliberately breaking one entry and restoring it, rather than shipped untested."}
{"date":"2026-08-19","scrapers":["local-scraper-runner.js","MacaroniKid-ALL"],"category":"code-bug","summary":"Rotation groups were selected purely from day-of-month by getDayGroup(), which is stateless, so a group whose run never started was never made up. Combined with Group 1 runs measuring 29.6-31.8h (08-07/10/13/16) and the FunHive-Scrapers task being registered MultipleInstances=IgnoreNew, every long Group 1 run silently discarded the following day Group 2 trigger - 2026-08-14 and 2026-08-17 have no scraper-run log at all. MacaroniKid sits at the tail of each group so it was the first casualty: MacaroniKid Group 2 had not completed since 2026-08-05/06 (14 days) and MacaroniKid-FL/NY/GA/CT have ZERO rows in the database, ever. Added scrapers/helpers/group-catchup.js: records a group completion only after the MacaroniKid tail finishes, and prefers any group starved >4 days over the calendar pick. Verified by counterfactual - 2026-08-15 would have run the starved Group 2 instead of Group 3."}
{"date":"2026-08-19","scrapers":["MacaroniKid-ALL"],"category":"code-bug","summary":"Tier 2+3 pacing tune across 34 of 43 MacaroniKid state files. MacaroniKid is 65-74% of every rotation group runtime at 9-40 min/site. Tier 2 cut the two per-event sleeps (500ms post-goto settle, redundant because waitForSelector(body) already ran, -> 150ms; 1000ms post-save politeness -> 250ms), saving ~1.1s per event. Tier 3 switched the calendar-page goto from networkidle2 to domcontentloaded, keeping each file own timeout, because the explicit waitForSelector calls after it are the real readiness gate. The 2000ms browser-init and 3000ms shutdown sleeps are lifecycle guards and were deliberately left alone; the transform asserts they survive. Applied via scripts/tune-macaroni-pacing.js (dry-run default, MK-PACING tagged, idempotent). Active Group 1 files EXCLUDED because a Group 1 rotation was live and the group runners require() state files lazily mid-loop. ak/ar were auto-declined for Tier 3 by a precondition: they gate on a blind setTimeout(2000) instead of a selector wait. Measured estimate: ~5.0h saved of 50.4h (10%), concentrated in high-throughput states (FL 108 min, NY 44, GA 39); slow states (MD 43.6 s/event, KY 48) are dominated by per-site timeouts rather than pacing. Verified by scripts/test-macaroni-pacing.js: 373 checks, plus a 7-case negative-control battery confirming each check fires."}
{"date":"2026-08-20","scrapers":["BiblioCommons-ALL","Communico-ALL","Drupal-Pennsylvania"],"category":"code-bug","summary":"BiblioCommons-*, Communico-* and Drupal-Pennsylvania have never contributed a single row to LIBRARY-SITE-AUDIT.md or the site report, and nothing errored. build-library-site-audit.js pairs the per-library header (the 'pin' line) with the NEXT 'Found {N} events' line; in both shared family files that line existed ONLY on the Puppeteer fallback branch, while the API branch that actually runs logged '✓ Using API data (N events)' and no count, and scraper-drupal-libraries-PA.js emitted no such line anywhere. The header parsed, went unpaired, and every site was dropped silently — which is why the 2026-08-19 audit shows 25 scrapers while BiblioCommons-KY/NJ/VA, Communico-DC/VA/CT/NC/MA and Drupal-Pennsylvania all ran successfully that same morning. Added the count line to the API path in both shared files and a summed foundOnPage counter in Drupal-PA (emitted unconditionally, including 0, so a genuinely empty library still gets a row instead of vanishing). Proven live rather than by reasoning: a Communico-MA re-run logged 'Found 5 events' and the builder emitted the Worcester Public Library row where it previously emitted none."}
{"date":"2026-08-20","scrapers":["test-age-detection.js"],"category":"code-bug","summary":"The age-detection regression suite — the guard rail CLAUDE.md requires after every detectAgeRange()/resolveAgeRange()/normalizeAgeRange() change — could not run at all. Its fixtures hardcoded eventDate 'August 15, 2026', flattenEvent() rejects past events by design, and that date passed on 2026-08-15, so every invocation died with 'Skipping past event' before asserting anything. The safety net had been down for five days with no signal. Replaced the literal with a FIXTURE_DATE derived as today+30 days so it cannot expire again. Suite restored: 42/42 passing on first run."}
{"date":"2026-08-20","scrapers":["event-save-helper.js"],"category":"code-bug","summary":"detectAgeRange() missed the parenthesised 't(w)een' spelling: the rule was /\btween/, so Springfield City Library Mason Square Branch's 'Crafternoon for T(w)eens' named its own bracket in the title and still landed in All Ages. Found via the 2026-08-20 all-ages verification, which recorded it as a confirmed MISMATCH. Widened to /\bt(?w)?een/, keeping the 'w' mandatory so a plain 'teen' cannot be captured here and still falls through to the teen rule. Added three regression cases including two negative controls ('Teen Movie Night' must stay Teens, 'Juneteenth Block Party' must stay All Ages); suite now 45/45."}
{"date":"2026-08-20","scrapers":["event-save-helper.js"],"category":"code-bug","summary":"Parks-and-rec feeds publish their town's meeting agenda on the same feed as their programming, so municipal governance items were being stored as family events: South Kingstown Parks and Recreation alone contributed Town Council, Planning Board - Regular Session, Municipal Court, Housing Court, Probate Court, Zoning Board of Review and four advisory commissions. Added a GOVERNANCE list to isJunkTitle(). The load-bearing part is the audience rescue guarding it — 'Teen Advisory Board' and 'Tween Advisory Board' are extremely common REAL library programmes, and a bare advisory-board rule deletes them; a measured scan of 12,000 rows hit 26 governance-shaped titles of which six were exactly those. Added scripts/test-junk-titles.js (36 cases, half of them must-survive) because isJunkTitle() DELETES and a rule one word too broad removes real programming leaving no trace. Prevents new rows only; the existing ~26 will be removed by the nightly fix-all junk step, which always full-scans."}
{"date":"2026-08-20","scrapers":["local-scraper-runner.js","MacaroniKid-ALL"],"category":"code-bug","summary":"scrapers/logs/group-last-run.json was three days stale for Group 1 — it claimed 2026-08-17 after a run that finished 2026-08-20T15:08Z — because recordGroupCompletion() only fires at the tail of a run and the 2026-08-19 run was already in flight when the catch-up helper shipped. Stale-old is the dangerous direction: STARVATION_DAYS is 4, so Group 1 would have become spuriously eligible for catch-up the next day and could have displaced Group 2, which is the group genuinely starving (last completed 2026-08-13 with 1 of 7 states; last FULL completion 2026-08-05/06, fifteen days). Added scripts/seed-group-last-run.js, which re-derives the state from the group-completion lines the runner itself writes into scraper-summary.log — the same event recordGroupCompletion() is meant to capture — and only ever moves a timestamp to the observed value, never over a newer one. After seeding, Group 2 reads 7.2 days starved and the next scheduled run should choose it over the calendar."}
{"date":"2026-08-20","scrapers":["MacaroniKid-PA","MacaroniKid-NC","MacaroniKid-MA","MacaroniKid-TN","MacaroniKid-AL","MacaroniKid-KY","MacaroniKid-RI","MacaroniKid-DC","MacaroniKid-WV"],"category":"other","summary":"Finished the tier 2+3 pacing tune started 2026-08-19. The nine Group 1 files were deliberately skipped that day because a Group 1 rotation was mid-flight; that run completed 2026-08-20T15:08Z, so tune-macaroni-pacing.js --save --include-group1 was safe to run and applied all three transforms to all nine. All 43 MacaroniKid files are now tuned; test-macaroni-pacing.js passes 427 checks. Effect remains unobserved — the baseline to beat is recorded in the reports/fix-notes.json _pending entry."}
{"date":"2026-08-21","scrapers":["date-normalization-helper.js","WordPress-NJ"],"category":"code-bug","summary":"normalizeDateString() never accepted hyphen-separated US dates. It handled MM/DD/YYYY (Pattern 5), M/D/YY (5b), M.D.YY (5c) and YYYY-MM-DD (Pattern 1), but MM-DD-YYYY fell through every branch and returned null, and event-save-helper.js drops any event whose date does not normalize. Mountainside Free Public Library publishes EVERY event that way, so all of them were lost on every run. Found by classifying the invalid-date strings in scraper-stdout.log rather than reading the summary counts: WordPress-NJ reported 277 InvalidDate, and while most were time-only strings (a separate known selector class), Mountainside's were fully-formed real dates. ORDER PROVEN, NOT ASSUMED: fetched mountainsidelibrary.org live, whose listing runs 08-25-2026 through 09-16-2026 in ascending order across the month boundary and includes 08-28-2026, whose second field of 28 rules out DD-MM. Added Pattern 5d anchored at start with the first field capped at two digits so it can never shadow ISO YYYY-MM-DD, and a first field >12 is REJECTED rather than swapped to DD-MM — a skipped event is recoverable, an invented date is not. Verified end-to-end through the real flattenEvent() path, not just the normalizer: 09-03-2026 + 6:30 pm yields date 2026-09-03 with start_time 6:30 PM. 16/16 unit cases including negative controls that ISO, slash, dot and text formats are unchanged and that 25-12-2026 and time-only strings stay rejected; all four repo suites still green. NEW ROWS ONLY - the skipped events were never inserted, so there is nothing to backfill; they return when the site is next scraped."}
{"date":"2026-08-21","scrapers":["supabase-adapter.js"],"category":"code-bug","summary":"The junk-title GOVERNANCE rule was DELETING the real library programme \"T(w)een Advisory Board\". Two changes landed on 2026-08-20 and contradicted each other from the moment they shipped: detectAgeRange() learned the t(w)een spelling, while the AUDIENCE_RESCUE guarding the advisory-board rule kept a plain tweens? word-boundary pattern that the parentheses defeat. The rescue is explicitly commented LOAD-BEARING precisely because a bare advisory-board rule deletes Teen/Tween Advisory Board, and it silently failed for the one spelling added the same day. Caught while verifying Ringwood Public Library for the Step 3d all-ages population - the live calendar runs a Tween Advisory Board, a Youth Advisory Board and 7th Grade Summer Math Tutoring while 74 of 94 rows sat in All Ages. This is the worst class of bug in this codebase because isJunkTitle() deletes without marking, so prior losses leave no trace and cannot be counted retroactively. Widened AUDIENCE_RESCUE with an explicit t(w)eens? alternative and a comment tying the two regexes together so they cannot drift apart again. Both directions re-verified: T(w)een / Tween / Teen Advisory Board and t(w)eens advisory committee all survive with correct brackets, while Town Council, Planning Board, Zoning Board of Review, Municipal Court and Historic District Commission are all still deleted. Added two regression cases to test-junk-titles.js, now 38/38; test-age-detection.js still 45/45. STILL OPEN and deliberately not fixed today: grade-level phrasing in a title (7th Grade Summer Math Tutoring) still resolves to All Ages."}
{"date":"2026-08-21","scrapers":["supabase-adapter.js"],"category":"code-bug","summary":"detectAgeRange() had no rule for a SINGLE grade. Every grade regex required either a second number (closed ranges, K-to-N) or an and-up phrase, so a lone grade fell straight through to All Ages. Found on Ringwood Public Library (WordPress-NJ), flagged at 79% All Ages in the 2026-08-21 audit, whose 7th Grade Summer Math Tutoring sat in All Ages despite naming its audience in the title. Added a last-of-the-grade-rules single-grade match, deliberately placed AFTER the closed-range and and-up regexes so a range is never truncated to its first grade - verified with Grades 3-5 still resolving to Kids and grades 6 and up still resolving to Tweens. The digit-first form REQUIRES an ordinal suffix rather than scanning bare digits, which is the same over-broad pattern the 2026-08-03 fix was written to avoid. Kindergarten is deliberately excluded: 1000 Books Before Kindergarten is a ubiquitous BABY programme and mapping a bare kindergarten to age 5 would file it as Preschool, the opposite of correct. Measured before/after with git stash rather than assumed: 7th Grade Summer Math Tutoring goes All Ages -> Tweens (9-12), and 1st Grade Story Hour moves Preschool -> Kids, which is the correct direction since a first-grader is six. grades 6 and up reads Tweens both before and after, so that is pre-existing lower-bound bucketing and NOT a regression from this change. Seven cases added to test-age-detection.js including two negative controls (upgrade, 1000 Books Before Kindergarten); suite 45/45 -> 52/52."}
{"date":"2026-08-21","scrapers":["list-url-collisions.js"],"category":"other","summary":"Added scripts/list-url-collisions.js, the missing triage half of gate 2 (Defect A). project-status.js reports the gate as one number - 504 entries on 166 hosts claimed by 2+ states - which names the defect but is not actionable: it does not say which hosts, which states, or which are dangerous. This turns it into a worklist, worst first, and asserts parity with the gate via --check (prints the identical 504/166 so a divergence between the two loaders is detectable). It also separates the three kinds the gate lumps together, because they need opposite fixes: multi-state platforms that are correct by design (macaronikid.com, libcal.com, activecommunities.com), shared registrable domains, and TRUE single-institution collisions. Result: 162 of the 166 colliding hosts are TRUE collisions covering 495 entries, and ALL 495 are WordPress-* - confirming these are the guessed {city}library.org entries CLAUDE.md already warns about. Note for whoever runs this next: a dns.resolve4 sweep of the 162 hosts is NOT a valid liveness test in this sandbox. It returned 162/162 dead, but google.com fails identically with ECONNREFUSED, so the resolver is blocked and the result measures the environment rather than the domains. Use verify-sites-puppeteer.js, whose browser stack reaches these hosts."}
{"date":"2026-08-21","scrapers":["WordPress-AL","WordPress-CT","WordPress-DE","WordPress-FL","WordPress-GA","WordPress-KY","WordPress-MA","WordPress-ME","WordPress-MS","WordPress-NC","WordPress-NH","WordPress-NJ","WordPress-NY","WordPress-PA","WordPress-SC","WordPress-TN","WordPress-VT","WordPress-WV"],"category":"seed-data","summary":"Defect A, first real bite. WordPress-* entries were scraping OTHER STATES' libraries because 83% of their config URLs (3,156 of 3,802) use the guessed {city}library.org shape CLAUDE.md warns about, and where two guesses collide on one string every claiming state scrapes the single real library behind it. NOT hypothetical: 1,983 of 8,899 WordPress-* rows in the DB come from a colliding host, the same site feeding several states - williamstownlibrary.org under NJ 127 / VT 56 / NY 49 / WV 11 / MA 9, baxterlibrary.org under ME 116 and TN 111, belfastlibrary.org under NY 159 and ME 44. Established ground truth for 13 hosts from LIVE PAGE evidence only - street address, ZIP, phone area code, or verify-sites-puppeteer's url-collision verdict which reads those same signals - never name similarity, which has produced wrong answers here before. madisonlibrary.org is Madison County PL in Richmond KENTUCKY and no KY entry even claims it, so all 11 eastern claimants are wrong; springfieldlibrary.org is Springfield MA (Brightwood is its own branch, so the MA entry is the correct one and was KEPT); chesterlibrary.org is Chestertown NY; oaklandlibrary.org is California, outside the region entirely. Added scripts/disable-collided-urls.js (dry-run default) which flags the 45 wrong entries with urlCollision and installs a run-time guard in all 21 active WordPress files. DELIBERATELY DOES NOT DELETE: the entry stays so the library remains a described gap rather than an unknown one, and the guard emits the 📍 header AND a Found 0 events line because the audit builder pairs them - dropping that pair is exactly what hid BiblioCommons-* and Communico-* from the audit until 2026-08-20. Inactive-region WordPress files are skipped entirely rather than flagged, because a flag with no guard to read it looks handled and is not. All 21 modified files pass node -c. Gate 2 moves 504 -> 448; that is a real reduction in RISK, NOT a URL being corrected, and a METHODOLOGY CHANGE note now renders in STATUS.md so the drop is never read as coverage improving."}
{"date":"2026-08-21","scrapers":["WordPress-CT","WordPress-GA","WordPress-KY","WordPress-MA","WordPress-ME","WordPress-MS","WordPress-NH","WordPress-NJ","WordPress-NY","WordPress-PA","WordPress-TN","WordPress-WV"],"category":"seed-data","summary":"Second pass on Defect A, and the guard is now PROVEN LIVE rather than reasoned. Ran WordPress-DE while the rotation was idle enough to allow one small scraper: it logged the skip line for Laurel Public Library, still emitted Found 0 events, and build-library-site-audit.js then produced all 14 DE rows INCLUDING Laurel at 0 - so a disabled entry keeps its audit row as an explained gap instead of disappearing, which was the whole design requirement. Rehoboth Beach returned 95 events in the same run, so the guard does not disturb the loop. Extended GROUND_TRUTH from 13 to 18 hosts, disabling 13 more entries (58 total). The striking finding is that FOUR of the five new hosts resolve OUTSIDE the 22-state active region, so no entry anywhere is correct for them: spartalibrary.org is Sparta Free Library in WISCONSIN (124 W Main Street, 54656, area 608) yet is claimed by NJ/TN/GA/NC and had already put 173 rows in the DB; newlondonlibrary.org is New London WISCONSIN (54961, area 920) claimed by CT/NH, which is why both showed identical LEGO Build events; dallaslibrary.org is Dallas TEXAS claimed by WV/PA with 91 rows; bathlibrary.org is Bath County Memorial Library in Owingsville KENTUCKY (area 606) claimed by NY/ME/NH, which finally explains why Dormann NY, Patten ME and Bath NH all reported the same Bookmobile routes - and separately closes the Dormann Library all-ages MISMATCH raised earlier the same day, since that 96%-All-Ages bookmobile programming is Kentucky's, not New York's. florencelibrary.org is genuinely Florence County SC, so the SC entry was KEPT and only MA/KY/MS disabled. Gate 2 now 434 entries on 148 hosts, down from 504/166, with 58 excluded as disabled."}
{"date":"2026-08-21","scrapers":["WordPress-KY","WordPress-MA","WordPress-MS","WordPress-NC","WordPress-NY"],"category":"seed-data","summary":"Third pass on Defect A. Four more hosts resolved from live page addresses, disabling 7 more entries (65 total, gate 2 now 425 entries on 144 hosts, down from 504/166 at the start of the day). Two more sit outside the active region: perulibrary.org is Peru Public Library in Peru ILLINOIS (1409 11th Street, 61354, area 815) but is claimed by MA and NY with 29 rows already in the DB, and warsawlibrary.org is Warsaw Community Public Library in Warsaw INDIANA (310 East Main Street, 46580, area 574) claimed by NY/KY/NC. The other two resolved IN FAVOUR of an existing claimant, which is the outcome this process is equally meant to produce: littlefallslibrary.org really is Little Falls NJ (8 Warren Street, 07424, area 973) so the NJ entry was KEPT and only the NY claimant disabled, and dekalblibrary.org really is DeKalb County GA (215 Sycamore Street, Decatur 30030) so the GA entry was KEPT and only the MS claimant disabled. Recording the kept ones matters as much as the disabled ones - it is the evidence that the rule is identity-based and not simply deleting anything that collides."}
{"date":"2026-08-22","scrapers":["local-scraper-runner.js","MacaroniKid-ALL"],"category":"code-bug","summary":"The end-of-run recap invented a stats-less MacaroniKid-GroupN entry because runMacaroniGroup() spawns the group runner as a child process and could not see its results, so formatSummaryRow() rendered it as a warning row reading 0 0 0 0 ? on all 10 runs from 2026-08-08 to 2026-08-22 - a healthy Group 2 that had scraped 8,592 events across 7 states looked identical to a dead one. The three macaroni-runner-group files now write scrapers/logs/macaroni-group{N}-results.json at exit and the parent reads it back behind a finishedAt staleness guard, falling back to the old exit-code-only entry if the file is missing. Write half proven live via a group3 dry run; the parent read-back first executes on the next rotation."}
{"date":"2026-08-22","scrapers":["supabase-adapter.js","RecDesk-Parks"],"category":"code-bug","summary":"detectAgeRange() could not read three real age notations, found by adjudicating the Step 3c all-ages flag list against the events own titles: Gymnastics Building 2105 Nash St (RecDeskParks-lcncpr) sat at 174/175 All Ages while its titles read Gymnastics PreK2 4-5YR and Tumbling 6-8YR. Added (1) a bare numeric range anchored by a YR/YRS/year-old suffix, which is RecDesk house notation and carries no ages keyword, (2) PreK with a trailing level digit, which the old trailing word boundary could never match, and (3) the abbreviated Gr K and Gr K-2 grade forms. The YR anchor keeps the rule safe from the classic false positives - times carry a colon, registration IDs are four digits, prices carry a currency symbol - and all of those are in the suite. Regression suite grew from 52 to 63 cases, all passing."}
{"date":"2026-08-22","scrapers":["WordPress-TN"],"category":"seed-data","summary":"Sequatchie County Public Library was configured with dunlaplibrary.org, proven live by the Step 3d Puppeteer verifier to serve Dunlap Public Library District in Dunlap, ILLINOIS - the same {city}library.org guessing defect tracked in the _global note. It was the only one of the four collisions surfaced today that lacked the urlCollision guard; Union Free CT, Dalton-Whitfield GA and Effingham GA already carried it, which is why they correctly report zero. Added the guard so the entry is skipped rather than risking Illinois events being ingested under a Tennessee name. This is a RECORDED COVERAGE GAP, not a repair - no working URL for the real Sequatchie County library has been found yet."}
{"date":"2026-08-22","scrapers":["fix-event-quality.js","supabase-adapter.js"],"category":"code-bug","summary":"scripts/fix-event-quality.js STEP 1b implemented its own hand-rolled junk-title predicate instead of calling the shared isJunkTitle(), so the two forked silently and every rule added to the helper was unreachable from the backfill path - the municipal-governance rules added 2026-08-20 had never removed a single pre-existing row. Proven live rather than reasoned: today's chain deleted 76 junk events while all 20 governance rows under South Kingstown Parks and Recreation survived, even though isJunkTitle() classifies every one of them as junk. STEP 1b now delegates to isJunkTitle() and retains its local checks as a supplement. The 385 newly-reachable rows were dry-run, reviewed title by title (municipal councils/boards/courts, Skip to main content, Ongoing events for <range> artifacts, bare date strings - no real programming) and deleted; South Kingstown went 21 to 0 while 9 T(w)een and 20 Teen Advisory Board rows survived, confirming AUDIENCE_RESCUE still protects them."}
{"date":"2026-08-23","scrapers":["WordPress-ALL","disable-collided-urls.js","purge-wrong-state-collision-rows.js"],"category":"seed-data","summary":"Bulk resolution of Defect A. Ran verify-sites-puppeteer.js once per colliding HOST with the sentinel expected state ZZ - because its identity check reports every address-shaped state mention that differs from the expected one, and no page is in ZZ, this makes it NAME the state each page actually belongs to instead of only firing against one claimant. 53 of 140 hosts resolved that way plus 2 dead (cooperstownlibrary.org DNS-fails under the stealth browser, edgewaterlibrary.org returns HTTP 503). The other 85 stayed UNVERIFIABLE and were deliberately NOT disabled - an unresolved host is unknown, not safe. Added DEAD as a marker distinct from a state so a dead host disables every claimant without inventing a geography. Total disabled entries went 66 -> 187 and gate 2 fell 504 -> 272 entries on 89 hosts. Notable resolutions: williamstownlibrary.org is NY (its NY claimant KEPT, MA/NJ/VT/WV disabled), kingstonlibrary.org NY, morristownlibrary.org NJ, salemlibrary.org OREGON, and a long tail outside the active region entirely - MI, OH, IL, KS, TX, CO, NE, WI. A BUG IN MY OWN FIX was caught by the mandatory node -c pass: the DEAD reason text contained an apostrophe (\"no state's entry\") which terminated the single-quoted JS literal and broke four config files; the generator now forbids apostrophes in that string and the four files were repaired. SEPARATELY, added scripts/purge-wrong-state-collision-rows.js and deleted 1,180 rows that these collisions had already written under the wrong state, with owner approval. It reads GROUND_TRUTH from disable-collided-urls.js rather than duplicating it, selects the full id list in a read-only pass BEFORE deleting anything so the delete never walks a shifting result set, always orders before ranging per the 2026-05-15 incident rule, de-dupes ids defensively and refuses above a 3000 ceiling. It queries per-host rather than one scraper_name ilike sweep because that sweep is a full scan and died with a statement timeout while the rotation was hammering the same table. The control that proves the host match is real: 417 rows on those same 76 hosts carried the CORRECT state and were left untouched, and a re-run reported 0 wrong / 417 right."}
{"date":"2026-08-23","scrapers":["activities-table"],"category":"other","summary":"Checked whether the URL-collision contamination reached the activities table as well as events. It did, but it is a DIFFERENT and much milder problem and was deliberately left alone. 88 activity rows sit on the 76 ground-truth hosts; 73 of them carry a source pointing at another state's library - madisonlibrary.org appears as the source for ten separate venue rows under WV, CT, NC, FL, NH, AL, ME, NJ, GA and MS. The crucial difference from events: an activity row is a VENUE, and its name, city and state came from the scraper's own config entry rather than from the scraped page, so Madison Public Library in Madison NJ is a REAL place that is correct in every field a user sees. Only the provenance field is wrong. Deleting these would remove genuine libraries from the map to fix a field nobody looks at, which is strictly worse than leaving them, so no deletion was performed. Recorded here so the discrepancy is not rediscovered later and mistaken for the events problem, which WAS user-visible (Kentucky's programming stored as New Jersey events) and was purged. If this is ever worth fixing, the correct action is to null or re-derive activities.source, never to delete the row."}
{"date":"2026-08-23","scrapers":["WordPress-ALL","resolve-collision-host-state.js"],"category":"seed-data","summary":"Fifth pass, and the end of the bulk phase of Defect A. Built scripts/resolve-collision-host-state.js because the ZZ-sentinel trick left 85 of 140 hosts unsettled for two reasons that were both properties of HOW we looked rather than of the sites: about 56 rendered fine but keep their address on a contact page rather than the homepage, and about 29 failed at the network layer purely because every one was fetched as https://www. and plenty of these sites do not serve TLS on the www name. The new resolver widens both axes - three URL variants, nine likely contact/about/hours/locations paths, and three independent signals weighted by how hard each is to fake in prose: ZIP-anchored 'ST 12345' (weight 5), phone area code via a NANP map (weight 3, with codes that straddle a state line OMITTED rather than guessed), and 'City, Full State' requiring the comma so running prose like 'our Virginia Woolf group' cannot score. It outputs EVIDENCE not a decision: a state is proposed only when the winner clears 3 points AND doubles the runner-up, otherwise CONFLICTED or UNRESOLVED. Validated against four hosts with independently known answers BEFORE trusting it on unknowns - ringwood NJ, belfast ME, mountainside NJ, sparta WI, all correct. Result: 46 of 85 resolved, 18 UNREACHABLE, 20 UNRESOLVED, 1 CONFLICTED; the 39 it could not settle are deliberately NOT in GROUND_TRUTH. Two results matter beyond the count: worcesterlibrary.org really IS Maryland, so the MD claimant is correct and was KEPT and its 34 rows were never wrong - which is a useful corrective given the Worcester incident in the playbook - and brownelllibrary.org really is VT so those 84 rows were fine too. hartfordlibrary.org turns out to be Hartford WISCONSIN, so the 70 VT rows were wrong and were purged. Totals across the day: disabled entries 66 -> 291, gate 2 504 -> 142 entries on 44 hosts, and 1,345 wrong-state rows deleted across two purge runs with 574 correct rows on the same hosts left untouched as the control."}
{"date":"2026-08-23","scrapers":["WordPress-ALL"],"category":"seed-data","summary":"Sixth pass: five colliding hosts turned out to be EXPIRED DOMAINS RESOLD to unrelated sites, which is why no resolver could ever name a state for them. franklinlibrary.org - claimed by NINE states - returns HTTP 301 to running-care.com, a French running-injury blog; chelsealibrary.org 302s to ww547.chelsealibrary.org with a token query, the classic parked/ad-monetised shape; brentwoodlibrary.org redirects to the bare IP 165.22.62.213 and then 403s; huntingtonlibrary.org and harmonylibrary.org redirect to spool.swatcha.net and fresupdate.com. This is the dead/hijacked-domain bucket in SCRAPER-DIAGNOSIS-PROMPT.md - a seed-data problem, not an extraction bug, so selector work would never have fixed any of them. Marked DEAD, which disables every claimant since none can be right; 20 more entries disabled, gate 2 142 -> 122 on 39 hosts. The redirect TARGETS were deliberately not fetched: the redirect itself is the evidence and the targets are untrusted third-party content. SEPARATELY, documented in purge-wrong-state-collision-rows.js why DEAD hosts are excluded from the purge rather than swept up with the rest. It looks like an oversight and is not: a dead host has no true state so wrong-state is undefined for it, and its leftover rows are AMBIGUOUS rather than wrong - franklinlibrary.org still has 10 rows tagged VT, and Franklin VT is a real library, so those are most likely genuine events captured before the domain expired. Deleting them would destroy possibly-real data to tidy a host that already stopped being scraped; they age out through the normal past-event cleanup instead."}
{"date":"2026-08-23","scrapers":["supabase-adapter.js","MacaroniKid-ALL"],"category":"code-bug","summary":"detectAgeRange()'s not-family guards were evaluated over the WRONG FIELDS, so a single incidental word in a description cancelled an explicit audience keyword in the title. The teen / kids / tots / storytime rules each carry a guard whose documented purpose is to keep \"Family Storytime\" out of Preschool - a statement about the event's own TITLE - but every one tested name + description combined. Found by adjudicating the Step 3c all-ages flag list against the events' own titles rather than accepting the counts: Wilmington Memorial Library (MacaroniKid-MA-burlingtonma) sat at 75% All Ages and its \"Bilingual Story Hour\" resolved to All Ages ONLY because the blurb names the \"Community Teamwork FAMILY Resource Network\" - an organisation, not an audience. The same shape was far worse elsewhere: \"Teen Book Club\" with \"Bring the family!\" in the blurb also resolved to All Ages, discarding an explicit teen label. Guards are now field-scoped through a guarded() helper: when the keyword is in the title, only the title can veto it; when it appears only in the description, the whole text still applies, so a blurb reading \"a family storytime for all ages\" under a generic title is unchanged. The ADULTS guard was DELIBERATELY LEFT WHOLE-TEXT and is the asymmetry that matters - filing a family event as preschool loses a filter, while filing an adults-only event as a children's event puts it in front of the wrong audience; \"Storytime\" with an adults-only blurb still returns null, verified as a negative control. Separately, a BRACKETED \"(All Ages)\" label in a title now outranks the keyword rules (\"Musical Storytime (All Ages)\" was resolving to Preschool) but is deliberately placed AFTER the three numeric rules so real numbers still win, and deliberately excludes running prose so \"Hunt & Riddle - For kids of all ages\" is untouched. MEASURED, NOT REASONED: over the 7,643 All-Ages rows in the current window, 242 (3.2%) now resolve to a specific bracket - 104 Preschool, 60 Kids, 12 Teens, rest explicit numeric. Suite 63 -> 74 cases, including six covering both directions of the field scoping. Save-time fix: existing rows do not move until re-scraped."}
{"date":"2026-08-23","scrapers":["supabase-adapter.js","fix-venue-title-quality.js","MacaroniKid-PA","MacaroniKid-MA","MacaroniKid-RI"],"category":"code-bug","summary":"Two title-extraction defects found in the same all-ages adjudication, both fixed at save time AND backfilled to zero. (1) DOUBLED TITLES: 33 rows stored the event name twice with no separator - \"Disney's The Lion KingDisney's The Lion King\", \"Ninja KidzNinja Kidz\" - all from Pittsburgh Cultural Trust listings reaching MacaroniKid-PA-southhills and -cityofpittsburgh, where a container-level textContent read concatenates a visible heading and a second nested copy. Besides being unreadable this destroys word boundaries, so \"Ninja KidzNinja Kidz\" is not a \\bkids?\\b match and the row cannot be age-detected either. New collapseDoubledTitle() carries TWO load-bearing guards because English has real doubled words: each half must be multi-word and at least 8 chars, which is what keeps couscous / bonbon / cancan intact, while separated repeats like \"New York, New York\" and \"Duran Duran\" never produce equal halves in the first place. (2) SCREEN-READER CHROME: 61 rows across four MacaroniKid sites carried \"(Opens in a new tab)\" welded onto the title, including \"Toddler Storytime(Opens in a new tab)\" and \"Baby Storytime(Opens in a new tab)\" - sr-only text inside the anchor. PROMO_BRACKET_RE extended to cover it plus the window / external-link variants. BOTH BACKFILLS PROVEN LIVE: doubled 33 -> 0, cruft 102 -> 0. New regression suite scripts/test-title-cleaning.js, 27 cases, over half of them negative controls."}
{"date":"2026-08-23","scrapers":["fix-venue-title-quality.js"],"category":"code-bug","summary":"The title-rename loops SWALLOWED UPDATE ERRORS and then reported the number of rows they had INTENDED to change as the number changed, so the script printed a clean success over work that had not happened. Caught only because a re-run kept finding the same 4 rows: it reported \"Cleaned 4 titles\" three runs in a row while the count stayed at 4. The real error was idx_events_unique_content - cleaning \"Books and Melodies in the Courtyard(Opens in a new tab)\" collides with the already-correct \"Books and Melodies in the Courtyard\", because the two are the same event scraped through two different link elements. So the rename is impossible AND the row is a genuine duplicate. All three rename loops now go through renameOrDedupe(), which checks the error, and on a unique-content collision CONFIRMS the clean twin exists with its own query before deleting the loser - never on the strength of the error message alone. Converged 4 -> 0 live. Also added --only=<ids> step selection (1, 1b, 2, 3) so a targeted backfill no longer has to accept an unrelated rewrite as a side effect: step 2's normalizeShoutedTitle() lower-cases a leading acronym in a long shouted title (\"AAAC ART -...\" -> \"Aaac Art -...\"), a known rough edge that is NOT fixed here and should not be dragged along by an unrelated run."}
{"date":"2026-08-23","scrapers":["fix-all-data-quality.js","fix-event-quality.js"],"category":"code-bug","summary":"A statement timeout made the nightly data-quality step a SILENT NO-OP THAT REPORTED SUCCESS. fetchAll() in fix-all-data-quality.js broke out of its pagination loop on the first error and returned whatever it had accumulated - on a page-1 failure, an empty array - and every caller reads empty as \"nothing to fix\". In today's chain it printed \"Error: canceling statement due to statement timeout\", then \"Total events: 0\", then zeros for all three steps, then \"ALL DONE\", over a 24h window that actually held roughly 4,700 newly-scraped MacaroniKid events. Nothing anywhere recorded that the window had been skipped; a broken run was byte-for-byte indistinguishable from a healthy one, which is the exact failure mode the diagnosis preflight exists to prevent. fetchAll() now retries three times with linear backoff (matching fix-event-quality.js's existing shape) and THROWS if it still cannot read a page, so main()'s catch exits non-zero and the chain reports the step as FAILED for the right reason. The same short-read break in fix-event-quality.js was changed to throw as well - its STEP 2b printed \"Found 0 duplicate events to remove\" immediately after two timeout retries the same day, which is indistinguishable from a genuinely clean table. PROVEN LIVE on the happy path: the script now reads 2,189 events in the same --recent-only window where it read 0, and exits 0 with no libuv teardown assertion. The throw path itself is deliberately unexercised."}
{"date":"2026-08-23","scrapers":["MacaroniKid-ALL"],"category":"other","summary":"Closed out the MacaroniKid pacing-tune pending item with a measurement rather than an impression, and the honest answer is that it did not demonstrably work. Its Group 1 rotation finally completed 2026-08-23T11:17Z. Wall clock fell 1249.6 -> 1035.9 min (-17.1%) and all nine states got faster, which reads as a clear win - but FOUND fell almost exactly as much over the same states, 4123 -> 3473 (-15.8%), so per-event time moved 18.2s -> 17.9s, a 1.6% change inside normal noise. PA on its own: pre-tune runs spanned 21127-25705s and the post-tune run was 21424s, inside the existing spread. The FOUND drop is NOT a coverage regression - RI has bounced 165/149/73/86/302/53 across six runs, so 302 was the outlier and 53 is unremarkable. Recorded as a pinned note on MacaroniKid-PA rather than left in _pending, because the trigger HAS occurred and re-waiting for it would just re-derive the same answer. If MacaroniKid runs need to be faster, 18s per event is the number to attack, not wall clock."}
{"date":"2026-08-23","scrapers":["resolve-collision-host-state.js"],"category":"code-bug","summary":"The collision resolver was Chrome-only and its biggest failure class was an artefact of its own probe. On the 2026-08-23 pass it returned UNREACHABLE for 16 of the 35 remaining hosts, every one ending on net::ERR_BLOCKED_BY_CLIENT against the http:// variant. Its own header already warned that this is Chrome refusing to issue the request locally rather than evidence about the site — but it concluded such hosts were unknowable, and that conclusion was wrong: every single one answered a plain node:http request instantly (http://clintonlibrary.org -> 301 clintonpresidentialcenter.org; http://lexingtonlibrary.org -> 200 with 12 KB of real HTML). Rewrote the fetch layer node-first with Chrome as the fallback for the two things Node cannot do — render a JS-only site, and follow a script or meta-refresh redirect that leaves the status at 200. Node also exposes the REDIRECT CHAIN, which turned out to be the strongest identity signal available, and the chain counts EVEN WHEN THE DESTINATION THEN REFUSES US: a first cut inspected only the final URL, so clintonlibrary.org's hop to the Clinton PRESIDENTIAL library was discarded as a 403 and the largest collision on the board (nine claiming states) stayed invisible. Two narrower fixes in the same change, each traced to one stuck host: the area-code regex demanded a separator after a parenthesised code, so bethellibrary.org scored {} for three passes while its contact page read (203)794-8756; and nav-link discovery did not look for faq, which is the only page carrying goshenlibrary.org's address. VALIDATED BEFORE USE, not after: re-ran the six hosts with independently known answers (ringwood NJ, belfast ME, mountainside NJ, worcester MD, sparta WI, hartford WI) after every change — 6/6 correct each time. Yield on the unknowns went from 1 of 35 to 22 of 35 in one pass, then 31 of 35 with the landing-page reads. DO NOT reintroduce a dns.resolve4 pre-check; that module is still blocked in this sandbox and returns ECONNREFUSED for google.com."}
{"date":"2026-08-23","scrapers":["WordPress-ALL","disable-collided-urls.js"],"category":"seed-data","summary":"Seventh, eighth and ninth passes on Defect A: 32 hosts settled, 91 entries guarded, and gate 2 fell 122 -> 18 entries on 39 -> 7 hosts. True single-institution collisions went 35 -> 3. The headline is clintonlibrary.org, claimed by NINE states, which 301s to clintonpresidentialcenter.org and on to clintonfoundation.org — it is the Clinton PRESIDENTIAL library and was never a public library at all. Also lexingtonlibrary.org (six states) serving a NameBright registrar parking page, newtonlibrary.org (six states) literally reading 'This domain is for sale', monroelibrary.org and plainvillelibrary.org both landing on the same video-game site — two unrelated library domains on one destination being the signature of a parking network — newfanelibrary.org now an Indonesian gambling site and stoningtonlibrary.org its 'SEDANG MAINTENANCE' sibling. THE MOST IMPORTANT FINDING IS THE OPPOSITE ONE: five hosts were not dead but MOVED, and marking them DEAD would have deleted real coverage. greensborolibrary.org -> library.greensboro-nc.gov (NC), littletonlibrary.org -> littletonma.org (MA), newcumberlandlibrary.org -> cumberlandcountylibraries.org (PA), eastrochesterlibrary.org -> eastrochesterny.gov (NY), carlislelibrary.org -> carlislepubliclibrary.org (IOWA, so all three claimants wrong). Each target was fetched and its own address read to fix the state. The four thinnest score-based resolutions were hand-checked against the live page rather than accepted on points, because a wrong call disables a real library: shrewsburylibrary.org turned out to be Cuttingsville VT 05738, not the plausible-looking MA claimant. THREE HOSTS DELIBERATELY LEFT OPEN: montgomerylibrary.org, greenelibrary.org and newberrylibrary.org ETIMEDOUT on all four variants under both stacks, and a timeout is an absence of evidence — the same trap as the DNS sweep. middletownlibrary.org WAS marked dead, on different evidence: its IIS server answers, but root 403s with no default document and every path 404s, identically for Googlebot and a bare client. node -c passed on all 48 WordPress config files after each pass. Purge found 0 wrong-state rows to delete: these entries had been pointing at parked domains and had never written any."}
{"date":"2026-08-23","scrapers":["GoogleCalendar-Libraries"],"category":"seed-data","summary":"First URL CORRECTION of Defect A rather than a disable. The Berkeley County Library - Sangaree Library entry in scraper-gcal-libraries.js carried url: summervillelibrary.org/events — Summerville PENNSYLVANIA, the same guessed {city}library.org shape as the rest of the defect. Every other colliding entry has been guarded because its true owner was another state's library; this one is legitimately South Carolina's and only the URL was wrong, so guarding it would have been the wrong move. Harmless in practice — the scraper reads a Google Calendar ICS feed by calendar id and never fetches url at all, so no Pennsylvania events were ingested under a South Carolina name — but url is what LIBRARY-SITE-AUDIT.md links a reader to, and it fed gate 2 as a live collision. Corrected to https://berkeleylibrarysc.org/locations-and-hours/, verified live: the site titles itself 'Berkeley County Library System, South Carolina' and lists 843-719-4223 (843 = SC), and that path returns 200. Note this entry could not have been fixed by disable-collided-urls.js in any case: it only walks the WordPress config files, so a collision living in another family's config is invisible to it — worth remembering if gate 2 ever stops falling while entries remain."}
{"date":"2026-08-23","scrapers":["detect-site-platform.js"],"category":"other","summary":"Added scripts/detect-site-platform.js and reports/platform-mismatches.md, which turn the largest stuck bucket in the project into a worklist. Gate 4 holds 392 UNVERIFIABLE sites and 228 of them carry one identical comment - rendered fully but shows no dated events, no event containers and no explicit empty-state message, cannot distinguish. That is a real epistemic limit and re-fetching those pages cannot move it, so the backlog had sat untouched. The productive question is not can we see events but WHY can we not: no events in the DOM has two causes needing opposite fixes, either the library genuinely has none, or its events live on a platform this codebase already parses and the WordPress DOM extractor cannot see any of them. Of the 152 unverifiable sites that matched a live non-collided WordPress-{state} config entry, 40 are the second kind - 7 Assabet, 6 Google Calendar iframes, 5 LibCal, 5 The Events Calendar, 4 BiblioCommons, 3 CivicPlus, 2 Communico, 1 LibraryMarket and 7 on LibraryAware, which is a newsletter tool with no calendar to scrape at all. Henrico County VA on LibCal, Warren County KY on BiblioCommons, Hartford Public Library CT on Communico: every one is filed where it can only ever return 0, and no amount of selector work would change that. A further 24 turned out to be seed-data problems in disguise - off-host, parked or empty URLs that never collided with another state and so never appeared in gate 2. The script fetches with node:http rather than Chrome for the reason established earlier the same day, matches only host and product strings unique to one platform (never the word calendar, which every library site carries), and names the registry family each entry belongs in. It deliberately decides nothing: a platform marker proves the platform is REFERENCED, not that it holds that library's events, so the file says confirm the feed carries real dated events before relocating. FALSIFIED ON THE WAY: the first hypothesis was that these sites expose a machine-readable Events Calendar REST feed the scraper ignores. Probed 30 sites for /wp-json/tribe/events/v1/events, /wp-json/wp/v2/tribe_events, ?ical=1 and /events/feed/ - 0 of 30 returned future events, so that idea was dropped rather than built on."}
{"date":"2026-08-23","scrapers":["WordPress-CT","WordPress-ME","WordPress-VT"],"category":"seed-data","summary":"Four URLs corrected from live-page evidence, found by the new platform detector rather than by a state collision - none of these four ever appeared in gate 2 because no second state claimed them. THE URGENT ONE: Norwalk Public Library CT was configured with norwalkpubliclibrary.org, which now redirects to agentaruhanjuditogel.com, an Indonesian online-gambling domain. An expired library domain resold to a gambling site is a CONTENT-SAFETY problem on a family events site, not merely a coverage one, and it is the second such case found - newfanelibrary.org was the first, discovered hours earlier in the collision work. Corrected to norwalkpl.org, which reads Norwalk Public Library, 1 Belden Avenue, Norwalk, CT 06850, phone 203-899-2780. Waterbury Public Library CT was configured with siloam.com, which serves an empty page and is not a library; Waterbury's public library is the Silas Bronson Library at bronsonlibrary.org, 267 Grand Street, Waterbury, CT 06702, 203-574-8225. Portland Public Library ME had .com where the site is .org, and Stowe Free VT pointed at stowelibrary.org which redirects to stowefreelibrary.org, 90 Pond Street, Stowe VT 05672, 802-253-6145. THE CONTROL THAT MAKES THESE EVIDENCE RATHER THAN NAME GUESSES: in every one of the four, the ZIP printed on the replacement site matches the ZIP already stored in the config entry. Deliberately recorded in the code comments: correcting these URLs does NOT fix their extraction - Norwalk runs CivicPlus, Waterbury runs LibraryAware, Portland runs LibCal - so all three stay on the relocation worklist. South Portland ME was left alone: its .com 403s and both .org variants are ENOTFOUND, so there is no proven replacement. node -c passed on all three files."}
{"date":"2026-08-23","scrapers":["GoogleCalendar-VT","WordPress-VT","scraper-gcal-libraries.js"],"category":"new-coverage","summary":"First relocation off the platform-mismatch worklist, and it is PROVEN LIVE rather than reasoned. Roger Clark Memorial Library (Pittsfield VT) publishes its entire programme through an embedded Google Calendar, so WordPress-VT's DOM extractor could never read a single event from it. Moved to the existing GoogleCalendar family as GoogleCalendar-VT, registered group 3. Ran it: 45 occurrences found, 3 real events saved after the junk rules below, a Roger Clark Memorial Library activity row created, all rows carrying state VT, the right venue and the correct source_url. A NOTE ON THE HOST, because it looks dead and is not: scraper-stderr.log had been logging net::ERR_BLOCKED_BY_CLIENT against pittsfieldlibrary.com for weeks, which is Chrome refusing the request locally rather than anything about the site — fetched with node:http it answers instantly. Identity confirmed before wiring from the live page: Pittsfield's Roger Clark Memorial Library, 40 Village Green, PO Box 743, Pittsfield VT 05762, 802-746-4067, ZIP matching the WordPress entry exactly."}
{"date":"2026-08-23","scrapers":["GoogleCalendar-GA","WordPress-GA"],"category":"seed-data","summary":"TWO RELOCATIONS REJECTED AT THE LAST MOMENT, and the near-miss is the most useful thing in this entry. cairolibrary.org and thomsonlibrary.org were both queued for GoogleCalendar-GA: the platform detector correctly identified Google Calendar embeds, and both feeds were live and real — 91 VEVENTs with 6 upcoming, and 316 VEVENTs respectively. Everything checked out except the one thing that matters. Reading the actual pages: cairolibrary.org is Cairo Public Library, 15 Railroad Ave, Cairo, NEW YORK 12413, (518) 622-9864 — Roddenbery Memorial Library in Cairo GEORGIA is a different institution in a same-named town, and WordPress-GA had already removed that entry on 2026-08-11 for precisely this reason, so the existing note was right and I was about to contradict it. thomsonlibrary.org titles itself Thomson Illinois York Township Public Library, phone 815-259-2480, area 815 = ILLINOIS. Wiring either would have imported another state's events under a Georgia library's name, which is the exact failure the whole URL-collision effort exists to undo, arrived at from a completely new direction. A WORKING FEED SAYS NOTHING ABOUT WHOSE FEED IT IS. The GoogleCalendar-GA registry key was drafted and then deliberately not added; the rejection is recorded in scraper-gcal-libraries.js so nobody re-queues them. Consequences: the per-site slug added to scraperNameFor() was REVERTED, since Georgia was the only prospective two-library state and a speculative rename would have churned attribution on MD/MA/SC rows for nothing. Separately, the WordPress-GA Thomson entry gained a urlCollision guard — it is the same {city}library.org guessing defect as Defect A, but it never showed in gate 2 because no second state happened to claim that host. RECORDED COVERAGE GAP: no working URL for the real Thomson-McDuffie library yet."}
{"date":"2026-08-23","scrapers":["supabase-adapter.js","GoogleCalendar-VT"],"category":"code-bug","summary":"isJunkTitle() had no rule for OPENING-HOURS markers, and the gap showed up the instant a new scraper ran. GoogleCalendar-VT's very first run saved 42 rows and 39 of them were the single title Library OPEN — a small library runs one Google Calendar for both its programme and its open/closed status, so every opening day arrives as a recurring all-day event. Two of the 42 rows were real programming. Left alone, one village library would have out-posted entire states. Added four anchored rules covering the status class: library is/will be open|closed, a bare Open or Closed, library closing, and closed today/early/for the holiday. ANCHORED AT BOTH ENDS ON PURPOSE, because open is a common word in real event names — Open Mic Night, Open House, Open Play, Open Gym, Open Swim, Grand Opening, Opening Reception and Library Open House all carry content beyond the status phrase and all survive; each is now a negative control in test-junk-titles.js, which grew 38 -> 54 cases. PROVEN IN BOTH DIRECTIONS ON REAL DATA: the 39 existing rows were deleted through a helper that calls isJunkTitle() rather than re-implementing it (the 2026-08-22 predicate fork is exactly what that avoids), taking the scraper from 42 rows to 3; then the scraper was re-run and reported 3 saved, 42 skipped, 39 junk title, so the save-time path rejects them too. DELIBERATELY NOT FIXED: a bare holiday name (Labor Day) still saves as an event. It is one row, and a bare-holiday rule would put real festival listings at risk."}
{"date":"2026-08-23","scrapers":["WordPress-FL","WordPress-CT"],"category":"seed-data","summary":"Tenth pass on Defect A, and it exists because gate 2 went UP by 2 and the cause needed naming rather than smoothing over. Correcting Waterbury Public Library CT from siloam.com to bronsonlibrary.org earlier the same day pointed a second state at a host WordPress-FL was already claiming for Levy County Public Library System in Bronson, FLORIDA - another guessed {city}library.org that had been invisible for exactly one reason: only one state claimed it, so gate 2 never counted it. The gate regressed while the data got MORE correct. Worth remembering before reading a future +2 as damage. Ground truth was already in hand from the correction itself - the site reads Silas Bronson Library, 267 Grand Street, Waterbury CT 06702, 203-574-8225, matching the CT entry ZIP exactly - so the CT entry is kept and the FL claimant is disabled. Gate 2 back to 18 entries on 7 hosts, of which only 3 are true single-institution collisions and all three are the hosts that time out under both node and the browser."}
{"date":"2026-08-23","scrapers":["supabase-adapter.js","fix-venue-title-quality.js","Library System of Lancaster County","ChildrensMuseums-Events-Eastern","Jefferson-Madison Regional Library","York County Libraries"],"category":"code-bug","summary":"HTML entities were being stored RAW in event titles and venue names and rendered literally on the live site - Rocky&#8217;s Book Club, Sit &amp; Stitch @ Wylliesburg Library, Tween Dungeons &amp; Dragons Club. This is a user-visible defect, not internal tidiness. Found while auditing the opening-hours junk rules added the same day: the surviving-titles control list was full of them. Measured across all 123,485 rows: 344 names and 7 venues affected, concentrated in four scrapers that read an attribute rather than textContent (Lancaster 97, ChildrensMuseums 85, Jefferson-Madison 63, York County 50), so a systematic extraction habit rather than stray data. Added decodeHtmlEntities() and composed it FIRST in the title chain and at the top of cleanVenueName() - order matters in both places: decoding first lets collapseDoubledTitle() compare real characters, and it lets the venue room-suffix rules see a dash that the entity had been hiding, which is how Osterhout Free Library &#8211; Central Branch kept its suffix. ANGLE BRACKETS ARE DELIBERATELY NOT DECODED: titles render as text so there is no injection either way, but decoding them can only turn readable text into what looks like broken markup, and neither appeared in any of the 123,485 rows. At most two passes, because double-encoded data is real (&amp;#8217;), stopping as soon as a pass changes nothing. Negative controls in the suite and verified live: a bare ampersand, Q&A with the Author, Rock &amp roll with no semicolon, and &lt;script&gt; are all untouched. Backfill added as fix-venue-title-quality.js step 1c and PROVEN LIVE: 351 rows fixed, re-run reports 0. test-title-cleaning.js 27 -> 37 cases."}
{"date":"2026-08-23","scrapers":["supabase-adapter.js"],"category":"other","summary":"Audited the blast radius of the new opening-hours junk rules across all 123,485 rows BEFORE the nightly chain could run that step with --save, because isJunkTitle() deletes without marking and a rule one word too broad leaves no trace. Result: 22 rows deleted, and every distinct title is a genuine closure notice - Closed x12, LIBRARY IS CLOSED x3, Library Closing @ Noon x2, LIBRARY CLOSED FOR COLUMBUS DAY, LIBRARY CLOSED FOR NATIVE AMERICAN HERITAGE DAY, Library Closing at 4 PM August 26th, Library Closing @ 5pm, Closed for the Holiday. Zero false positives. The control matters more than the count: 2,377 rows whose titles contain open or clos SURVIVE, including Open Play, Open Mic @ GWR Waxhaw, Teen Open Studio, Open Swim, Adult Basketball Open Gym, Open House / New Family Orientation, MUSEUM OPEN and Recreation Opening Day. Recording this so the rules are not later widened on the assumption they were under-catching: four closure-shaped titles do still survive (***CLOSED*** (City of Aiken Public Pool), CLOSED (Southport Ballroom), Closed - Christmas, Library Holiday - Closed) and were left alone deliberately - they carry a venue or a holiday name, which is exactly the shape a real event also has, and 4 rows is not worth the risk of a broader anchor."}
{"date":"2026-08-23","scrapers":["merge-verification-comments.js","generate-site-report.js","CivicRec-Parks-Eastern","MacaroniKid-NJ"],"category":"code-bug","summary":"A SCRAPER RENAME ORPHANED ITS VERIFICATION VERDICTS AND DUPLICATED ITS AUDIT ROWS. Raised by the owner reading the report: Jackson County Parks & Recreation appeared twice in the Age breakdown with identical totals of 115 - MATCHES under CivicRec-Parks-Eastern, UNVERIFIABLE under CivicRec-Parks-Eastern-jackson-county-ms. Checked against the database rather than assumed: ZERO rows remain under the bare name for that venue and 460 sit under the new one, so it is one site whose name moved, not duplicated data, and the older row is a historical audit artifact. Both stores are keyed on scraper_name, so a rename mints a second key for a site that has not changed. Step 3d's own don't-re-verify rule - if a site already has a non-UNVERIFIABLE comment, carry it forward and skip the fetch - is keyed on the exact scraper name, so the rename defeats it and the site is re-fetched as if never checked. Measured across the store: 18 rename pairs, 16 of which had traded a settled verdict (15 MATCHES, 1 MISMATCH) for UNVERIFIABLE. Added merge-verification-comments.js --migrate-renames (dry-run default): it treats a strict hyphen-prefix as a rename, carries ONLY in the settled -> UNVERIFIABLE direction, REFUSES to overwrite one settled verdict with another and reports that as a CONFLICT for a person, validates before writing, and is idempotent. Store 1282 -> 1266 entries, gate 4 392 -> 376. This matters forward more than backward: gate 6 counts 283 names still to migrate and the two largest drift families are hundreds of sites each."}
{"date":"2026-08-23","scrapers":["generate-site-report.js"],"category":"code-bug","summary":"Second half of the rename fix, and the interesting part is the number I did not trust. generate-site-report.js already collapses SITE-name drift within one scraper; it had no mirror for SCRAPER-name drift within one site, so a renamed scraper rendered every one of its sites twice. Added collapseRenamedScrapers(), keeping the post-rename row. The first cut keyed on cfgKey(site), the same normalization the existing collapse uses, and it collapsed 801 age rows rather than the handful I expected. Checking that instead of banking it found a real fault: cfgKey STRIPS A TRAILING PARENTHETICAL, so Rourk Meeting Room (rear half), (front half) and (combined) all normalize to one key - three separately bookable rooms, and dropping one because another exists under the renamed scraper is exactly the No aggregation, ever mistake that once wiped 10 SandhillRegional-NC branches to a single row. Tightened to EXACT site text, which loses nothing real because a rename by construction does not change the site text. Verified with an independent checker: 0 pairs whose raw site text differs (was 26), distinct sites preserved 10704 -> 10704, and all three Rourk rooms still present in the output. Jackson County now renders once, under the post-rename name, carrying its MATCHES verdict."}
{"date":"2026-08-23","scrapers":["LibCal-VA","LibCal-CT","WordPress-VA","WordPress-CT"],"category":"new-coverage","summary":"First two relocations off the platform-mismatch worklist. Henrico County Public Library (VA) and Wethersfield Public Library (CT) both run LibCal, so their WordPress entries could never read an event and both had been sitting at the UNVERIFIABLE renders-fully-but-shows-no-dated-events verdict. Moved to the shared LibCal config. INSTANCES PROVEN LIVE BEFORE WIRING, and proven WITHOUT Chrome because a Group 3 rotation was running and the fix-notes record concurrent heavy Chrome as the leading suspect for a 37-scraper launch failure: henricolibrary-va.libcal.com/calendar returns HTTP 200, titles itself LibCal - Henrico County Public Library and carries 50 event cards; wethersfieldlibrary.libcal.com the same with 38 cards and the matching title. Henrico is one of the larger systems in Virginia so this is a real gap closed. HONEST LIMIT: both are CODE-ONLY. LibCal-VA is group 3 and LibCal-CT group 2, neither has run since the move, so the relocations are unverified end-to-end and are recorded as such rather than claimed as working. Also fixed the instance regex, which had been reporting api3.libcal.com - every LibCal site loads the vendor API host, so matching it named the vendor rather than the institution; Wethersfield resolved correctly once api/static/assets hosts were excluded."}
{"date":"2026-08-23","scrapers":["verify-coverage.js","WordPress-NC","WordPress-AL"],"category":"code-bug","summary":"A COVERED-ELSEWHERE CHECK NEARLY DELETED A WORKING ENTRY, and the near-miss hardened the authoritative script. Of the first 8 platform mismatches with a usable instance, 5 turned out to be ALREADY CONFIGURED in their destination family - relocating them would have created duplicate coverage under two scraper names. Checking which were genuinely covered, an ad-hoc script compared scraper names CASE-SENSITIVELY and reported Hartford Public Library CT as covered by Communico on the strength of 80 rows that were actually its own, stored under the drifted lowercase name wordpress-CT. That is exactly the circular argument scripts/verify-coverage.js exists to prevent, and the repo script was fine - its norm() already folds case and punctuation. But it had a related gap that the active rename migration opens: it excluded only the EXACT audited name, so a renamed form of the audited scraper (Family-ST -> Family-ST-siteslug) would count as third-party evidence. Replaced the exact comparison with isSelf(), which treats a strict hyphen-prefix in either direction as the same scraper; nine unit cases including negative controls that Communico-CT-hplct, BiblioCommons-NC and WordPress-CTX are NOT self. Re-run with the hardened rule: Hartford correctly reads INCONCLUSIVE, and its WordPress entry is in fact WORKING with 80 future-dated rows, so its UNVERIFIABLE verdict is stale rather than its coverage being missing. Two entries WERE proven redundant and removed - Matthews Branch NC (BiblioCommons-NC holds 102 rows on cmlibrary.bibliocommons.com, 18 under venue Matthews) and Homewood AL (Communico-AL-homewood). Deliberately left alone: Beatties Ford Road NC shares the Matthews URL but has no row under its own venue name, and an absence is not proof; George Hail RI resolves to the Ocean State Libraries CONSORTIUM instance, so wiring it under one library name would attribute a consortium to it; Graham NC and Bridgeport CT are configured in LibCal already but that destination holds 0 rows, which makes the destination the thing to fix."}
{"date":"2026-08-24","scrapers":["supabase-adapter.js","LibCal-CT","LibCal-PA","LibCal-TN","Berks-County","Rockbridge-Regional","CustomDrupal-Libraries"],"category":"code-bug","summary":"FIXED THE ACTIVE ONGOING DATA LOSS recorded 2026-08-13 and pinned as STABLE-ID-COLLISIONS, and it is PROVEN LIVE rather than reasoned. Root cause confirmed by reading the code rather than inferred: ~25 call sites across the library and venue scrapers build their event url as event.url || library.website, so the instant per-event link extraction fails EVERY event at that library inherits ONE shared URL, _stableEventId hashes the url first, all of them collapse onto ONE row id and every event after the first is silently dropped by the insert. The previous session recorded this and deliberately did not fix it because both obvious remedies - extract a real per-event URL, or pin identity to name|eventDate|venue - change the id for EVERY event the scraper emits and would re-insert the whole fleet under new ids. THE WAY PAST THAT OBJECTION: the collision detector added 2026-08-09 already knows the exact moment a URL is PROVEN shared - when a second, differently-named event claims an id that is already taken. Added _resolveEventId() in supabase-adapter.js which re-keys ONLY that newcomer onto the name|eventDate|venue content key. Blast radius is deliberately tiny and is the point: events with genuine per-event URLs never collide so never reach the path and never churn, and the FIRST event under a shared URL keeps its url-derived id and does not churn either. The only ids that change are the ones currently being LOST. One central change repairs all ~25 call sites with no per-scraper edit, which is what makes it verifiable in one session where a 20-file sweep would not have been. TWO SAFETY PROPERTIES WORTH RECORDING: the re-key target is byte-identical to the DB idx_events_unique_content key (name+date+venue), so if scrape order ever flips between runs and ownership of the shared-url id changes hands, the resulting insert collides on CONTENT and returns duplicate:true rather than creating a duplicate row; and registration into _seenStableIds was moved to the RAW data.name instead of the flattened row.name, because flattenEvent rewrites titles (entity decode, ALL-CAPS to Title Case) and comparing a cleaned name against a raw one would make the same event look new on its next sighting and trigger a spurious re-key - one name source, both directions. PROVEN LIVE on the exact library in the original record: ran LibCal-CT, the re-key fired at Hamden Public Library on Preschool Storytime colliding with Time For Twos Storytime and the very next log line is a save, plus a second fire on All Together Storytime vs Fall Into Art Color Party. Verified in the database, not just the log: 9 distinct Hamden rows with 9 distinct ids in the run window, including two Preschool Storytimes on Sept 10 and Sept 17 and two Wonderful Ones Storytimes on Sept 14 and Sept 21 - four rows that under the old scheme would have been one. Regression suite scripts/test-shared-url-rekey.js, 13 cases, and the NEGATIVE half is the important half: a resolver that re-keys too eagerly does not lose events it DUPLICATES them, so the suite asserts ids stay put for first sightings, for ordinary re-scrapes of both normal and already-re-keyed events, and for genuine per-event URLs. HONEST LIMIT, not fixed here: the scrapers still EMIT the shared url, so the url column on those rows still points at a calendar page rather than the event. That is now a display-quality defect rather than data loss, and the 25 call sites remain the real repair."}
{"date":"2026-08-24","scrapers":["LibCal-VA"],"category":"code-bug","summary":"HENRICO COUNTY PUBLIC LIBRARY 0 -> 20 EVENTS, PROVEN LIVE. Two defects, found by clearing the _pending entry PLATFORM-MISMATCH-WORKLIST rather than by a failure report. FIRST, an ordering trap that would have been misread as a failed relocation: the 2026-08-23 move of Henrico from WordPress-VA to LibCal-VA was committed at 11:38Z on 08-24 while LibCal-VA had already run at 07:23Z, so today audit shows LibCal-VA visiting only Arlington, Massanutten and Fairfax with no Henrico line at all. That is rotation ordering, not a broken entry - the same shape as the AGE-DETECT-RECDESK note. Re-ran the scraper by hand to settle it instead of waiting a rotation. SECOND, with Henrico now actually reached it returned Found 0 events from a live site, and the two real causes were both confirmed against the rendered DOM rather than guessed. (a) WRONG PAGE: the config URL was the bare host https://henricolibrary-va.libcal.com, which is Henrico ROOM RESERVATIONS page and renders only a 5-item upcoming sidebar widget; /calendar renders 20 real event cards with per-event /event/<id> links. Both pages carry ~50 s-lc-eventcard markers in raw HTML, which is exactly why the plain-HTTP check used during the relocation could not tell them apart - a marker count is not a page identity. (b) ORDINAL DATES, the general half: LibCal default /calendar view renders Date: Mon Aug 24th 2026, and ALL SIX of the extractor date patterns fail on it - the weekday rule wants Monday not Mon, the two comma rules find no comma, and the loose Aug 24 fallback dies on the word-boundary between 24 and th. Verified by running the six patterns against the real card text before touching anything: six no-match. Added two ordinal-tolerant patterns plus an ordinal strip so downstream parsers, which have no ordinal handling, receive Aug 24 2026. ANCHORED ON REAL MONTH NAMES DELIBERATELY: a bare w{3,9} would read the literal word Date in Date: Mon Aug ... as a month, and that negative control is asserted. PROVEN END TO END: re-ran LibCal-VA, Henrico went 0 -> 20 found and 9 rows saved, verified in the database with correct dates, state VA, and a conforming per-site name LibCal-VA-henricolibrary-va. The ordinal fix is shared by every library in the file, so other LibCal sites on the default calendar view should also stop losing events. NOTED NOT FIXED: Henrico rows store a room code as the venue (VA Meeting Room, GA Meeting Room, LM Storytime Room) rather than a branch name, which will fragment its per-site audit rows."}
{"date":"2026-08-24","scrapers":["supabase-adapter.js","CivicRec-Parks-Eastern"],"category":"code-bug","summary":"detectAgeRange() could not read an OPEN-ENDED age minimum. Its explicit-range rule requires TWO numbers, so \"Ages 18+\" matched no numeric rule at all and fell through to the All Ages catch-all. Found by adjudicating the Step 3c flagged list against the titles actually scraped today rather than by re-fetching pages: CivicRec Recreation Campus (middletown-ny) stores \"October 2026 Registration - Ages 18+\" as All Ages. THE DIRECTION MATTERS AND IS WHY THIS IS NOT COSMETIC: adult-only rows are meant to be REJECTED at save time on a family events site, and they can only be rejected if they resolve to Adults first, so an unread 18+ does not merely mistag a row, it publishes it. Added an \"ages N+ / N and up / and older / and over\" rule returning the raw N+ form, because normalizeAgeRange() already buckets that natively (18+/21+/55+ -> Adults, 13+ -> Teens, 6+ -> Kids) — no bucketing logic duplicated. ANCHORED ON THE age(s) KEYWORD exactly like the closed-range rule, per the 2026-08-03 rule that unanchored numerics misread times, prices and registration IDs; three negative controls assert a bare N+ in a price, a room number and a capacity stays null. Suite 74 -> 83 cases, all passing, and junk-title 54/54, title-cleaning 37/37 and the new shared-url 13/13 were re-run to confirm no cross-effect. TWO THINGS DELIBERATELY RECORDED RATHER THAN CLAIMED. First, the adult cases had to move into ADULTS_CASES, which asserts resolveAgeRange() directly, because flattenEvent() rejects adult rows outright and cannot report a bracket — the first cut put them in the main block and crashed the suite, which is exactly the harness telling the truth. The suite total is now summed from all three groups after it briefly printed a nonsensical 83/80. Second, and OPEN: while proving this, rows whose titles the CURRENT guards reject were found being INSERTED daily anyway — Abington Fest 5K (Ages 18+) exists as three separate rows created 08-15, 08-18 and 08-24. That is a separate, unexplained content-safety bypass plus a non-stable-id defect, pinned as NONFAMILY-GUARD-BYPASS at high priority. This age fix does NOT close it. HONEST LIMIT: the new rule affects NEW rows only; existing All-Ages rows are unchanged unless scripts/backfill-age-range.js is run."}
{"date":"2026-08-24","scrapers":["WordPress-MA","WordPress-NH","WordPress-SC","WordPress-WV"],"category":"seed-data","summary":"ELEVENTH PASS ON DEFECT A, and the notable part is that NONE of these eleven could ever have appeared in gate 2. Every one is the same guessed {city}library.org, but each host is claimed by exactly ONE state, so there was no second claimant to collide with and the gate-2 duplicate-host detector was structurally blind to them - the same blind spot as the 2026-08-23 Thomson GA case. They surfaced from a completely different direction: the Step 3d Puppeteer verification of ZERO-EVENT sites, which reads the address on the page the scraper actually lands on. A library returning zero events turns out to be a good detector for a library that is not the configured library. Eleven wrong-state entries flagged urlCollision across four active config files: WordPress-MA loses six (blackstonelibrary.org is really CT/James Blackstone Library, brightonlibrary.org NY, carverlibrary.org ME, hollandlibrary.org NJ, lenoxlibrary.org MI/Lenox TOWNSHIP Michigan, westhamptonlibrary.org NY/Westhampton Beach), WordPress-NH two (effinghamlibrary.org IL, starklibrary.org OH - Stark County Ohio, not Stark NH), WordPress-SC two (estilllibrary.org KY/Estill COUNTY Kentucky, lamarlibrary.org GA/Georgia PINES Barnesville-Lamar) and WordPress-WV one (marioncountylibrary.org is really SC). Four of the eleven point outside the active region entirely (MI, IL, OH), where no entry anywhere could be correct. IDENTITY FROM THE LIVE PAGE ONLY, never name similarity - each verdict rests on address-shaped state mentions plus the page own title, which is the rule that exists because Lenox MA and Lenox Township MI are both real libraries whose names differ by one word. Ground truth recorded in disable-collided-urls.js so it stays the single register; evidence strings written WITHOUT apostrophes after an apostrophe broke four config files on 2026-08-22. node -c passed on all four touched files. PURGE RAN AND FOUND NOTHING, which is the expected and self-consistent result: these entries surfaced BECAUSE they return zero events, so they had written no rows to clean up - 603 rows scanned across 152 ground-truth hosts, 577 on the right state and 0 wrong-state ids. THESE ARE ELEVEN REAL COVERAGE GAPS, not resolved coverage: the towns of Blackstone, Brighton, Carver, Holland, Lenox and Westhampton MA, Effingham and Stark NH, Estill and Lamar SC and Marion County WV now have no working URL. Disabling is not deleting - each entry keeps its row in LIBRARY-SITE-AUDIT.md as an explained gap."}
